### 260810 학습내용

### SELECT 문법과 JOIN ###

1. 조건으로 좁히기

 (1) where 필터링

 <형태>
 SELECT column1, column2
 FROM table_name
 WHERE condition ;

 <기호>
- = <>           같다· 다르다
- < > <= >=      크기비교. 날짜에도 그대로 씀
- AND OR NOT     여러 조건을 엮는 것
- IS NULL        빈 값은 = 로 비교하지 않고 is null로 비교

 (2) LIKE 패턴검색 (어떤 값(문구)을 포함하는 값 가져오기)

 %는 글자 수 제한 없음, _는 정확히 한글자

 <형태>
 SELECT column1, column2
 FROM table_name
 WHERE columnN LIKE pattern;


 (3) AND OR NOT – 논리 연산자
 AND가 OR보다 먼저 계산
 괄호가 쳐지면, 괄호안 조건을 먼저 만들고, 그 다음 순차적으로 연산

 
 (4) DATETIME 탐색
 월별조회를 효율적으로 하는방법

< 권장 형태 >  (범위로 쓰기)
WHERE order_dt >= '2024-03-01'
AND order_dt < '2024-04-01'

-- 같은 결과지만 느린 형태 (칼럼을 가공하면, 인덱스를 사용하지 못함)
WHERE DATE_FORMAT(order_dt,'%Y-%m') = '2024-03'
 * 모든 행의 날짜를 하나씩 변환하여 비교하기 때문에 느림


 (5) IN 포함 여부 체크

<형태>
WHERE column_name IN (value1, value2, …)

** 같은 결과 나오는 식
WHERE column_name= value1
    OR column_name=value2
    OR column_name= ;

    

 (6) NOT IN을 쓸 때 유의사항
 NULL이 섞인 컬럼에 쓰면 의도치 않은 결과가 나타납니다
 (NULL 부분이 잡히지 않음. 값이 있는 부분에서만 필터링 함)
     

 (7) BETWEEN - 범위 
 비교연산자와 AND 대신, 값의 범위를 줄 때 효과적

 < 권장 형태 >  BETWEEN (양끝포함)
WHERE column_name BETWEEN value1 AND value2

-- 같은 결과지만 다른 형태
WHERE column_name>= value1
    AND column_name<= value2

** 날짜에 쓸 때는 조심해야함.(예. '2024-03-31' BETWEEN은 그날 00:00만 포함)

 
 (8) NULL 취급주의
 '모른다'이므로 =로 비교되지 않음

<형태>
grade IS NULL
빈 값인 행을 찾습니다
반대는 IS NOT NULL

< 틀린 방법 >
"grade = NULL"
항상 0건. 참도 거짓도 아닌 '알수없음'이 되기 때문

<주의>
"NULL + 1 = NULL"
연산에 NULL이 섞이면 결과도 NULL이 됨

 
 (9) IFNULL, COALESCE - NULL 대체
 보고서에 NULL을 그대로 내보내지 않음

 IFNULL(grade, '미지정') : grade값이 나오거나, NULL이면 '미지정' 반환 (a가 NULL이면 b 반환)
 COALESCE(grade, city, '알수없음'...) : grade값이 나오거나, NULL이면 city값, city값도 NULL이면 '알수없음' 반환 
 (a가 NULL이면 b, b가 NULL이면 c, c가 NULL이면 d......이렇게 반환) 


2. 정렬과 가공

 (1) ORDERBY – NULL 처리
MariaDB는 NULL을 가장 작은값으로 봄

<형태>
SELECT *
FROM tb_customer
ORDER BY grade;


 (2) AS 이름 붙이기
 계산 뒤에 'AS 이름명'을 붙여쓰기


 (3) CASE문 - 조건에 따라 다른 값
 엑셀의 IF 중첩을 한 문장으로 정리

<형태>
SELECT column1, column2,
    CASE
        WHEN condition1 THEN result1
        WHEN condition2 THEN result2
        WHEN conditionNTHEN resultN
        ELSE result
    END AS 끝나는 이름(문구)
FROM table_name;

 (4) Pivot – CASE 응용
기존 테이블에서 월별 주문건수를 피벗
조건에 맞을때만1, 아니면0
열 개수를 미리 알아야함

3. 집계

 (1) 집계함수– COUNT
 행의 갯수를 구함

<형태>
SELECT COUNT(column1) AS alias1
FROM table_name
WHERE condition;

 (2) SUM, AVG, MIN, MAX

 (3) 평균의 분모
  집계함수는 NULL을 계산에서 빼버림

<형태>
SELECT AVG(stock_qty) AS a,           # NULL을 뺸 평균
    SUM(stock_qty) / COUNT(*) AS b    # Null을 0으로 본 평균
FROM product;

 (4) GROUP BY - 묶어서 세기
 GROUP BY에 사용한 컬럼은 SELECT에 사용
 "~별"이라는 질의가 떠오른다면 GROUP BY를 사용

 (5) WHERE와 HAVING

< 조건문 작성 순서 >
 ① SELECT country, COUNT(*) AS cnt
   FROM customer
 ② WHERE signup_dt>= '2023-03-01' 행을거른다(집계함수사용불가)
 ③ GROUP BY country 묶는다
 ④ HAVING COUNT(*) >= 2 그룹을거른다(집계함수사용가능)
 ⑤ ORDER BY cnt DESC

* WHERE로 행을 먼저 줄인다

< 조건문에 들어갈 내용 확인 순서 >
① FROM 어느 테이블에서 가져올지 정한다
② WHERE 데이터를 먼저 필터링, 집계 전에 실행되어 집계함수를 사용하지 못함
③ GROUP BY 남은행을 묶는다
④ HAVING 묶인 그룹에서 필터링하며, 집계함수를 사용가능
⑤ SELECT 컬럼을 선택하고 AS로 별칭을 부여
⑥ ORDER BY 정렬한다. 별칭을 사용할수있다.
⑦ LIMIT 잘라낸다 — 가장 나중

4. 서브 쿼리 - 쿼리 안의 쿼리
한번에 답이 안 나올때 두 단계로 나눕니다
괄호 안에 먼저 계산됨

SELECT column_name(s)
FROM table_name
WHERE column_name > (SELECT COUNT(column_name) FROM table_name);


SELECT column_name(s)
FROM table_name
WHERE column_nameIN
(SELECT column_nameFROM table_nameWHERE condition)

 (1) SELECT 절 서브쿼리
 SELECT로 구성된 쿼리전체를 넣어 각 행에 넣기

 (2) FROM 절 서브쿼리

 (3) EXISTS – 존재여부 확인


5. 조인 기초
 중복을 없애려고 나눈 표를 다시 합치는 것 

 (1) INNER JOIN -양쪽에 다 있는것만 합침

 <형태>
SELECT column_name(s),
FROM table_1
INNER JOIN table_2
ON table_1.column_name= table_2.column_name;


 (2) LEFT JOIN– 기준 테이블은 모두
 짝이없으면 오른쪽 컬럼이 NULL로 채워집니다

 <형태>
SELECT column_name(s),
FROM table_1
LEFT JOIN table_2
 ON table_1.column_name = table_2.column_name;

** 언제 쓰는가 :  "주문이 없는 고객도 0으로 보여줘" 같은 요구
** <RIGHT JOIN> 방향만 반대. 실무에서는 LEFT로 통일해 씁니다
** <UNION> 테이블 모든데이터를 JOIN 합니다

6. 다중 조인과 VIEW

 (1) ERD – 테이블 관계를 도식화
테이블 간의 관계와 조인할때 사용할키(PK, FK)를 그림으로 확인합니다 

* PK(Primary Key) : 기본키. 테이블에서 각 행을 유일하게 식별하는 열의 이름
* FK(Foreign Key) : 외래키. 다른 테이블의 PK를 참조하는(가져온) 열의 이름
* FK는 가까운(연관이 높은) 테이블에서 PK를 가져온다
* 조인(JOIN)키는 항상 PK-FK 쌍입니다.

 (2) 다중 조인 - 여러 개를 한번에

<형태>
SELECT column_name(s),
FROM table t
JOIN table_1tbl1ON t.column1= tbl1.column1
JOIN table_2 tbl2 ON t.column2= tbl2.column2
JOIN table_3 tbl3 ON tbl1.column3 = tbl3.column3
WHERE condition;

 (3) FOREIGN KEY – 외래키
 존재하지않는 고객의 주문이 생기지않게 막습니다
 FK는 데이터가 추가, 삭제될 때 무결성검사를 대신해주는 기능

<형태>

-- 만들때
FOREIGN KEY (customer_id)
REFERENCES customer(customer_id)

-- 나중에추가
ALTER TABLE orders ADD CONSTRAINT fk_o_c
FOREIGN KEY (customer_id)
REFERENCES customer(customer_id);

--삭제
ALTER TABLE table_nameDROP FOREIGN KEY key_name;

* 부모컬럼 : PK나 UNIQUE
* 지켜주는 것 : 없는값 입력 차단, 참조중인 행삭제 차단
* 실무현실 : 적재성능 때문에 FK를 안 거는곳도 있음

 (4) VIEW – 복잡한 쿼리에 이름 붙이기
 항상 최신의 데이터를 조회 But, 빨라지지는 않는다
 조회할 때마다 원본 쿼리가 실행 → 복잡한 쿼리는 VIEW를 활용
 VIEW를 통한 수정은 제한적
 속도가 필요하면 물리테이블을 생성

<형태>
CREATE OR REPLACE VIEW v_order_detail AS          # v_order_detail 이름의 VIEW 생성
SELECT o.order_id, o.order_dt, c.customer_name,
    p.product_name, i.qty,
    i.qty * i.unit_price AS amount
FROM tb_orders o
    JOIN tb_customer c ON o.customer_id= c.customer_id
    JOIN tb_order_itemi ON o.order_id = i.order_id
    JOIN tb_product p ON i.product_id = p.product_id;
