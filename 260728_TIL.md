# 260728 학습내용

1. zip(*iterables)

```py

last_names = ["Jack","Matt","Obama"]
middle_names = ["D","S","O"]
first_names = ["Cock", "Bal", "Care"]

이렇게 주어졌을 때,
list 형변환은 가능하나, dict 형변환은 불가
<예시>
list ( zip ( last_names, middle_names, first_names) )
=> [('Jack', 'D', 'Cock'), ('Matt', 'S', 'Bal'), ('Obama', 'O', 'Care')]

dict( zip( last_names, middle_names, first_names) )
=> ValueError: dictionary update sequence element #0 has length 3; 2 is required
즉, 딕셔너리는 key, value로 2개씩 묶여야 형변환 가능



(1-1). zip은 기본적으로 튜플로 묶이는데, 이걸 풀고 다시 리스트형으로 변환할 수 있음

<예시>
full_name = zip ( last_names, middle_names, first_names )

- (1)
result = [ ]
for name in full_name:  
    result.append(' '.join(name))

    # 'separator'.join(iterable) 형식
    # "( 'last_names', 'middle_names', 'first_names' )" 이렇게 튜플로 묶이던게,  "last_names middle_names first_names" 으로 묶임
    # last_names middle_names first_names 형태로 새로운 result 리스트에 넣음

print(result)

- (2)
result = [' '.join(name) for name in full_name]
full_name = result
print(full_name)

위와 같은 결과가 나옴. (1) = (2)

그리고 list(enumerate(full_name))을 하면, [(0, 'Jack D Cock'), (1, 'Matt S Bal'), (2, 'Obama O Care')] 도출. "(위치값=인덱스. 값)" 형태

(1-2). zip은 기본적으로 튜플로 묶이는데, 이걸 풀고 딕셔너리로 변환할 수 있음

<예시>
last_names = ["Jack","Matt","Obama"]
middle_names = ["D","S","O"]
first_names = ["Cock", "Bal", "Care"]
emails = ['jackcock@gmail.com','BalMatt@gmail.com','ObamaCare@gmail.com']
address = ['dongsack-dong','imnae-dong','jangsajin-dong']

# full_name만드는거 먼저
full_name = zip ( last_names, middle_names, first_names )
names = [ ]                                                                  <= 새로운 리스트 생성(이름 통합본)
for name in full_name:
    names.append( ' '.join(name) )
print(names)                                                                 <= names = ['Jack D Cock', 'Matt S Bal', 'Obama O Care']

full_info = zip(names, emails, address)                               <= 다시 새로운 조합으로 튜플 생성(각 리스트에서 같은 인덱스에 있는 값 합침)

# ('Jack D Cock', 'jackcock@gmail.com', 'dongsack-dong'),  (~~~) 형태

# 사람을 한명씩 꺼내서
# name, email, address 라는 key로 애들이랑 묶어준다. -> zip
# dict로 형변환

results = [ ]
for i in full_info:
# 이때, 진행되는 것
# 첫 번째 반복 (i): ('Jack D Cock', 'jackcock@gmail.com', 'dongsack-dong')
# 두 번째 반복 (i): ('Matt S Bal', 'BalMatt@gmail.com', 'imnae-dong')
# 세 번째 반복 (i): ('Obama O Care', 'ObamaCare@gmail.com', 'jangsajin-dong')

    keys = ['names', 'email', 'address']                                    <= 딕셔너리에서 가져가는 key값 설정
    temp = dict(zip(keys, i))                                                 <= 한개의 튜플안에서 각 key값에 맞는 value값을 대응하여 반환 
        
# 첫 번째 반복을 기준으로 이때 keys와 i는 각각 다음과 같다.
# keys = ['names', 'email', 'address']
# i = ('Jack D Cock', 'jackcock@gmail.com', 'dongsack-dong')
# 여기서 zip(keys, i)를 실행하면,
# ('names', 'Jack D Cock'), ('email', 'jackcock@gmail.com'), ('address', 'dongsack-dong')    
# 여기에 dict를 실행하면, 앞은 key값으로 뒤는 value값으로 정해 정리함
# {'names': 'Jack D Cock', 'email': 'jackcock@gmail.com', 'address': 'dongsack-dong'}     
                             
    results.append(temp)
# 이걸 순서대로 넣으면, 리스트 안에 첫번째, 두번째, 세번째 딕셔너리가 들어감
print(results)

=>
[
{'names': 'Jack D Cock', 'email': 'jackcock@gmail.com', 'address': 'dongsack-dong'}, 
{'names': 'Matt S Bal', 'email': 'BalMatt@gmail.com', 'address': 'imnae-dong'}, 
{'names': 'Obama O Care', 'email': 'ObamaCare@gmail.com', 'address': 'jangsajin-dong'}
]


```
2. 리스트 comprehension
```py
[expression for 변수 in iterable]
list(expression for 변수 in iterable)

<예시>
cubic_list = [ num**3 for num in numbers ]
cubic_list = []
for num in numbers:
    cubic_list.append( num**3 )

cubic_list

> list comprehension 조건문
[expression for 변수 in iterable if 조건식]

<예시>
(1)
pitas = []

for x in range(1, 50):
    for y in range(x+1, 50):      # range범위를 어떻게 지정하느냐에 따라, 반복횟수를 줄여 성능을 향상시킬 수 있음
        for z in range(y+1, 50):
            if x**2 + y**2 == z**2:
                pitas.append( (x, y, z) )

pitas

(2)
pitas = [ 
    (x, y, z) 
    for x in range(1, 50) 
    for y in range(x+1, 50) 
    for z in range(y+1, 50)
    if x**2 + y**2 == z**2
    ]

pitas

```
3. 딕셔너리 copmprehension
```py
{키: 값 for 요소 in iterable}
dict({키: 값 for 요소 in iterable})

> 딕셔너리 copmprehension 조건문
{키: 값 for 요소 in iterable if 조건식}

<예시>
(1)
result = {}

for key, value in dusts.items():
    if value > 80:
        result[key] = value

(2)
result = {key: value for key, value in dusts.items() if value > 80}
result


** 참, 거짓 조건에 각 값이 들어간다면, 조건문을 쓰는 제일 뒤에 붙여쓰는 것이 아니라, 값을 쓰는 앞쪽에 넣어야함

<예시>
(1)
result = {key: '나쁨' for key, value in dusts.items() if value > 80 else '보통'} => 오류
# [값 for 변수 in 반복가능객체 if 조건]
# 조건을 만족하는 데이터만 걸러서(Filtering) 추출
# 조건을 만족하지 않으면 아예 제외하므로 else를 붙일 수 없음
result

(2) 
result = {key: '나쁨' if value > 80 else '보통' for key, value in dusts.items()}

# 조건표현식이 하나의 '값'이므로 제일 앞쪽에 옴.
# 만약 뒤로 간다면, if조건문이 하나밖에 오지 못하는 조건표현식 구성과 맞지 않음

# [참일때값 if 조건 else 거짓일때값 for 변수 in 반복가능객체]
# 데이터의 개수는 그대로 유지하되, 조건에 따라 표현할 값(Value)을 변경
# 삼항 연산자 전체가 하나의 '값'으로 취급되므로 반드시 else가 함께 있어야 함

result

```
4. Built-in Function
```py
iterable(순회가능한) 구조에 적용 가능
(list, dict, set, str, bytes, tuple. range)

- (4-1) map(function, iterable)
순회가능한 구조의 모든 요소에 함수를 적용한 후 그 결과를 돌려줌
return값은 map_object 형태

<예시>
(1)
numbers = [1, 2, 3]
str_nums = []
for num in numbers:
    str_nums.append(str(num)) # '1', '2', '3'으로 들어감
''.join(str_nums) # 위 3개를 합쳐 '123'을 만듦

(2)
# map() 활용
map(str, numbers)
list(map(str, numbers))
=> ['1', '2', '3']


- (4-2) filter(function, iterable)
순회가능한 구조의 모든 요소에 함수를 적용한 후 그 결과 True인 값들만 돌려줌
return값은 filter object 형태

<예시>

def odd(n):
    return n % 2 # 1이 나오면 True(홀수), 0이 나오면 False(짝수)
numbers = [1, 2, 3]  # 1, 3은 1이 남으므로 True, 2는 0이므로 Flase
new_numbers = list(filter(odd, numbers)) # True가 나오는 값만 반환
print(new_numbers)
=> [1, 3]

```
5. 함수
특정한 기능을 하는 코드의 묶음
함수의 장점
 - 가독성
 - 재사용성
 - 유지보수 (하나를 바꾸면, 이후에 쓰여있는 부분 전부 바뀜 => 오류 수정이 쉬움)

함수의 형태
```py
def <함수이름>(parameter1, parameter2):
    <코드 블럭>
    return value
```
- 이때, return은 반드시 하나의 객체를 반환
- return a, b, b와 같이 쓰여있다면, 튜플형태로 값 반환 => (a, b, c)
- return 값이 없으면(return을 쓰지 않으면) None을 반환
** print는 그냥 화면에 출력하는 것이고(값이 나오는게 아님), 함수를 return으로 마무리 하지 않으면 '함수이름(변수)'를 출력시 'None' 반환

* dir(__builtins__)을 쓰면, 내장함수 확인 가능

함수는 정의된 순차적으로 계산하기 때문에, 함수끼리 연결될 수 있다.

<예시>
```py
num1 = 0  # 별도 지정이 없으면 num1 = a
num2 = 1  # 별도 지정이 없으면 num2 = b

def func1(a, b):
    return a + b
    
def func2(a, b):
    return a - b
    
def func3(a, b):
    return func1(a, 5) + func2(5, b)  # (0+5) + (5-1)
    
result = func3(num1, num2)
print(result)
=> 9

```
6. 함수의 output

 - return
     오직 한개의 객체만 반환
     함수가 return 혹은 종료되면, 함수를 호출한 곳으로 돌아감

7. 함수의 입력(Input)

 (1) 매개변수(parameter)

```py
def func(x):       # 여기서 x가 매개변수. 정의부분에서의 변수
      return x + 2


 (2) 전달인자(argument)
     실제로 입력되는 값   # 호출부분에서의 변수

  * 인자의 특징

  (2-1). 위치에 따라 함수 내에 전달

  <예> 
  def cylinder(r, h): => cylinder(5, 2) 하면, r=5, h=2

  (2-2). 기본 인자값을 설정하여, 호출할 때 더 적은 인자로 호출 가능

  <예> 
  def greeting(name='익명'):        # name에 다른 인수를 지정하지 않으면, '익명'을 출력하도록 지시
         print(f'{name}, 안녕?')   # 즉, name의 기본값이 '익명'인 것
         return

  greeting() => '익명, 안녕?' 반환
  
  (2-3). 기본 인자값(a)이 있더라도, 호출할 때 다른 값(b)을 넣으면 해당 값(b)으로 호출 가능
  
  (2-4). 기본 인자값을 설정할 떄, 기본 인자값을 가지는 인자 다음에 기본 값이 없는 인자를 사용할 수는 없음
  
  <예>
  def greeting(name='john', age):     # greeting(1)을 하면, 이걸 name에 넣을지 age에 넣을지 프로그램이 정하지 못함
    return f'{name}은 {age}살입니다.'
  => 오류

  def greeting(age, name='john'):      # 기본값을 가지는 인자가 반드시 뒤쪽에 있어야함
    return f'{name}은 {age}살입니다.'
  => 실행됨

 (3) 키워드 인자
 정의된 함수를 호출 할때, 키워드 인자를 사용해 호출하면 순서를 바꿔도 함수 실행 가능

  <예>
  def greeting(age, name):
    return f'{name}은 {age}살입니다.'

  greeting(name='철수', age=24)   # 키워드 인자 지정
  => '철수은 24살입니다.'
   
```

