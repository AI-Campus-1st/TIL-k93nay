# 260917 학습내용

## 딥러닝 4회차

### 1. 딥러닝 모델

 (1) 딥러닝 개론

 - 퍼셉트론 : 사람의 뉴런을 모방한 인공뉴런
 - 다수의 입력 신호가 합산되어 하나의 통로로 출력되는데, 합산된 값이 일정 값 이상이면 다음 뉴런으로 신호 전달, 그 이하면 아무것도 하지 않음
  * w1x1+w2x2+......+b(가중합) -> 활성화함수(선형)로 0 또는 1로 출력

 (2) 퍼셉트론

 - 퍼셉트론은 선형 분류모형이기 때문에, AND, OR, NAND 등의 선형 문제만 해결 가능
 - 하지만 비선형 문제(XOR 등)는 풀 수 없음

 (3) 다층 퍼셉트론

 - 퍼셉트론 여러개를 합쳐 비선형 문제를 해결하는 것. 모든 비선형문제를 해결하는 것은 아님
 - 다층 퍼셉트론 = Neural Networks(CNN, RNN 등) = 딥러닝

 (4) 딥러닝 = 비선형 경계선

 - 원래 비선형인 문제를 A_feature로 옮김으로서 문제를 선형문제로 바꾼다음, 선형 경계선을 그려서 문제를 풀겠다는 것이 바로 딥러닝의 비선형 문제 해결 방식

 (5) 가중치 개수

 - 입력층, 은닉층, 출력층 사이의 화살표(연결관계) 수가 가중치 개수
 - 딥러닝의 가설함수는 단일 퍼셉트론의 가설함수가 중첩된 형태
 - 뒤에서부터 한 층씩 보는게 수월

 (6) 딥러닝(다층 퍼셉트론) 한계

 - 은닉층이 2개 이상일 경우, 심층 신경망이라고 하는데 모든 노드들이 서로 연결되어있는 구조에 깊이까지 있는 형태를 띔
 - 전체식을 미분하기에는 계산량이 많아 시간이 오래걸림

 (7) 오차 역전파 (Back-propagation)

 - 오차역전파는 순전파(입력->출력)으로 나온 오차를 경사하강법을 통해 줄이는 방향으로 바로 앞 가중치를 수정해나가는 과정(출력->입력 / 입력층에서 적용될 가중치를 업데이트 하는 것)
 - 가중치를 수정할 때 순전파에서 계산한 y=f(x)의 편미분값을 오차에 곱해 상단 노드에 전달
   * 편미분 사용 이유 : 모든 노드의 가중치값을 전부 고려할 필요없이 연결된 가중치들만 고려하면 되기 때문. 또한 체인룰 기법을 사용하면 출력층과 입력층 사이에 은닉층이 많아도 간단한 미분으로 기울기를 계산할 수 있다

 - 체인룰은 주로 합성함수의 미분에서 등장하는 개념으로 합성 함수란 $f(g(x))$ 와 같이 함수 안에 또 다른 함수 $g(x)$ 가 들어있는 형태를 말한다. $f(g(x))$ 안에 있는 함수 $g(x)$ 를 $x$라 보고 미분한 값과 $g(x)$ 를 미분한 값을 서로 곱해 주면 된다. 

 $$f(g(x))' = \frac{\partial f}{\partial x} = \frac{\partial f}{\partial g} \cdot \frac{\partial g}{\partial x}$$

 - 여기서 등장하는 개념이 바로 연쇄 법칙이라고 부르는 체인 룰(chain rule)이다. 이름에서 알 수 있듯이 분모와 분자를 연결 짓는 $\partial g$항이 체인의 고리 처럼 식을 이어주기 때문이다. 만일 합성 함수가 $f(g(h(x)))$ 와 같다면 미분은 다음처럼 표현한다.

 $$f(g(h(x)))' = \frac{\partial f}{\partial x} = \frac{\partial f}{\partial g} \cdot \frac{\partial g}{\partial h} \cdot \frac{\partial h}{\partial x}$$

 - 다층퍼셉트론에서의 오차역전파는 출력층에서 입력층으로 가중치가 더이상 업데이트되지 않을때까지 반복 계산

 (8) 체인룰(Chain-Rule)

 - 역전파 계산을 위해서는 오차를 파악한 뒤, 업데이트하고자 하는 가중치 편미분으로 나눠야한다.
 - 하지만, 오차는 해당 가중치와 직접 연결되어 있지 않기 때문에, 오차와 y(출력값) 관계 / 가중합~활성화함수~출력값 관계/ x(입력층)값과 가중합 관계 이렇게 3가지 다리를 거쳐서 확인해야한다.
 - 즉, 오차를 합성함수로 미분하기 위해서는 체인룰을 적용해야한다.

    #### 1. 연쇄 법칙 다이어그램 흐름
    $$W_{13}^{(2)} \leftarrow \left[\text{가중합}_3 \leftarrow \text{활성화 함수}\right]_{n_3} \leftarrow y \leftarrow E$$
    * **① 구간:** $E$와 $y$의 관계
    * **② 구간:** $y$와 $\text{가중합}_3$의 관계 ($n_3$ 노드 내부)
    * **③ 구간:** $\text{가중합}_3$과 $W_{13}^{(2)}$의 관계


    #### 2. 체인룰을 통한 오차 기울기(Gradient) 계산식

    $$\frac{\partial E}{\partial W_{13}^{(2)}} = \frac{\partial E}{\partial y} \cdot \frac{\partial y}{\partial \text{가중합}_3} \cdot \frac{\partial \text{가중합}_3}{\partial W_{13}^{(2)}}$$

    각 항의 미분 결과를 대입하면 다음과 같이 최종 계산됨.

    $$= (y - y_t) \cdot y(1 - y) \cdot a_{h1}$$

    이 값을 기존 가중치에서 빼주면, 새로운 가중치로 업데이트됨. 이 식을 일반화하면 다음과 같음.

    $$\begin{aligned}w_{ij}^{(2)} &= \text{기존 } w_{ij}^{(2)} - \alpha \cdot \frac{\partial E}{\partial w_{ij}^{(2)}} \\&= \text{기존 } w_{ij}^{(2)} - \alpha \cdot (y - y_t) \cdot y(1 - y) \cdot a_{hj}\end{aligned}$$

    여기까지가 [출력층~은닉층]까지의 계산임.

    [은닉층~입력층]까지 마찬가지로 계산 한 뒤, 아래와 같이 구할 수 있음

    $$w_{ij}^{(1)} \leftarrow \text{기존 } w_{ij}^{(1)} - \alpha \cdot (y - y_t)y(1 - y) w_{ki}^{(2)} \cdot a_{hi}(1 - a_{hi}) \cdot a_{hj}$$

 (9) 기울기 소실 문제(Vanishing Gradient Problem)

 - 오차함수의 기울기가 크면 가중치를 크게 수정하고, 작으면 작게 수정, 0이 되면 학습을 멈춤
 - 그런데, 층을 깊게 쌓았더니 입력층에 도달하기도 전에 오차 함수의 기울기가 0이 되어버리는 문제가 발생
 - 기울기 소실 문제 발생 원인
    * 역전파를 통해 가중치를 계산하는 과정을 보면, 위 식 중 y, (1-y), $a_{h1}$, $a_{hi}$, $(1 - a_{hi})$, $a_{hj}$ 등이 0~1사이의 값을 가짐. 이 부분이 층이 많아질수록 비례하여 많아지므로 입력층에 가까워질수록 기울기는 0에 수렴한다.

 - 어떻게 해결?
    * 가중치 초기값을 제대로 설정 ( Random 방식 ➔ Restricted Boltsman Machine 방식으로 )
    * 시그모이드 함수 대신 Relu함수 사용 ( x <= 0이면 y = 0, 양수일때는 x = y인 선형 그래프)

 (10) Restricted Boltsman Machine (RBM)

 - 가중치를 이용해 예측값 계산(1번째) : 기존과 같음
 - 거꾸고 복원해보기(Reconstruction, 2번째) : 방금 네가 만든 노드값을 가지고, 처음에 들어왔던 입력값이 뭐였을지 거꾸로 맞춰봐!" 하고 다시 역으로 계산(복원)
 - 가중치 수정(3번째) : 거꾸로 복원한 값이 원래 처음 입력값과 최대한 똑같아지도록 가중치(W) 수정

 (11) Relu

 - 절대값이 항상 0~1사이로 수렴하는 시그모이드 함수보다 WX>0일 때, 자신의 값을 그대로 유지하는 Relu가 기울기소실에 더 강함
 - 하지만 Wx<0일 때 전부 0에 수렴하는 죽은 뉴런 문제가 있어, 최근에는 Leaky Relu, Parametric Relu같은 모형 사용 

### 실습(구글 colab)

 (1) torch 설치

 (2) 데이터셋 불러오기(정의) : data set 이후 data load

 (3) 모델구조 잡기 : 기본 모델(완전연결계층) = Linear = Fully Connected Layer. 마지막에는 softmax로 0~1사이의 값으로 변환

 (4) 모델 세부 설정

 - 모델을 device로 이동 : 훈련 시 모델을 cpu, gpu로 이동시킵니다.
 - 옵티마이저(Optimizer) : 데이터와 손실 함수를 바탕으로 모델의 업데이트 방법을 결정합니다.
 - 스케줄러(Scheduler) : 학습률을 학습이 진행됨에 따라 적절한 값으로 변경하기 위해 사용합니다.

 (5) 학습 진행

 - 모델을 학습 상태로 변경 (가중치 업데이트가 가능한 상태로)
 - dataloader에서 뽑은 배치 사이즈만큼의 훈련 데이터를 device로 이동시킴
 - 훈련 데이터를 모델에 주입 (모델이 이미지와 레이블을 매핑하는 방법 학습, 오차역전파 진행)
 - 테스트 세트에 대한 모델의 예측을 만듦
 - 이 예측이 테스트의 레이블과 맞는지 확인
   * 훈련하는 동안 모델의 오차 측정(손실함수)
 - 학습 진행 : 에포크 등 설정 - 기울기 0으로 초기화 - 모델에 데이터 주입 - 에러계산 - 오차역전파 - 가중치 업데이트 - 로깅 순서
 - optimizer.step()은 배치마다 수행. scheduler.step()은 테스트 시 수행

### 2. 합성곱 신경망(CNN)

 (1) CNN 특징

 - 필터링(특성 학습) 후 분류. 즉, 원본데이터로부터 득성을 더 잘 나타내는 feature map을 만들어내 분류하는 것
 - 부각시키고자 하는 특성에는 큰 숫자(가중치 크게)를, 중요하지 않는 부분에는 작은 숫자(가중치 작게)를 곱하면 됨
 - 그렇게 만들어진게 feature map

 (2) Feature map

 * 3가지 요소
    1. "원본" (큰 격자판, \(7 * 7\)): 인공지능이 분석할 전체 이미지입니다. 숫자가 높을수록 밝거나 특정 색이 강한 부분입니다.
    2. "Filter" (파란색 작은 격자판, \(3 * 3\)): 이미지에서 눈, 코, 입, 또는 가로선, 세로선 같은 특정 특징을 찾아내는 '돋보기' 또는 '체(Sieve)'입니다.
    3. "Feature Map" (초록색 결과판, \(5 * 5\)): 필터로 필터링하고 남은 핵심 특징들만 모아놓은 요약본입니다.

 * 연산과정
    1. 필터 덧대기: 원본 이미지의 빨간색 점선 테두리(\(3 * 3\) 크기 영역) 위에 파란색 필터(\(3 * 3\))를 그대로 포개어 올립니다.
    2. 같은 자리끼리 곱하기 (Elementwise 곱): 겹쳐진 아홉 개의 칸에 적힌 숫자끼리 1:1로 각각 곱해줍니다.
    - 첫 번째 줄: \((1 * 1) + (1 * 0) + (0 * 1) = 1\)
    - 두 번째 줄: \((1 * 0) + (1 * 1) + (0 *  0) = 1\)
    - 세 번째 줄: \((1 * 1) + (1 * 0) + (0 * 1) = 1\)
    3. 싹 다 더하기: 곱해서 나온 결과들을 다 더하면 (\(1 + 1 + 1 + 1\)) 총 4가 됩니다.
    4. 지도에 기록하기: 이 4라는 숫자가 결과물인 Feature Map의 해당 위치(초록색 칸의 4)에 쏙 저장되는 것입니다.
    5. SCAN : 이 돋보기(Filter)를 오른쪽으로 한 칸 이동시켜서 똑같은 짓(곱하고 더하기)을 반복. 끝까지 가면 아래로 한 칸 내려와서 또 반복

 (3) channel

 - 원본데이터가 n장으로 이루어져 있다면, filter도 n장 (channel = n)
 - channel은 아무리 여러장이어도 filter의 개수는 1장(= n장이 합쳐진 1묶음)으로 간주. channel이 여러장인 filter(평면결과) 1개(1세트) = 피쳐맵(출력결과) 1개
 - 따라서, 서로다른 filter n개는 피쳐맵 n개를 만듦 ('channel=n' = 피쳐맵 n개)

 - 맥스 풀링 : 특성을 줄이는 과정. filter의 크기와 stride를 결정
 - 그럼, 그에맞게 ‘scan’을 하며 feature를 줄인다. pooling은 max pooling, avg pooling, min pooling 등이 있다.
   * 각 범위에서 가장 큰 값을 모아 만듦

 - 이 과정이 모두 끝나면, 단순한 완전 연결계층으로 분류과정을 수행

### 3. VGGNet

 - 의사결정나무 구조와 달리 구조가 단순하고 단일 모델로서는 더 나은 성능을 보이는 장점때문에 주목받고 있음

 (1) 배경

 - network의 깊이가 Neural Network의 성능에 어떻게 영향을 주는지 연구하기 위해 만들어진 모델
 - convolution layer의 filter의 사이즈를 동일하게 고정하고 convolution의 개수를 늘려나간다
   (원래 filter를 거치면 가로/세로가 -2가됨. 이때 바깥에 padding을 한칸씩 적용하는 것. 원본이 가로/세로가 각 +2가 되기때문에, convolution layersms 원본크기를 그대로 가져갈 수 있음)

 (2) 과정

 - convolution layer에서는 크기가 줄지 않고, pooling layer에서 피처맵이 절반으로 resize됨
 - feature learning(convolution layer -> pooling layer 반복) 후 완전연결계층인 분류 수행
 - 분류는 총 3개층으로 구성 (가중치 개수가 많음)

 (3) 특징

 - VGGNet의 연구는 Convolution layer를 늘려나가는 방식으로 연구가 진행
 - 일반적으로 Layer를 늘려나갈수록 성능은 더욱 좋아진다
 - 단, 19개 이상이되면 성능이 다시 낮아지는 경향을 보임

 (4) 차이

 - 5X5 필터를 이용한 Convolution layer 1층과 3X3 필터를 이용한 Convolution layer 2층은 서로 성능이 다름
 - 먼저 계산량이 다르기 때문. 가중치 개수가 1층은 25개, 2층은 18개임(계산량이 더 적어 성능이 더 좋음)
 - 또 다른 이유는 Non-linearity Relu 개수 때문. Relu 개수가 더 많은 후자가 학습이 더 잘된다고 볼 수 있음

### ResNet

 (1) 개념
 
 - Deep Residual(잔차) Learning for Image Recognition(He 외 3명) 논문을 바탕으로 함
 - ‘잔차'는 ‘오차'와 같은 말이다. Residual = 잔차 = 오차 = error
 - 일반적인 네트워크는 F(x)가 y에 가까워지는 것에 목표
 - ResNet은 H(x)(=F(x)+X)가 X에 가까워는 것에 목표를 두고 학습하는 것. 즉, H(x)-x=F(x)=0를 목표로 함

 (2) X = Y ?

 - y는 X가 누구인지 라벨링한 값. 즉 X에 대해 표현한 값이므로 근본적으로 같은 값이라고 할 수 있음
 - 따라서 H(x)-y가 잔차라면, H(x)-X도 잔차가 됨

 (3) Deep & Skip

 - ResNet은 VGGNet을 진보시킨 모델
 - 잔차 학습(Residual learning)은 단순한 stacked layer의 학습보다 가중치가 훨씬 적어 빠르게 계산됨
 - 일부 가중치들이 레이어를 스킵하고 뒤의 레이어로 바로 연결되는 것과 같음
 - 그래서 Skip connection이라고도 부름

 

