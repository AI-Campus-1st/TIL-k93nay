# 260730 학습내용

[###클래스###]

** 클래스 내 함수 정의 시, 함수에는 꼭 마지막에 'return'을 써야 값을 가질 수 있음.
** print만 쓰는 오류는 금지, 반드시 return쓰기. (값을 밖에 보이고 싶다면 "return print()" 형태로 작성)

<예시> - 260729 과제

```py
class Circle:
    def __init__(self, r, x, y):
        self.r = r
        self.x = x
        self.y = y

    def area(self):
        pi = 3.14
        return print(pi*self.r**2)  # print만 쓰면 함수 결과를 겉으로는 출력하지만, 값을 가지지는 못함. return 꼭 쓰기!!!

    def circumference(self):
        pi = 3.14
        return print(2*pi*self.r)

    def center(self):
        return self.x, self.y
```    

1. 매직 매서드

- 더블언더스코어(`__`)가 있는 메서드는 특별한 일을 하기 위해 만들어진 메서드이기 때문에 `스페셜 메서드` 혹은 `매직 메서드`라고 부름
- 매직(스페셜) 메서드 형태: `__someting__`

<예시>
```py
 '__str__(self)',         # 문자열로 반환
 '__len__(self)',         # 길이 반환
 '__repr__(self)',        # 주피터 output 관련
 '__lt__(self, other)',   # lt~ge는 길이와 관련
 '__le__(self, other)',
 '__eq__(self, other)',
 '__ne__(self, other)',
 '__gt__(self, other)',
 '__ge__(self, other)',


(예시). __str__(self)
- 특정 객체를 출력(`print()`) 할 때 보여줄 내용을 정의. 문자열로 형변환

class Person:
    def __str__(self):
        return '객체 출력(print)시 보여줄 내용'
=> object를 문자열로 형변환 후 반환

<예시2>
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age  # int, 숫자
        return

    # 비교연산자는 비교대상이 있어야함. 따라서 self와 비교할 수 있는 대상 'other' 써야함
    # self: Person 이렇게 쓰는 이유는, Person 클래스에 있는 객체라는 표현. 생략해도 됨
    def __gt__(self: Person, other: Person):     
        return self.age > other.age

    def __eq__(self, other):     
        return self.age == other.age

p1 = Person('1', 1)   
p2 = Person('2', 2)

print(p1 > p2)  => False  
# 1 > 2  
# name는 함수식에 없으므로 따로 출력되지 않고, age와 관련된 객체 비교 결과만 출력됨 (arg는 반드시 맞게 2가지가 들어가야함)
# 정의한 함수 중 요구하는 비교연산(위의 경우 > )이 있는 식에 적용해 True, False 확인

```
2. 인스턴스 & 클래스 변수

*** 객체(Object)와 비교
> 객체 (Object): 어떤 클래스로 만들었는지 상관없이, 메모리에 존재하는 모든 데이터 독립체를 부르는 말
> 인스턴스 (Instance): 그 객체가 어떤 클래스로부터 태어났는지(관계)를 명확히 밝혀서 부르는 말

*** 객체 설정 및 해당 클래스의 인스턴스가 제대로 설정되어야 함수 결과값 반환 가능
(예. p1 = Puppy('name', 'breed') <= 이름과 종이라는 인스턴스가 정의되었다면, 객체(p1)를 쓸때, 클래스명과 인스턴스 변수명을 써야  클래스에 정의된 함수가 적용)

```py
<예시>
class Car:
    pass

# my_car라는 물리적 공간이 메모리에 생성됩니다.
my_car = Car()    # my_car : 객체(독립적인 하나의 실체), Car 클래스의 인스턴스(Car라는 설계도로 만들어진 결과물이라는 '관계'를 강조)


(2-1). 인스턴스 변수
생성자 매서드에서 self.변수명으로 정의
각 인스턴스들의 고유한 변수

class Person:

    def __init__(self, name):    # 인스턴스 메서드 (생성자) 
        self.name = name         # 인스턴스 변수

(2-2). 클래스 변수
모든 인스턴스가 공유
클래스 내부에서 정의
클래스.변수명 으로 접근

class Circle:
    pi = 3.14

print(Circle.pi)   => 3.14 출력
```
아래와 같은 형태 어떤 것이든 같은 결과 도출(클래스변수 공유)
 > print(클래스명.클래스변수)
 > 변수명 = 클래스명()
 > 클래스명.클래스변수
 > print(변수명.클래스변수)

3. 이름공간
```py
클래스를 정의하면, 생성과 동시에 이름 공간 생성(namespace)
인스턴스를 만들면, 인스턴스 객체가 생성되고 해당되는 이름 공간이 생성
인스턴스의 속성이 바뀌면, 변경된 데이터를 '인스턴스 객체 이름공간'에 저장
따라서, 인스턴스에서 특성 속성에 접근하면 '인스턴스 > 클래스'(가까운 순)로 속성 탐색
(인스턴스에서 해당 이름공간이 없으면, 클래스 속성 적용)

<예시>
class Person:
    name = 'unknown'

p1 = Person()
p1.name   # 'unknown' 반환

p1.name = 'Jack'   # 인스턴스 p1의 name 이름공간 변경 ('unknown' => 'Jack')
print(p1.name) #=> 인스턴스의 name     # Jack 출력
print(Person.name) #=> 클래스의 name   # unknown 출력

```
4. 매서드 종류
인스턴스 매서드 : 첫번째 인자로 인스턴스(self) 전달
클래스 매서드 : 첫번째 인자로 클래스(cls) 전달 (`@classmethod` 데코레이터를 사용하여 정의)
스태틱 매서드 : 아무것도 자동으로 전달하지 않음 (`@staticmethod` 데코레이터를 사용하여 정의)


### 인스턴스와 메서드
- 인스턴스는 3가지 메서드 모두에 접근할 수 있습니다.
- 하지만, 인스턴스가 할 행동은 모두 인스턴스 메서드로 한정 지어서 설계합니다.
- 인스턴스에서 클래스 메서드와 스태틱 메서드는 되도록 호출하지 않아야 합니다. (가능하다 != 사용한다)
```py
<예시>
class MyClass:
    def instance_method(self):
        return self
    
    @classmethod
    def class_method(cls):
        return cls
    
    @staticmethod
    def static_method(arg):
        return arg

my_instance = MyClass()

# [인스턴스 매서드] 인스턴스 생성 후 메서드를 호출하면 자동으로 첫 번째 인자로 인스턴스(my_instance)가 들어갑니다.
my_instance.instance_method(.., ..)  

# [클래스 매서드] 자동으로 첫 번째 인자로 클래스(MyClass)가 들어갑니다.
MyClass.class_method(.., ..) 

# [스태틱 매서드] 아무런 일도 자동으로 일어나지 않습니다.
MyClass.static_method(.., ..)
```

### 클래스와 메서드
- 클래스가 할 행동은 다음 원칙에 따라 설계합니다. (클래스 메서드와 정적 메서드)
    - 클래스 자체(`cls`)와 그 속성에 접근할 필요가 있다면 **클래스 메서드**로 정의
    - 클래스와 클래스 속성에 접근할 필요가 없다면 **정적(스태틱) 메서드**로 정의
        - 정적 메서드는 `cls`, `self`와 같이 묵시적인 첫번째 인자를 받지 않기 때문.(그냥 정리를 위해 클래스에 들어가있는 것뿐)

<예시>
```py
class Puppy:
    population = 0
    
    def __init__(self, name, breed):
        self.name = name
        self.breed = breed
        Puppy.population += 1    # 클래스 변수 변경 시에는 클래스명(Puppy)을 써서 작성
        
    def __del__(self):
        Puppy.population -= 1
    
    def bark(self):
        print(f'왈왈! 나는{self.name}, {self.breed}(이)야')
    
    @classmethod   # class 변수인 population에 접근해야하기 때문에, 클래스매서드 정의
    def get_population(cls):
        print(f'현재 강아지 마리수: {cls.population}')
        return cls.population   # 클래스에 현재 강아지 수 값 반환

p1 = Puppy('코코', '푸들')
Puppy.get_population()  => 1 도출

*** 만약 "p1 = Puppy('코코', '푸들')"(특정 객체에 할당하여 인스턴스 생성)가 아니라, "Puppy('코코', '푸들')"로만 호출한다면,
    __init__와 __del__이 동시에 진행되므로, population이 올라가지 않음

*** 만약 "p1 = Puppy('초코', '말티즈')"로 써서 적용한다면, p1객체에 변수를 업데이트하고 이전값은 지우는 것이므로, population이 올라가지 않음 (객체명이 같으면 덮어쓰기)

```
5. 상속(Inheritance)
즉, 다른 클래스에서 먼저 정의한 클래스를 사용할 수 있다는 것(코드 재사용성이 높아짐)
```py
class ChildClass(ParentClass):
    <code block>
```
*** 부모클래스 설정 후 자식클래스 설정 시, 범위로 치면 부모 클래스 안에 자식 클래스가 있는 것
*** 따라서 같은 인스턴스 변수명이 있어도, 자식 클래스(더 가까운 클래스)의 함수로 정의 및 실행됨

<예시>
```py
class Person:
    population = 0
    
    def __init__(self, name='사람'):
        self.name = name
        Person.population += 1

class Student(Person):
    def __init__(self, student_id, name='학생'):
        self.name = name
        self.student_id = student_id  
        Person.population += 1

s1 = Student(2345, )
s1.name  => '학생' 출력


(5-1). issubclass(자식클래스, 부모클래스)
맞으면 True, 아니면 False
내장 자료형들도 아래와 같이 상속 관계가 있음

<예시>
print(issubclass(bool, int)) # True
print(issubclass(float, int)) # False

(5-2). super()
자식 클래스에 매서드 추가 가능
부모 클래스의 내용을 사용하고자 할때, super()사용
* 이것은 부모 클래스에서 진행한 내용 자식 클래스에서 중복 실행되지 않도록 제거(중복제거)하는 기능
+ 부모 클래스에서 최초 설정한 값을 그대로 가져오고 싶을 때에도 사용할 수 있음(대신 자식클래스의 중복식, 기본설정값은 지워야함)

<예시>
class Person:
    population = 0
    
    def __init__(self, name='사람'):
        self.name = name
        Person.population += 1
        return

class Student(Person):
    def __init__(self, student_id, name):
        super().__init__()
        self.student_id = student_id 
        return

s1 = Student(2345, )
s1.name  => '사람' 출력
Person.population => 1 출력

 (5-3). 매서드 오버라이딩(method overriding)
 자식 클래스에서 부모 클래스의 매서드를 재정의하는 것
 같은 매서드 이름을 써서 덮어씌우는 것 -> 그러면 자식 클래스 사용시, 자식 클래스에서 정의된 함수 사용됨

 (5-4). 상속 관계에서의 이름 공간
 인스턴스 > 자식 클래스 > 부모 클래스 순으로 탐색

```
6. 다중 상속
```py
두개 이상의 클래스를 상속받을 때, 다중 상속이라고 함
상속받은 모든 클래스의 요소 활용 가능
중복된 속성, 매서드가 있는 경우, 상속 순서에 따라 결정

<예시>

class Person:

    def __init__(self, name):
        self.name = name
        return

class Mom(Person):
    gene = 'XX'

    # 생성자 매서드(__init__는 따로 쓰지 않음. 부모 클래스에서 그대로 가져오기 때문)
    # 다른 속성(변수)이 있을때만, 매서드 오버라이딩하면 됨 

    def swim(self):
        return print('수영하러 가자~!')

class Dad(Person):
    gene = 'XY'

    def walk(self):
        return print('그냥 걸어가자~!')

class FirstChild(Dad, Mom):   # 다중 상속을 쓸 때, 처음 쓴 class명부터 상위순으로 상속받음(같은 인스턴스가 있으면 dad 것을 받음)

    def swim(self):              # Mom 클래스의 swim매서드 오버라이드
        return print('어푸어푸')

    def cry(self):
        return print('뿌엥')

baby1 = FirstChild('샛별이')
baby1.gene  => 'XY'로 받음(Dad가 상속 우선순위 클래스이므로)


```
### 모듈 ###
파일 단위로 코드를 재사용하는 것


|용어|정의|
| --------| ------------------- |
|  모듈(파일)  | 특정 기능을 `.py` **파일 단위**로 작성한 것. |
|  패키지(폴더)  | 특정 기능과 관련된 여러 **모듈들의 집합**. 패키지 안에는 또다른 서브 패키지를 포함 할수도 있음.  |
| 파이썬 표준 라이브러리 | 파이썬에 **기본적으로 설치된 모듈과 내장 함수**를 묶어서 파이썬 표준 라이브러리 (Python Standard Library, PSL) 라 불림. |
| 패키지 관리자(**`pip`**) | `PyPI` 에 저장된 외부 패키지들을 설치하도록 도와주는 패키지. |

1. 모듈 활용
import 써서 불러옴
dit(모듈명)을 쓰면, 사용 가능한 함수 확인 가능

2. 패키지
```py
my_package/             # 패키지(폴더)
    __init__.py
    math/               # 하위 패키지(하위 폴더)
        __init__.py
        tools.py        # 파일
```
> 모듈 이름 `my_package.math`는 `my_package`라는 이름의 패키지에 있는 `math`라는 이름의 하위 패키지를 가리킵니다.

- **`__init__.py`**?
> python3.3 버전부터는 `__init__.py` 파일이 없어도 패키지로 인식합니다(PEP 420). 하지만 하위 버전 호환 및 일부 프레임워크에서의 올바른 동작을 위해 `__init__.py` 파일을 생성하는 것이 권장됩니다. (`__init__.py`은 비워둠)


* 특정 패키지의 모듈을 불러올 시 사용
> 'from 상위패키지(폴더).하위폴더 import 모듈명(파일명)' 형태

* 특정 모듈의 데이터를 불러올 시 사용
> 'from 상위패키지(폴더).하위폴더.모듈명(파일명) import 데이터(함수명, 변수명 등등)' 형태

* 특정 모듈 안 데이터(함수, 변수)를 모두 불러올 시 사용
> 'from 상위패키지(폴더).하위폴더.모듈명(파일명) import *' 형태

* 특정 모듈의 데이터를 불러온 뒤, 별명을 붙여 사용
> 'from 상위패키지(폴더).하위폴더.모듈명(파일명) import 데이터 as 별명' 형태