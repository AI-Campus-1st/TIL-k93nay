# 260916 학습내용

## 머신러닝 3회차

### 1. 랜덤포레스트

 (1) 배깅

  - 부트스트랩(bootstrap aggregating) : 원본 데이터 집단으로부터 원본과 유사한 새로운 데이터 집단 추출
  - 이런 집단들을 학습시켜 병렬로 합치는 것
    * scikit-learn에서는 원본 데이터 개수가 N개라면, 원본데이터로부터 중복을 허용하여 무작위로 N개의 데이터를 추출하여 샘플데이터 생성
  - Aggregating : Average of frequency. 서로 다른 데이터셋에서 나온 약한 학습기(의사결정나무) 결과의 예측값을 합치는 것
    * 회귀분석은 평균, 분류는 빈도수 계산

 (2) 랜덤포레스트

  - 부트스트랩으로 만들어진 여러 샘플 데이터를 가지고, 여러 번의 의사결정나무를 수행한 다음, 이를 Aggregation하는 것이 Random Forest
    * 진행방법
        1. 학습 데이터 N개로부터 중복을 허용하여 무작위로 샘플 데이터 N개를 추출한다.
        2. 샘플데이터에서 중복을 허용하지 않고, 무작위로 X값 (input feature) d개를 선택한다.
        3. d개의 feature에 대해 Decision Tree(의사결정나무)를 수행한다.
        4. 위의 1-3 과정을 k번 수행한다.

### 2. AdaBoost

 (1) 부스팅

  - 원본 데이터 집단으로부터 원본과 유사한 새로운 데이터 집단을 k번 추출하여 k번의 약학습기 학습을 진행(배깅과 같음)
  - 차이점은, 순차적으로 먼저 진행한 샘플링 학습결과를 바탕으로 오차가 발생한 데이터에 더 큰 가중치를 두고 다음 샘플링 진행. 즉 오차가 발생했던 데이터가 다음 번에 또 뽑힌다면 또 틀리지 않도록 학습하는 것

 (2) AdaBoost 

  - Adaptive Boosting의 줄인 말이며, 의사결정나무의 부스팅 모델 중 가장 표준이 되는 모델. 깊이(depth)=1(경계선 1개)
  - 부트스트랩(bootstrap aggregating) 진행
  - 지수 손실로 계산
    * 진행방법
        1. 학습 데이터 N개로부터 중복을 허용하여 무작위로 샘플 데이터 N개를 추출한다.
        2. 샘플 데이터에서 중복을 허용하지 않고, 무작위로 X값(input feature) d개를 선택한다.
        3. d개의 feature에 대해 Decision Tree를 수행한다.
        4. 오차가 발생한 데이터가 샘플링할 때 더욱 잘 추출되도록 가중치를 부여한다.
        5. 위의 1-3 과정을 k번 수행한다.

### 3. Gradient Boosting

 - Adaboost처럼 반복마다 데이터 셋의 가중치를 수정하는 대신, 이전 예측 모델에서 계산된 Cost(잔여 오차)를 경사하강법으로 계산하여 새로운 예측 모델을 학습
 - 하나의 트리를 학습할 때, 학습 데이터 중 일부 (대체적으로 0.5~0.7) 만을 무작위로 추출하여 학습한다. 이는 overfitting을 개선한 것
 - 정확도는 높지만, 시간이 오래걸림 (의사결정나무의 매 노드마다 최대의 정보이득(=최소의 엔트로피)을 비교연산하는 방식을 가져오므로)


### 4. XGBoost

 - XGBoost는 이 시간지연을 개선하기 위해, 트리를 구성할 때 병령처리를 이용하여 개선한 모델
 - 보통 트리를 구성할 때, 노드 단위 순회 루프 + feature단위 순회 루프 총 2번의 루프를 돌게 되는데, 
 - XGBoost는 노드단위로 순회하는 루프를 병렬처리(동시 처리)함

### 5. LightGBM

 - XGBoost를 더 효율적으로(메모리 효율) 개선한 모델
 - 그러나 데이터가 작은 데 쓰면 과적합 우려가 있음

### 6. 비계층적 클러스터링 (비지도학습, 군집형)

 (1) k-means

  - 사용자가 정한 K개의 클러스터로 클러스터링하는 알고리즘
  - 첫번째 작업은 '중심점의 초기값'을 설정하는 것. 초기값은 데이터 중에서 임의로 K개 선택
  - EM 알고리즘 : Expectation(중심점을 기준으로 군집을 나누는 것)과 Maximization(나눠진 군집을 기준으로 중심점을 재설정하는 것)
  - EM 알고리즘을 반복하여 군집을 구성
  - 실루엣계수로 성능 평가

 (2) K-Means++

  - K-means에서 조금 더 개선된 방법
    * 진행방법
        1. K개의 중심점 중 첫 번째 중심점을 임의로 선택한다.
        2. 중심점으로 선택되지 않은 다른 모든 데이터에 대해 거리를 계산한다.
        3. 그 거리에 비례하여 기존의 중심점으로부터 거리가 먼 데이터가 다음 중심점으로 선택될 확률을 크게 주어 K개의 중심점을 선택해나간다.

### 7. 계층 클러스터링 (비지도학습)

 - 병합 클러스터링(Agglomerative Algorithm)이라고도 부름
    * 진행방법
        1. 데이터 하나를 하나의 클러스터로 간주하여 시작한다.
        2. 각 데이터(클러스터)간의 모든 유사도(거리)를 계산한다.
        3. 유사도가 높은(거리가 가까운) 클러스터들을 통합해나가며 클러스터의 총 수를 줄여나간다.

 - 데이터들간의 유사도를 먼저 계산해야하기 때문에, 계산 복잡도가 높다
    * 유사도 계산 방법
        1. Centroid(중심연결법): 각 클러스터의 중심점 사이의 거리
        2. Single(단일연결법, 최소연결법): 각 클러스터 간의 데이터들의 조합 가운데 가장 가까운 거리
        3. Complete(완전연결법, 최대연결법): 각 클러스터간의 데이터들의 조합 가운데 가장 먼 거리
        4. Average(평균연결법): 각 클러스터간의 데이터들의 조합들의 평균

### 8. 변환 모델(Transformation Model)

 (1) PCA(주성분분석)

  - 비지도학습 변환 방식 : 입력 특성을 변환하는 **비지도 변환**
  - 차원 축소(PCA)로 최대한 정보를 유지하면서도(정보손실 작게 발생) 특성 수를 줄여 연산능력을 높임
  - 그 외에도 NMF(비음수행렬분해), t-SNE 등이 있음

  - 왜 필요한가? : **차원의 저주**가 있기 때문. (데이터의 벡터 공간 차원은 엄청나게 크지만 실제로 필요한 true data는 작은 차원 공간으로 표현해도 충분한 경우 차원의 저주 발생)
  - 만약 차원축소를 하지 않으면 과적합이 발생할 수 있음

  - 차원축소 진행 시, 데이터의 특성을 가장 잘 나타내는 주성분(분산을 가장 크게 갖는 벡터/ 데이터 정보를 가장 많이 갖고 있음)을 찾아야함. 
  - 이 주성분 중 일부만을 선택해 데이터의 차원을 축소시키는게 **차원축소**
  - 다음 주성분은 첫번째 주성분과 직교하는 쪽으로 찾아냄

  - 주성분 개수는 **데이터 수, 특성 수 중 작은 값**이 됨
