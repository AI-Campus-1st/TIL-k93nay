### 260825 학습내용

### 웹크롤링 6일차 - 죽지않는 크롤러 ###


1. 100건과 10000건

 - 실패확률이 낮아도 횟수가 늘어남
 - 시간이 오래걸림
 - 죽으면 통째로 날아감. 재수집 비용이 많이 듦

 (1) 에러 사례
 |에러명|설명|
 |:--:|:--:|
 |ConnectionError |상대 서버가 잠시 끊었거나 내 네트워크가 흔들렸음 |
 |Timeout |응답이 오지 않음. timeout이 없으면 여기서 영원히 멈춤|
 |429 Too Many Requests |너무 빨리 보냄. 속도를 줄이라는 신호|
 |AttributeError: NoneType |그 페이지에만 없는 항목. 한건 때문에 전체가 멈춤|
 |UnicodeDecodeError | 인코딩이 다른 페이지가 섞여 있음|

 (2) 중간에 실패해도 멈추지 않게하는 방법을 알아야함

 - 무엇이 실패했는지 나중에 확인 가능
 - 멈춘 지점부터 이어받을 수 있고, 다시 돌려도 중복이 생기지 않게할 수 있음


2. 아키텍쳐

 (1) 한 파일에 다 쓰면 목록과 상세가 붙어있는 문제가 생김

 - 수집 실패 시, 모두 날아감
 - 목록과 상세를 분리하여 수집해야 어디에서 오류가 났는지, 중단한 부분부터 확인하고 다시 돌릴 수 있음

 (2) URL큐

 - 페이지를 훑어 URL목록 수집 후, 파일 또는 DB(목록과 상세 수집결과 연결해주는 저장소)에 상태와 함께 저장

 * 상세수집은 큐를 하나씩 읽어 처리

 (3) URL큐 설계

 - 주소와 상태를 함께 담음
 - 상태 칼럼 하나로 재개, 재시도, 통계 등이 모두 해결됨 (예. status 칼럼 : pending, done, failed로 상태 바로 파악 가능)
 - pending만 골라 돌리면, 그게 '이어받기'

 (4) 큐를 어디에 둘 것인가

 | | 파일 (CSV · JSONL) | DB (SQLite · MySQL)|
 |:--:|:--:|:--:|
|적정규모 |수만 건까지 |수십만 건이상|
|중복검사 |매번 전체를 읽어야함 |UNIQUE제약으로 즉시|
|부분갱신 |파일전체 재작성 |한 행만 UPDATE|
|동시접근 |위험 |안전|
|시작비용 |없음 |설치와 설계필요|

3. 예외처리

 (1) 예외 5가지

|예외 |원인 |대응|
|:--:|:--:|:--:|
|Timeout |응답이오지않음 |재시도 — 일시적일 가능성이 높음|
|ConnectionError |연결 자체가 실패 |재시도 — 간격을 두고|
|HTTP 4xx |요청이잘못됨 |재시도 무의미— 기록하고 건너뜀|
|HTTP 5xx |서버문제 |재시도 — 백오프필수|
|파싱실패 |구조가다름 |기록하고 건너뜀— 나중에 원인분석|

 (2) try, except로 실패도 기록

 - continue쓰면 실패해도 진행
 - raise_for_status() 를 부르면 4xx·5xx도 예외로잡힘
 - 예외 객체를 남겨야 나중에 원인 분류 가능

 (3) try는 어디에 걸 것인가

 - 너무 넓은 범위를 잡으면 원인을 잃고, 너무 좁으면 코드가 복잡해짐
 - 반복문 안에 try를 두는 것이 원칙
 - except Exception: pass는 절대 쓰면 안됨(침묵하는 버그)
 - 요청과 파싱을 분리해 잡는것이 좋음

 (4) 실패 큐 분리 

 - 실패한 것만 모아 나중에 다시 돌릴 수 있음
 - 실패 유형을 남겨야 진행 가능
 - 전체를 다시 돌리는 것보다 훨씬 빠름
 - 실패율이 10%를 넘으면, 재설계를 생각해봐야함


4. 재시도

 (1) 재시도 판단

 - Timeout, ConnectionError, 429, 500, 502, 503은 다시 시도해도 좋음
 - 400, 401, 403, 404는 존재하지 않는 페이지거나 요청 자체가 오류이므로 다시 시도하는 것은 좋지 않음

 (2) 고정 간격

 - 차단이 원인이면 간격을 늘려가는 것이 재시도에 유리
 ** 고정간격 : 1초→ 1초→ 1초→ 1초 (재시도 과부하)
 ** 지수백오프 : 1초→ 2초→ 4초→ 8초 

 (3) 지수 백오프

 - 예외발생 시, 함수 자체를 종료시키게 만드는 게 좋음
 - 지수백오프는 2의 거듭제곱으로 늘리는 것이 표준
 - 난수가 더해진 지터로 재시도 하는 것이 과부하 방지에 유리
 - 마지막시도까지 실패하면 그때 예외를 올려보냄

 (4) urllib3.Retry 장착

 - retry시, 오류세션코드를 바로 보여줌 : status_forcelist에 적은 코드만 재시도 (404는 자동 제외)
 - backoff_factor가 1이면 1 → 2 → 4 → 8초로 늘어남
 - 세션에 한번 붙이면 이후 모든 요청에 적용됨


5. 관측과 재개

 (1) print 대신 logger 사용

 - logging.FileHandle 사용
 - 무슨일이 있었는지 자동으로 추적됨
 - 파일과 화면에 동시에 남길 수 있음
 - 레벨을 바꿔 상세도 조절 가능

 (2) 로그 레벨 설계

 | 레벨 | 남길 것 | 예시| 
 |:--:|:--:|:--:|
|DEBUG |평소엔 안보고싶은것 |요청URL, 응답크기|
|INFO |정상 진행상황 |페이지완료, 누적건수|
|WARNING |실패했지만 계속감 |한건파싱실패, 재시도|
|ERROR |멈춰야할 문제 |DB 연결실패, 인증오류|

 (3) 로그가 너무 커지면 파일을 잘라가며 남겨야함

 - RotatingFileHandler 사용
 - 오래된것부터 자동으로 지워짐
 - 실패 로그만 따로 파일을 나누면, 관리가 편함

 (4) 진행률과 통계

 - tqdm : 진행상황 확인이 가능한 라이브러리(남은 시간까지 추정해 보여줌)
 - 완료, 실패 건 수, 건너뜀 수 등 실행결과 확인 가능

 (5) 체크포인트 세가지 방법

 - 진행파일 : 마지막 페이지 번호를 파일에 적어놓음(순차 수집에만 통함)
 - 상태칼럼 : 큐의 각 행에 done/pending 표시. 순서와 무관하게 재개 가능
 - 수집완료 목록 : 이미 저장된 결과에서 키를 읽어 제외함. 결과가 커지면 느려짐

 (6) 상태칼럼으로 재개

 - pending만 골라 돌리는 '이어받기' 진행 가능
 - Ctrl + C로 껐다 다시 켜도 남은 것만 돔

 (7) 멱등성

 - 몇번을 돌려도 결과가 같아야하기 때문에, 고유키를 정하고 이미 있으면 건너뛰거나 덮어쓰도록 정리
 - DB의 UNIQUE 제약이 이 일을 대신해줌

 (8) 오늘 코드 총정리

 - 세션 - 헤더 + retry / 로거 - 파일 + 로테이션 / q설정(저장위치) / finally - 실패 성공과 무관하게 설정해놓아야 함
 - 분리(목록과 상세 분리) - 예외(기록) - 재시도(지수 백오프) - 로깅(파일+레벨) - 재개(상태칼럼 + 멱등성)