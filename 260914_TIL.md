# 260914 학습내용

## 머신러닝, 딥러닝

### 1. 머신러닝

 (1) 머신러닝에 필요한 역량

 * 프로그래밍 언어 능력
 * 데이터분석 능력(이론적 지식)
 * 수학 능력(선형대수학, 미적분, 확률 통계 등)\
 * 도메인 지식

 (2) 파이썬

 * 귀도반 로섬이 1991년 개발한 오픈소스 코드언어

 (3) 머신러닝 관련 오픈소스

 * Sklearn : 딥러닝을 제외한 범용적인 머신러닝을 위한 제너럴리스트
 * Pytorch : 딥러닝을 위한 스페셜리스트

### 2. 머신러닝의 개념

 (1) 컴퓨터가 명시적인 프로그래밍 없이도 스스로 학습할 수 있도록 하는 연구 분야
 (2) 어떤 컴퓨터 프로그램이 작업 T의 부류와 성능 측정 기준 P에 대하여 경험 E로부터 학습한다는 것은, P로 측정한 작업 T에서의 성능이 경험 E를 통해 향상되는 경우를 말한다.
    * T (Task): 컴퓨터가 수행해야 하는 작업
    * P (Performance): 얼마나 잘하고 있는지 측정하는 성능 지표
    * E (Experience): 학습의 기반이 되는 데이터나 반복 수 등의 경험

 * 예시
 - 예시 1: 바둑 프로그램 (A program for Go)
    - T (작업): Playing Go (바둑 두기)
    - P (성능): Win or Lose (승패 / 승률)
    - E (경험): The number of Games (대국 횟수 / 대국 데이터)
    - 해설: 바둑 프로그램은 많은 대국(E)을 치르면서 승률(P)을 높이는 방향으로 바둑을 두는 작업(T)을 학습합니다.
    
 - 예시 2: 스팸 메일 감지 (Spam mail Detection)
    - T (작업): Classifying emails as spam or ham (이메일을 스팸 또는 정상 메일로 분류하기)
    - P (성능): spam or ham (스팸/정상 메일 분류의 정확도)
    - E (경험): The number of classified emails (기존에 분류된 이메일의 수 / 데이터)
    - 해설: 스팸 필터는 기존 이메일 데이터(E)를 학습하여 새로운 메일이 왔을 때 스팸인지 아닌지 분류하는 작업(T)의 정확도(P)를 높입니다.

 (3) 방법론

 * 인공지능[가장 포괄적] : 규칙기반 - 사람이 규칙 혹은 알고리즘을 미리 만들어 놓음
  ( 외부 관찰자에게 인간처럼 스마트하게 소프트웨어를 작동시키는 폭넓은 방법, 알고리즘 및 기술 /머신러닝, 컴퓨터 비전, 자연어처리, 로봇공학 및 그와 관련된 모든 주제를 포괄하는 개념 )
 * 머신러닝[인공지능 안] : 학습기반 - 대량의 데이터를 컴퓨터에게 보여줌으로써 스스로 규칙을 만들게 하는 방법
  ( 더 많은 데이터 축적을 통해 성능을 개선할 수 있도록 하는 다양한 알고리즘과 방법론 / 신경망, 서포트 벡터 머신, 결정 트리, 베이지안 신뢰 네트워크, k 최근접 이웃, 자기 조직화 지도, 사례 기반 추론, 인스턴스 기반 학습, 은닉 마르코프 모델, 회귀 기법)
 * 딥러닝[머신러닝 안] : 신경망을 이용한 네트워크. 여러 개의 히든 레이어를 통해 깊게 학습

 (4) 지도학습, 비지도학습

 * 지도학습(supervised learning) : 변수와 레이블 관계 파악. 새로운 변수의 결과 예측
 * 비지도학습(unsupervised learning) : 변수끼리의 관계 파악. 변수의 군집화 혹은 변환
 * 데이터의 형태를 보면, 학습의 목적을 알 수 있음

 (5) 회귀분석, 분류

 * 회귀분석 : 실제 숫자를 예측
 * 분류 : 숫자로 라벨링된 카테고리를 예측
 * 결과값이 타겟

 (6) 클러스터링(군집화), 변환

 * 클러스터링 : 변수들간의 관계를 파악해 군집화
 * 변환 : 변수간의 관계 파악해 공통 특성 추출(정보 압축으로 정보 손실이 있을 수 있음)

### 3. 학습방법

 (1) 지도학습 : 가설 함수(hypothesis)가 머신러닝의 '목적'(증명할 부분). 
 가설 함수를 통해 예측(prediction). cost(평가지표)로 평가 후 최종 value 도출

 (2) 좋은 가설함수란? : cost(실제값과 예측값의 차이)가 낮은 hypothesis가 좋은 가설함수

 (3) 머신러닝의 목적 : 예측을 잘하는 것 = 변수와 레이블의 관계를 잘 찾는것 = cost가 낮은 가설함수를 찾는 것 = cost가 낮은 가중치를 찾는 것
  * "Cost는 Hypothesis에 대한 함수" 

 (4) 가설함수(hypothesis) : weight(가중치)과 bias(편향)에 의해 정해짐
  * cost는 hypothesis에 의해 정해짐 -> 따라서 cost는 가중치에 의해 정해진다고 볼 수 있음
  * 가중치가 정확하면 cost가 낮고, 가중치가 높으면 cost가 높음
    * "Cost는 Hypothesis에 대한 함수"이자 "Cost는 weight에 대한 함수" 

 (5) 가중치(weight) : 변수의 중요도를 의미함. 즉, 머신러닝은 cost가 가장 낮을 때의 weight를 찾는과정 = 변수값의 중요도를 찾는 것
  * 전체과정 : 가중치 초기값 설정 후 첫번째 cost계산. 그리고 cost의 기울기를 계산해 가중치를 업데이트한다.

  * cost가 가장 낮을 때의 가중치를 찾는 방법 : cost값 자체보다, 기울기(Gradient)를 보고 기울기가 낮아(완만)지는 방향으로 가중치 조정 -> 기울기가 0일때 최적의 가중치
  * 즉 기울기 = 가중치가 1만큼 변할 때, cost가 어느 방향으로, 얼만큼 변하는지에 대한 수치
  * 경사하강법(gradient descent) : 기울기값이 -면, 가중치가 증가하면 cost는 낮아짐. 기울기값이 +면, 가중치가 증가하면 cost도 증가함. -> 따라서 cost가 감소하는 방향(기울기 0에 수렴하는 방향)으로 가기 위해서는 기울기값의 반대방향으로 가중치를 이동하면 된다. (기울기값이 -면 +쪽으로 이동, 기울기값이 +면 -쪽으로 이동)

### 4. 머신러닝 파이프라인

 (1) 파이프라인 구조 : **데이터 탐색 - 데이터 가공 - 모델 구축 - 모델 학습 - 모델 검증**
  * 모델 검증이 잘못됐을 시, 데이터 재가공 혹은 모델 튜닝 등 반복 진행

 (2) 데이터 탐색 : 데이터를 직접 살펴본다. 판다스를 활용해 통게치를 뽑아본다. matplotlib, Seaborn 등을 활용해 차트를 뽑아본다. (데이터 이해 - 가설 수립 - 분석 전략 수립)

 (3) 데이터 가공 : 데이터 정제(결측치, 이상값, 오차 등 처리). 피처엔지니어링(정규화, 피처선택, 피처 재생산 등) 

 (4) 모델 구축 : 목적에 따라 설정. 회귀/분류, 정형/비정형, 대규모/중소규모 등 데이터 형태 파악
  * 선형 분류 모형 - 로지스틱 리그레션, SVM
  * 트리 앙상블 모형 - 랜덤포레스트, XGBoost, LightGBM, Catboost
  * 딥러닝 모형 - VGGNet, ResNet, SENet, Xception, LSTM, GRU
  * 나이브베이즈분류모형, KNN
  * 오브젝트 디텍션

 (5) 모델 학습 : 학습(learning)은 **최적화(optimization)**
  * SGD(확률적 경사하강법) → Gradient Descent, 너무느려.. 조금보고 빠르게
  * Momentum(모멘텀) → SGD, 너무 지그재그야., 가던 길로 계속 가자 (글로벌 최소점에 도달할 수 있음. 지역최솟값 탈출 가능)
  * Adagrad → SGD, 탐색거리 맞춤형 조정, 처음엔 크게, 나중엔 작게 (학습률이 0에 수렵할 수 있는 위험)
  * RMSprop → adagrad, 맨날 점점 줄어들어.. 상황을 보면서 줄여야지 (학습률 0이되는 것 방지)
  * Adadelta → adagrad, 맨날 점점 줄어들어.. 가중치의 변화량을 고려해야지
  * Adam → Momentum + RMSprop (이전 데이터방향도 습득 + 학습률 0(기울기소실) 문제도 해결)  <주로 사용>

  * 교차 검증방법 : 홀드아웃, K-fold, LpOcv, LOOCV
  * 학습데이터를 통해 학습할 때는 **규제화**가 중요
  * 과소적합(Underfitting) : 학습데이터도로 검증 잘 못 함
  * 과(대)적합(Overfitting) : 학습데이터로는 검증을 잘 하는데, 검증데이터로는 검증을 잘 못 함
  * 모델 복잡도가 높아질수록 과적합이 발생할 수 있다. 

  * cost = 실제값과 예측값의 차이 + 모델의 복잡도
  * cost가 높아지는 경우 : 차이가 커질 때 or 모델의 복잡도가 커질 때
  * 차이도 줄어들면서 모델의 복잡도가 너무 높아지지 않는 쪽으로 가중치를 설정하는 것 = **규제화**

  (6) 모델 평가
   * R2-score(결정계수) 사용 : 1 - (잔차제곱합, SSE) / (총제곱합, SSR) = (회귀제곱합, SSR) / (총제곱합, SSR)
   * 혼동행렬(confusion Matrix) : TP, FP, TN, FN 표
     * 정확도 : TP+TN / TP+FN+TN+FP
     * 정밀도 : TP / TP+FP  * 실제 n인 데이터 예측을 p로 예측하면 치명적인 경우(스팸 메일 등)
     * 재현율(=민감도) : TP / TP+FN   * 실제 p인 데이터 예측을 n로 예측하면 치명적인 경우(질병 진단, 금융사기 등)
     * F1-score : 2*(정밀도*재현율) / 정밀도 + 재현율

### 5. 선형 회귀

 (1) **가설함수에 어떤 형태의 식을 넣느냐**를 설정하고 학습 진행. 선형형태로 가설함수를 넣어서 회귀분석을 이용할 수 있음

 (2) 선형 회귀분석(Linear Regression) : OLS(Ordinary Least Square )즉, 최소제곱법이라는 회귀용 선형 알고리즘 사용
   * y = wx + b를 가설함수에 넣음 ( “컴퓨터야. X와 y의 관계를 y=wx+b의 형태로 찾아줘. 거기서, w랑 b만 너가 찾아줘” )
 
 (3) 선형 회귀분석의 cost : MSE 등과 같은 함수 방법으로 구한다

 (4) 정규화 : 방정식의 수 = 데이터의 수 = 가중치의 수.
  * 가중치의 수 > 데이터 수 라면, 과적합이 일어날 수 있음


### 6. 릿지 회귀

 - 릿지 회귀는 선형회귀식에서 가중치를 선택하는 기준이 **가설함수가 타겟value에 가까워지는 것 + 제약 조건 만족**
  * 가중치가 클 때는 크게 감소하다가, 0에 가까워질수록 가중치 변화는 거의 일어나지 않는다
    (일반 선형회귀때보다 움직일 때 가중치가 L2 norm만큼 더 감소)

### 7. 라쏘 회귀

 - 
