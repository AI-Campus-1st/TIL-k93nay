# 260729 학습내용

[###함수###]
정해지지 않은 여러 개의 인자 처리

1. 가변(임의) 인자 처리
def func(a, b, *args):  a, b 그밖에 다른 인자들

> `*args` : 임의의 개수의 위치인자를 받음을 의미
          : 튜플형태로 처리되며, 매개변수에 * 표시

2. 가변(임의) 키워드 인자
def func(**kwargs):

> '**kwargs : 정해지지 않음 키워드인자들은 함수를 정의할 때 사용
            : 딕셔너리 형태로 처리되며, 매개변수에 ** 표시
    - 식별자(키)는 숫자로만 이루어질 수 없음
    - 식별자(키)를 리스트 형식으로 만들면, 딕셔너리 가능
    - 함수에서 def <변수명>(**kwargs)라고 만들면, 딕셔너리 형태로 나옴
    - 함수 내 형태 변형 혹은 다른 식이 들어갈 때, key/value값을 넣을 곳을 지정하여 정리하면, 원하는 형태로 return 가능
```py
<예시>
def my_url(**kwargs):
    base_url = 'https://api.go.kr?'
    for key, value in kwargs.items():
        base_url = base_url + f'{key}={value}&'
        # 'https://api.go.kr?sidoname=서울&'
        # 'https://api.go.kr?sidoname=서울&key=asdf&'
    return
```
3. 함수와 스코프
 (1) 전역 스코프(`global scope`) : 코드 어디에서든 참조 가능한 공간
 (2) 지역 스코프(`local scope`)**: 함수가 만든 스코프로 함수 내부에서만 참조할 수 있는 공간
 (3) 전역 변수(`global variable`)**: 전역 스코프에 정의된 변수
 (4) 지역 변수(`local variable`)**: 로컬 스코프에 정의된 변수
   - 매개 변수는 지역 변수
   - 로컬 영역에서 정의한 변수는 글로벌(로컬 밖)에서는 찾을 수 없음 = 전역 스코프에서는 지역 스코프의 변수를 참조할 수 없음
   - 반대로 글로벌에서 정의한 변수는 로컬 영역에서 적용 가능 = 즉, 글로벌이 로컬을 포함한 형태(글로벌 > 로컬)

4. 변수의 수명주기
변수의 이름은 각자의 수명주기(lifecycle)가 있음
 (1) 빌트인 스코프`(built-in scope)` : 파이썬이 실행된 이후부터 영원히 유지
 (2) 전역 스코프`(global scope)`: 모듈이 호출된 시점 이후 혹은 이름 선언된 이후부터 인터프리터가 끝날 때 까지 유지
 (3) 지역(함수) 스코프`(local scope)`: 함수가 호출될 때 생성되고, 함수가 종료될 때까지 유지 (함수 내에서 처리되지 않는 예외를 일으킬 때 삭제됨)

5. 이름 규칙
식별자들은 이름공간에 저장
아래와 같은 순서로 이름을 찾아나가며, 'LEGB rule'이라고 부름
(가장 가까운 곳부터 식별자를 찾음)
  - `L`ocal scope: 함수                                   (가장 작은 범위)
  - `E`nclosed scope: 특정 함수의 상위 함수 
  - `G`lobal scope: 함수 밖의 변수 혹은 import된 모듈
  - `B`uilt-in scope: 파이썬안에 내장되어 있는 함수 또는 속성  (가장 큰 범위)

특징:
* 기본적으로 함수에서 선언된 변수는 Local scope에 생성되며, 함수 종료 시 사라집니다.
* 해당 스코프에 변수가 없는 경우 LEGB rule에 의해 이름을 검색합니다.
    * 변수에 접근은 가능하지만, 해당 변수를 수정할 수는 없습니다.           
    * 값을 할당하는 경우 해당 스코프의 이름공간에 새롭게 생성되기 때문입니다. 
    (= 같은 변수명이어도, 로컬내용이 글로벌에 적용되지 않음)

    * **단, 함수 내에서 필요한 상위 스코프 변수는 인자로 넘겨서 활용합니다.** (클로저 제외) 
    (= 즉, 함수 내부에 변수명을 새로 설정해준 뒤, 상위 스코프 변수를 인자(값)에 넣어서 사용)

* 상위 스코프에 있는 변수를 수정하고 싶다면 global, nonlocal 키워드를 활용 가능합니다.
    * 단, 코드가 복잡해지면서 변수의 변경을 추적하기 어렵고, 예기치 못한 오류가 발생합니다.
```py
<예시>
a = 10
b = 20
def enclosed():
    a = 30
    def local():
        c = 40
        print(a, b, c)
    local()
    a = 50
enclosed()

> 순서
 (1) 로컬, 인클로즈드, 글로벌, 빌트인 순으로 확인
 (2) c=40 먼저 확인(로컬)
 (3) 다 정리하고 나오면, a=50으로 바뀜(로컬)
 (4) 그 다음 a=30, c=40으로 바뀜(인클로즈드)
 (5) 마지막으로 빈 b는 b=20 가져옴(글로벌)
 (6) 따라서 print(a, b, c) 결과는 30 20 40
```
6. 재귀 함수 
함수 내부에서 자기 자신을 호출하는 함수
1000번까지 반복
잘못 사용하면 용량 많이 차지
base case 설정 중요

 (1-1) 팩토리얼 계산 (fact())
```py
def fact(n):  # for문 이용
    ans = 1
    for n in range(1,n+1):
        ans = ans * n
    return ans

> 1부터 n까지 차례로 곱해서 계산한다는 식 

 (1-2) 팩토리얼 계산 (factorial())

def factorial(n):  # 재귀함수 이용
    # 팩토리얼 초기값 설정이 중요 ( '종료조건=base case' 설정 필수 )
    # 팩토리얼 계산에서의 base case는 **n이 1일때, 함수가 아닌 정수를 반환하는 것**
    if n == 1:
        return 1   # 1! = 1이라는 뜻. (1-1)식에서 ans=1이라는 초기 설정과 비슷함
    return factorial(n-1) * n

```
7. 피보나치 수열
```py
F0 = F1 = 1 (base case)
Fn = Fn-1 + Fn-2

 (1-1) 피보나치 계산 (fib())

def fib(n):  # 재귀함수 이용
    # 1번째 항과 2번째 항은 1을 출력한다는 base case 먼저 설정
    if n == 1 or n == 2:  # = 'n in [1,2]'
        return 1
    return fib(n-1) + fib(n-2) # 이전 값(base case까지)을 계속 구해서 더해야함. 즉, 계속 같은 값을 한번 더 계산하는 것. 

(1-2) 피보나치 계산 (fib_loop())

def fib_loop(n):  # for문 이용
    if n in [1,2]:
        return 1
        a = b = 1   # 뒤의 a,b식 구성을 위해 초기값 설정

    # < a, b = b, a+b  구성 순서 >
    # a, b를 한칸씩 뒤로 밀면서 업데이트한다고 생각했을때,
    # 3번째 자리는 a+b 값이 와야함
    # 업데이트 되면, 새롭게 2번째 자리가 a, 3번째 자리가 b가 됨
    # 그러면 a = b, b = a+b로 생각 가능
    for n in range(n-2):
        a, b = b, a+b
    return b

(1-2) 피보나치 계산 (fib_loop2())

def fib_loop2(n): # while문 이용
    a = b = 1

    i = 3 # 3번쨰 항부터 계산
    while i <= n : # n번쨰항까지 반복하도록 설정
        a, b = b, a+b
        i += 1   # i = 4, 5, 6, 7...가도록 설정  
    return b

** 반복문이 재귀함수보다 훨씬 빠름(메모리 사용도 적음)
** 따라서, 특정 요구가 아니라면 반복문을 사용하는게 좋음

```
[###클래스###]

1. 객체(object)

파이썬에서 모든것은 객체
모든 객체는 타입(=자료형), 속성, 조작법을 가짐

- **타입(type)**: 어떤 연산자(operator)와 조작(method)이 가능한가? 
 > 공통된 속성과 조작버을 가진 객체들의 분류
 > 인스턴스(instance) : 특정 타입의 실제 예시

 ## 타입(Type)과 인스턴스(Instance)

| type         | instance                 | 
| -------------| ------------------------ | 
| `int`        | `0`, `1`, `2`            |
| `str`        | `''`, `'hello'`, `'123'` | 
| `list`       | `[]`, `['a', 'b']`       | 
| `dict`       | `{}`, `{'key': 'value'}` | 

- **속성(attribute)**: 어떤 상태(데이터)를 가지는가?
- **조작법(method)**: 어떤 행위(함수)를 할 수 있는가?

 ## 속성(Attribute)과 메서드(Method)


| type         | attributes       | methods                                |
| -------------| ---------------- | -------------------------------------- |
| `complex`    | `.real`, `.imag` |                                        |
| `str`        |       _          | `.capitalize()`, `.join()`, `.split()` |
| `list`       |       _          | `.append()`, `.reverse()`, `.sort()`   |
| `dict`       |       _          | `.keys()`, `.values()`, `.items()`     |

속성은 '<객체>.<속성>' 형태로 씀 (소괄호를 붙이지 않음)
 - .real : 실수부, .imag : 허수부

매서드는 '<객체>.<매서드>()' 형태로 씀 (소괄호를 씀. 함수 호출)


2. 객체 지향 프로그래밍
절차(순서) 중심, 객체 중심, 함수 중심 형태가 있음
종류를 혼합해서 사용함(한가지 중심으로만 구성하지는 않음)

ex. sum, stdev, avg 등 수학 관련 매서드 묶음 => class math에 있는 함수 묶음(='객체 지향 프로그래밍')
> 객체 지향은 설계도(클래스)를 잘 만들어두고, 필요할 때마다 실물(객체)을 뽑아내어 조립식 가구처럼 프로그램을 완성하는 효율적인 방법

특이점
(1) 코드의 직관성
(2) 활용의 용이성
(3) 변경의 유연성

3. 클래스(class)
객체들의 분류를 정의할 때 쓰는 키워드
클래스 안에 정의된 데이터는 속성, 함수는 매서드라고 부름

작성 형태는 아래와 같음
```py
class <클래스이름>:
    <statement>

클래스 이름은 PascalCase 로 정의
```
 ## case 종류
 # snake_case : 변수, 함수 이름에 주로 사용
 - 단어와 단어 사이에 _ 사용
 - int_nums, words, blood_types

 # UPPER_SNAKE_CASE : 상수에 사용 (constant)
 - BASE_URL, PI

 # PascalCase : 클래스명에 사용
 - Person, MariaDB

 # camelCase : javascript, java, rust 변수, 함수 이름
 - crawlOnaPage(), gerConnection()

class <클래스이름>:
    """ (함수설명하는 내용)"""  # = docstring이 들어가 있는 것. 클래스명에 커서를 가져가면, 설명이 나옴 (= 데이터 = 속성)

(3-1). 인스턴스 생성
인스턴스 = 클래스() 형태로 써서 호출

(3-2). 매서드 정의
특정 데이터 타입(또는 클래스)의 객체에 공통적으로 적용 가능한 행위(behavior)들
클래스 안에 여러개의 함수(매서드) 정의 가능
```py
<예시>
class Person:
    # 메서드(method)
    def talk(self):    # 인자로 self를 정의해봅시다.
        print('hi')

    def eat(self, food):    
        print(f'{food}, 냠냠')

p1 = Person()
p1.eat('피자')

=> 피자, 냠냠

```
4. self(인스턴스 자신)
self는 첫번쨰 인자로 본다
따라서 클래스 내 함수 정의에 self가 들어가면, 이후에 밖에서 인자를 넣지 않아도 기본값 출력
함수 출력 위치값과 print 위치값이 동일 (p1.test() = print(p1))
빈자리가 아님

5. 생성자(constructor) 매서드
인스턴스 객체가 생성될 때 가장 기본으로 호출되는 함수. 반드시 `__init__` 이라는 이름으로 정의 (=initialize)

```py
class MyClass:
    def __init__(self):
        print('생성될 때 자동으로 호출되는 메서드입니다.')

<에시>
class Person:
    def __init__(self, name):
        self.name = name
        print('사람 생성')
    
    def talk(self):
        print(f'I am {self.name}')

ex. p1 = Person('john') 은 출력// p1 = Person()은 오류 ==> 인자(name)를 넣지 않으면 기본 함수 계산이 안되므로 오류 발생

위처럼 함수 인수가 결합되어 있는 경우, 
p1 = Person('john')
p1.talk()
하면 I am John 출력
```
6. 소멸자(destructor) 메서드
인스턴스 객체가 소멸(파괴)되기 직전에 호출되는 함수. 반드시 `__del__` 이라는 이름으로 정의

```py
def __del__(self):
    print('소멸될 때 자동으로 호출되는 메서드입니다.')


<예시>
class Person:
    def __init__(self):
        print('응애!')
        
    def __del__(self):
        print('떠날게..')

p1 = Person() => 응애!
del p1 => 떠날게..

```
7. 속성 정의
특정 데이터 타입(또는 클래스)의 객체들이 가지게 될 상태/데이터를 의미
self.<속성명> = <값> 혹은 <인스턴스>.<속성명> = <값>`으로 설정
```python
class Person:
    def __init__(self, name):
        self.name = name
```
 * 속성 정의는 꼭 생성자(__init__) 메서드에 작성해야 하지는 않지만, 일반적으로 생성자 메서드에 많이 작성
 * 생성자 메서드를 통해 생성과 동시에 인스턴스 속성에 값을 할당할 수 있기 때문

인스턴스 속성의 값을 변경할 수도 있음

<예시>
p1.name = 'alice'
p1.talk()
=> 안녕, 나는 alice