### 260826 학습내용

### 웹크롤링 7일차 - 동시성과 Scrapy ###


#### 1. 왜 동시성인가

 (1) 순차 처리의 한계

 - 요청시간 내 계산시간은 매우 작고, 나머지는 대기 시간
 - 이 때 다음 요청을 보내면, 시간이 크게 줄어듦

 (2) I/O bound 와 CPU bound

 | | I/O bound | CPU bound |
 |:--:|:--:|:--:|
 | 병목 | 네트워크·디스크응답대기 | 계산그자체 |
 | 예 | 크롤링, API 호출, 파일읽기 |이미지처리, 대량파싱, 압축|
 |맞는도구 |스레드· asyncio |프로세스|
 |GIL 영향 |거의없음 |있음— 스레드로는 안빨라짐|

 (2) GIL : 한 번에 하나의 스레드만 파이썬코드를 실행
 
 - 그래서 계산 작업은 스레드를 늘려도 빨라지지 않음
 - 네트워크를 기다리는 동안에 GIL이 해제됨

 (3) 세 가지 도구

 - 멀티스레딩, 멀티프로세싱, 비동기(httpx)

#### 2. 멀티스레딩

 (1) Pool 과 Map

 - teminal에서 실행 확인해야 편리
 - pool.map(f, args) — 함수를 인자목록에 하나씩 적용
 - 결과는 인자 순서그대로 리스트로 돌아옴
 - with 문을 쓰면 풀이 자동으로 정리

 (2) multiprocessing.dummy : 이름과 달리 실제로는 스레드

 - 프로세스API를 그대로 흉내낸 스레드
 - 스레드와 프로세스를  import 한 줄만 바꿔 갈아탈 수 있음
 - 어느쪽이 빠른지 실험할 때 사용

 (3) map 계열 네가지

 |메서드| 인자 |결과 |쓰는 때|
 |:--:|:--:|:--:|:--:|
 |map |인자하나 |순서대로 리스트 |기본값|
 |starmap |인자 여러개(튜플) |순서대로 리스트 |함수가 인자를 둘이상 받을때|
 |imap |인자하나 |순서대로 이터레이터 |결과가 크거나 진행률을 보고싶을때|
 |imap_unordered |인자하나 |끝난 순서대로 |순서가 상관없고 가장빠를때|

 (4) 인자가 여러개일 때

 - starmap은 튜플`(a, b)` 를`f(a, b)` 로 풀어 호출
 - lambda는 오류
 
 (5) 예외는 어디서?

 - 하나가 실패하면, map 전체가 예외를 올림
 - 안전하게 하려면, 함수를 잡아서 결과로 돌려주는 방식이 좋음

#### 3. 멀티 프로세싱

 (1) Pool.map — 코드는 그대로

 - import만 바꾸면 프로세스로 바뀜
 - `if __name__ == "__main__":` 이 없으면 프로세스가 무한 생성(반드시 필요)
 - 수집해둔 것을 CPU를 통해 파싱할 때 유용

 (2) 스레드와 프로세스

 | |스레드| 프로세스|
 |:--:|:--:|:--:|
 | 메모리 |공유한다 |따로쓴다(복사)|
 |데이터전달 |그냥참조하면 됨 |직렬화해서 복사|
 |생성비용 |작다| 크다|
 |GIL |걸린다(I/O 중엔해제)| 각자자기GIL|
 |적합 |네트워크대기| 계산|

 (3) 직렬화 제약

 - lamda, 세션 객체는 못넘김
 - 함수 형태는 넘길 수 있음

 (4) 프로세스가 필요한 경우

 - 대량 파싱할 때, 이미지/문서 처리할 떄 (수집 그 자체에 목적이 있을 때는 사용하면 안됨) 

#### 4. 비동기 - httpx

 - 대기하지 않고, 여러개를 동시에 처리하는 방식 (하나의 CPU를 최대한 활용)
   * 프로세스는 여러 개의 CPU를 할당하는 방식

 - 즉, 하나의 스레드 안에서 대기가 생길 때마다 다음작업으로 바로 넘어감
 - 전환 비용이 거의 없어, 대량의 요청도 감당 가능
 - 대신 async, await 문법 등을 사용해야함

 (1) httpx - requests의 비동기 방식

  - 코드는 requests와 거의 같음
  - 차이는 같은 라이브러리가 비동기도 지원한다는 점

 (2) 비동기방식의 기본 골격 - AsyncClient와 gather 사용

 - import asyncio, httpx
 - async def fetch(client, url): ~~~ 이런 형태
 - await가 붙은 지점에서 다른 작업으로 넘어감
 - `return_exceptions=True` : 하나가 실패해도 전체가 멈추지 않음 
   * 예. return await asyncio.gather(*tasks, return_exceptions=True) > 오류나면 exception 객체 그대로 가져옴
   * cf. gather : 비동기로 동시 호출했던 것을 다 모으는 것

 - 결과에 실패(예외) 객체가 섞여오므로 따로 걸러내야함

 (3) 비동기는 동시 진행 수에 제한을 걸어야함

 - `asyncio.Semaphore`로 동시에 진행되는 작업수 제한
 - 예. async with sem:
 - sem = asyncio.Semaphore(3)  ## 최대 3개로 제한
 - await asyncio.sleep(0.7)  ## 매너지연
 - `httpx.Limits` 로 커넥션 수 자체도 제한(2차 제한)
 - 예. limits = httpx.Limits(max_connections=3)

 (4) 3가지 방식 비교

 | |Thread Pool | Process Pool | asyncio + httpx |
 |:--:|:--:|:--:|:--:|
 |적합 |I/O bound | CPU bound | I/O bound (대량)|
 |코드변경 | 거의없음 | 거의없음 | 전면수정 |
 |호출형태 | pool.map | pool.map | asyncio.gather |
 | 오버헤드 | 작음 | 큼 | 가장작음 |
 |적정규모 | 수백~ 수천 | 코어 수 만큼 | 수천~ 수만 |
 |추천 | 기본값 | 파싱전용 | 대규모전용 |


#### 5. 매너크롤링

 (1) 크롤링할때 지켜야할 것

 - 동시성은 상대 서버에 그대로 전가되는 트래픽 비용
 - 상대 서버가 부하가 올 수도 있음
 
 (2) 실전 기본값

 - 워커 수와 지연을 함께 넣어야 실제 요청 속도가 나옴
 - MAX_WORKERS = 3 DELAY = 0.7 # 초  => 실효속도 : 3 / 0.7 ≒ 초당 4.3 요청 (시간당 15,000건)
 
 (3) 429와 서킷브레이커

 - status_code를 확인하면서 대기시간을 조절하면, 과부하 방지 가능
 - `Retry -After`헤더가 있으면 그 값을 그대로 따름
 - 차단이 반복되면 속도를 줄이거나 수집을 재검토해야함


#### 6. Scrapy

 (1) 재시도, 지연, 동시연결제한, 로깅, 중복제거가 기본 탑재

 - 비동기 엔진 위에서 돌아감(async 쓰지 않아도 됨)
 - 이미 만들어진 크롤링 프레임워크로 생각하면 됨
 - 단일 스레드 사용
 - 어떻게 수집, 파싱하는지만 지정하면 됨

 (2) Scrapy 구성요소

 - SPIDER : 시작 URL, 파싱 규칙 (fetch() + parse())
 - ITEM : 수집결과의 스키마, 필드를 미리 선언
 - PIPELINE : 정제, 검증, 저장 (clean() + save())
 - MIDDLEWARE : 요청 전후 처리. 헤더주입·재시도·프록시. 안정성 관련 장치들

 (3) SPIDER 한벌

 ** import scrapy
class BookSpider(scrapy.Spider):
    name = "books"
    start_urls = ["https://books.toscrape.com/"]
    def parse(self, response):
        for card in response.css("article.product_pod"):
        yield {
            "title": card.css("h3 a::attr(title)").get(),
            "price": card.css("p.price_color::text").get(),
        }
    nxt = response.css("li.next a::attr(href)").get()
    if nxt:
        yield response.follow(nxt, self.parse) # 다음페이지 

 - `yield` 로 결과를 내보내면 Scrapy가 알아서 모음
 - `response.follow`가 상대주소를 절대주소로 바꿔 이어감

 (4) Pipeline — 정제와 저장

 - 여러 작업을 순서대로 이어 붙일 수 있음
 - `DropItem`을 쓰면, 조건에 맞지 않는 항목 걸러냄

 (5) Settings.py에서 설정하는 것들

 | 설정 | 하는 일 | 우리가 만들었던 것 |
 | :--: | :--: | :--: |
 | DOWNLOAD_DELAY |요청간격 | time.sleep(0.7) |
 | CONCURRENT_REQUESTS_PER_DOMAIN | 도메인 당 동시연결 | Semaphore(3) |
 | AUTOTHROTTLE_ENABLED | 응답속도에 맞춰 자동조절 | (직접만들지않음) |
 | RETRY_TIMES · RETRY_HTTP_CODES | 재시도 | urllib3.Retry |
 | ROBOTSTXT_OBEY | robots.txt 준수 | RobotFileParser |
 | FEEDS | 결과저장 | to_csv | 

(6) 언제 사용?

- 수집 대상이 많고, 오래 운영할 프로젝트일 때
- 1-2개 사이트나 작은 규모는 직접 짜는 게 빠름
- 자바스크립트 랜더링은 별도 플럭그인이 있어야함

(7) 동시성 단계

진단(I/O인가 CPU인가) > 도구 선택(스레드, 프로세스, async 중) > pool.map(함수와 인자목록) > 안전장치 설정(장기/대규모인 경우, semaphore) > 프레임워크 구성

