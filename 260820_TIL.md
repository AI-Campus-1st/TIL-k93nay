### 260820 학습내용

### 웹크롤링 3일차 - 동적웹 ###

1. 브라우저가 나중에 불러오는 데이터를 바로 가져오는법

- 동적웹은 데이터가 있어야할 자리가 비어있음

 (1) 비동기 렌더링 4 단계

 * HTML 수신 - JAVASCRIPT 실행 - API서버에 데이터 요청 - 화면 완성

 (2) 브라우저와 requests

 * 브라우저는 자바스크립트를 실행하지만, requests는 실행하지 않는다
 * 따라서 추가 데이터 요청도 requests는 하지 않는다
 * 브라우저는 최종 화면, requests는 최초의 뼈대(겉모습)를 보여줌

 (3) Selenium(브라우저를 띄우는 도구)은 쓰지 않음

- 화면 안 데이터를 전부 가져오므로 requests보다 훨씬 느림(속도문제). 그리고 데이터가 더 자주 깨짐(안전성문제)
- 데이터는 HTML보다 JSON포맷에서 뽑아내므로 더 깔끔함

2. 진단하기

- CTRL + U : 서버에 원본값이 있는가 페이지 소스 확인 가능 (CTRL+F로 원하는 데이터가 있는지 검색 가능)
- response.text : 파이썬이 받은 문자열에 값이 있는지 확인
- Elements : 브라우저가 완성한 화면 안에 있는지 확인(F12)

 (1) 페이지 소스 확인
  - Beautifulsoup으로 파싱해보고, 데이터 확인(정적웹에서 배웠던거)

 (2) response 확인
  - URL = 'https://cine21.com/movie/rank'
  - response = requests.get(URL)
  - print(“스파이더맨" in response.text)     # True면 정적, 파싱하면 됨. False면 값없음, 동적웹 의심

 ** 페이지소스와 결과가 다르면 헤더나 쿠키 문제일 수 있음
 ** len(response.text)를 해보면 빨리 파악 가능

 (3) Elements로 확인
  - F12의 Elements 탭은 자바스크립트까지 실행된 결과를 보여줌
  - 여기엔 값이 있는데 페이지소스(Source)엔 없다면, 확실히 동적

 (4) 요약
| 소스 | Elements | 판정 | 대응 |
| :---: | :---: | :---: | :---: |
| 있음 | 있음 | 정적 | requests + BeautifulSoup |
| 없음 | 있음 | 동적— 내부API | Network에서 요청을 찾아 requests로 재현 |
| 없음 | 없음 | 로그인·권한필요 | 세션처리 |
| 없음 | 있음· 요청못찾음 | 토큰·암호화 | Selenium 검토 |

3. Network탭 정독

 (1) 네트워크 탭 구조

- Name(파일이름), Status(응답코드), Type(fetch, xhr 등의 요청종류), Size(응답크기)

 (2) Fetch/XHR 필터 : 가장 먼저 눌러야 할 버튼

- 자바스크립트가 보낸 데이터 요청만 남김

 (3) preserve log : 페이지를 넘겨도 기록이 남음

- 체크하지 않으면 페이지 이동순간 목록이 초기화
- 로그인·검색처럼 화면이 바뀌는 동작을 관찰할 때 필수

* 관찰순서
** 1. Network 탭열기
** 2. Preserve log 체크
** 3. Clear 로 비우기
** 4. 화면에서 동작 수행
** 5. 새로 생긴 요청만 확인

 (4) Headers 읽기 : 요청에 필요한 정보가 다 있음

| 네트워크 탭 | 요청 | 요청내용 |
| :---: | :---: | :---: |
|General |Request URL · Method | 어디로 어떤 방식으로 보냈는가. 재현의출발점 |
|General |Status Code | 200이어야 데이터가 담긴 요청(성공) |
|Request |Headers User-Agent · Referer | 서버가 요구하는 신원정보. 빠뜨리면 403이 돌아옴 |
|Payload |Query String · Form Data | 무엇을 조건으로 보냈는가. 페이지·검색어가 여기에 있음 |

 (5) payload : 조건이 어디 실렸는지 확인

- Query String이면 URL에 그대로 붙이면 됨
- Form Data면 requests의 data= 로 넘기면 됨

 (6) Preview로 데이터 찾기 

- JSON을 트리로 보여줌 (코드가 정리되어 보여짐)
- 화면에서 본 값이 여기 보이면, 그 요청이 정답
** Response 탭은 원문코드 그대로 — 복사해서 쓰기 좋음

 (7) 값으로 역추적하기 : 유일한 값으로 찾아보기

- 한글은 인코딩문제로 검색되지 않을 수 있음


4. API재현과 JSON

 (1) Request로 URL 해부

 (2) Copy as cURL : 요청복사 (Copy as Fetch-node js-가 더 많이 쓰임)

- 헤더와 쿠키까지 통째로 복사 (requests 코드로 가져오면 브라우저와 똑같은 요청이 됨)
- fetch : 헤더 확인할 때, 한번에 긁어와서 사용. (user-agent는 따로 가져와야함)

 (3) cURL을 requests로 : -H 는 headers, --data 는 data 로

- -H 하나가 headers 딕셔너리의 항목 하나

 (4) JSON : 데이터의 표준 형식 (키는 반드시 큰따옴표만 사용)

- 파이썬의 딕셔너리와 비슷
- 중괄호는 딕셔너리, 대괄호는 리스트
- true/false/null이 파이썬에서는 True/False/None 

 (5) JSON과 파이썬 비교

|JSON | 파이썬 | 예시 |
| :---: | :---: | :---: |
| object | dict | {"id": 1} |
| array | list | [1, 2, 3] |
| string | str | "orange" |
| number | int · float | 12345 |
| true / false / null | True / False / None | – |

** JSON 포맷이 잘못됐을 경우에는, 일부만 뽑아내어(split) 문자열 처리 후 json.loads 시행

 (6) response.json() : 한줄이면 딕셔너리가 됨

<형태>
data = response.json() # 권장

** 같은일
import json
data = json.loads(response.text)

- response.json()은 인코딩까지 알아서 처리
- 실패하면 response.text[:200]을 찍어 오류 확인

 (7) 중첩구조 접근 : preview에서 어떤 구조로 되어있는지 확인 후 실행

- preview에서 확인한 키를 대괄호로 씌워 가져오면 됨
- 리스트가 나오면 반복문(for), 딕셔너리가 나오면 키접근({})

 (8) 리스트 안의 딕셔너리 

<형태>
result = []
for item in items:
    result.append({
        "제목": item.get("hname", ""),
        "관객수": item.get("aud", ""),
    })

- 이 형태로 모으면 DataFrame·CSV·DB 어디로든 바로 감
- 키 이름은 수집 시점에 한글로 통일해두면 나중이 편함
- csv로 저장한다면, utf-8-sig 로 저장해야 엑셀에서 한글이 깨지지않음

 (9) 숨은API 처리 요약

* 진단(동적웹인지 확인) > 관찰(fetch, xhr 탭 확인) > 확인 > 재현(cURL, requests) > 파싱(json, dict)