### 260901 학습내용

### 웹크롤링 11일차 - Streamlit ###


#### Streamlit

 (1) 특징

 - 간단한 설치 및 사용 가능
 - 빠른 대시보드 개발 속도(빠른 학습)
 - 직관적인 API (예. 텍스트 = st.write, 데이터프레임 = st.dataframe)
 
 (2) 장점
 
 - 빠른 프로토타이핑 : 가공된 데이터를 빠르게 시각화하고 공유
 - 오픈소스 프로젝트 : 커뮤니티 활발, 튜토리얼 등 매뉴얼 제공
 - 확장성 : 파이썬과 호환되기 떄문에, 다양한 시각화 라이브러리와 쉽게 통합할 수 있음
 - 배포 용이성 : 클라우드에 쉽게 배포 가능(github링크로도 deploy-전개-가능)

 (3) 설치

 - 프로젝트 단위로 requirements.txt를 작성해 의존성을 관리하는 것을 권장 (버전관리의 목적으로도 사용)
   * 가상환경은 대시보드를 개발하는 데 필요한 라이브러리만 깔끔하게 관리 (버전관리를 함께해 충돌 발생가능성 낮춤) > 동일한 환경을 다시 구성할 수 있음 > 배포의 편의도 있음


<streamlit 실전 예제>
 - conda, venv, poetry 등 활용
 - pip install streamlit으로 설치
 - pip install -r req {TAB누르면} - requirements.txt 실행


** ====================================================================
 import streamlit as st

*# 대시보드 제목 적기
st.title('첫 번째 대시보드')
st.write('대시보드 앱 개발 시작')

*# 간단한 본문 글자 적기
st.write('새로 저장')
st.write('always rerun하면 개발할 때 유용함')

*# streamlit run app.py (파일경로) 실행하면 대시보드(8501) 뜨면서 창이 뜸

v = st.button('클릭')
print('if문 밖', v)
if v:
    print('if문 안', v)
    st.success('성공')

vol = st.slider('볼륨', 0, 100, 25)
print('볼륨 값:', vol)
*# (print)가 다 실행된다.
*# 2. 상호작용이 있는 뒤에 모든 코드가 다시 실행된다.

*# 3. 저장을 할때마다 rerun (auto rerun) 다시 실행
*# (파일 변경할때마다)

*# 결론: streamlit은 사용자의 상호작용이 있은 뒤에,
*# 전체 코드를 다시 실행한다.

*# 3개의 입력창을 한 줄에 나란히 배치하기 (컬럼 나누기)
col1, col2, col3 = st.columns(3)

with col1:
    email = st.text_input('이메일 주소')

with col2:
    age = st.number_input('나이', min_value=0, value=0, step=1)

with col3:
    intro = st.text_area('자기소개')

*# 터미널 콘솔창에 입력값 확인용 print 출력 (선택 사항)
print('텍스트입력:', email)
print('나이:', age)
print('자기소개:', intro)

*# 접고 펼 수 있는 '더보기' 메뉴 만들기
with st.expander('더보기'):
    st.write('여기에 더보기 안의 내용을 작성하시면 됩니다.')

** ====================================================================

st.set_page_config(layout="wide")   # 페이지 레이아웃을 wide로 넓게 설정 (기본값은 centered)

st.title('Streamlit 레이아웃 설정 예제')
st.write('이 예제는 Streamlit의 레이아웃 설정 방법을 보여줍니다.')

*# st.sidebar.write('## 사이드바')

*# 왼쪽에 있는 사이드바 생성 코드 
with st.sidebar:
    st.title('사이드바')

    st.write('여기에 사이드바 내용을 추가할 수 있습니다')
    v = st.slider('사이드바 슬라이더', 0, 100, 25)

    st.write(f'선택된 값: {v}')


cols = st.columns(2)    # ([0.5, 5, 2]) 이런식으로 쓰고 3개 칼럼을 나누면, 0.5:5:2 비율로 칼럼이 나뉨

*# 칼럼은 한번 나눠지면, 그 안에서 다시 나눌 수 있음. 하지만 이후에 합쳐지는 경우는 적음

with cols[0]:
    st.title('칼럼1')
    st.write('여기는 첫번째 칼럼입니다')
    st.button('칼럼1 버튼')

with cols[1]:
    st.title('칼럼2')
    st.write('여기는 두번째 칼럼입니다')
    st.button('칼럼2 버튼')

with st.expander('더보기'):
    st.write('여기에 추가정보를 넣을 수 있습니다') 
    with st.container(width='content', horizontal_alignment='center'):
        st.markdown('![예시 이미지](https://placeholder.com/150x150//EEE/31343C)')
        st.markdown('<span style="color: gray;">예시 이미지</span>',
                text_alignment='center',
                unsafe_allow_html=True)