# 260723 학습내용

1. 예약어 
def : 함수정의
class : 객체 정의
del : 변수 삭제 // return
if, elif, else : 조건문
match-case : 구조적 패턴 매칭
for, while // break, continue, pass : 반복문
import, from, as : 모듈
try-except, finally, raise : 에러처리
in : 포함 테스트

2. 문자열 타입
(1) ', "로 표현 가능, 삼중따옴표는 여러 줄 작성시 사용
(2) 변수에 input()의 결과를 저장하면, 기본적으로 str(문자열) 타입으로 출력
int(정수)로 바꾸고 싶다면, 별도 변수를 설정하고 int()를 사용해 앞선 변수를 숫자로 변환
-> 이때, 변환할 문자열이 숫자 형식을 갖추고 있어야함

2. 따옴표 사용
(1) 하나의 표현식 안에 같은 문장부호(따옴표)를 계속 사용하고 싶으면, 역슬래시 사용
(2) 여러줄은 """ 사용
(3) 문자열 조합 시, *(반복), +(문자열 합치기)는 사용 가능하나, 나누기 빼기는 안됨
그리고 *(반복) 시에는 반드시 숫자와 곱해야함(단어 반복시키는 것이므로)
(4) 문자열을 활용하는 경우 특수문자 혹은 조작을 하기 위하여 사용되는 것으로 `(역슬래시)\`를 활용하여 이를 구분합니다. 
|\n : 줄 바꿈|
|\t : 탭|
|\r : 캐리지리턴|
|\0| : 널(Null)|
|\\ : \ 출력
|\' : 단일인용부호(`'`)|
|\" : 이중인용부호(`"`)|

(5) 문장에서 end옵션 기본은 줄바꿈. 이걸 바꾸고 싶으면 -> ", end ='(넣고싶은 부호)'"를 넣어 출력
ex. print('내용을 띄워서 출력하고 싶으면?', end='\t')
     print('옆으로 띄워짐')
-> 내용을 띄워서 출력하고 싶으면?	옆으로 띄워짐

3. string interpolation (문자열 보관법)
기본 : %-formatting (%d 정수, %f 실수, %s 문자열)
파이썬에서는 str.format()
작성 방법은 3가지가 있음
- (1) print('내 이름은 %s' %name + '내 학점은 %.2f' %score) : '내 이름은 jason 내 학점은 4.50'
- (2) '내 이름은 {} 점수는 {}'.format(name, score) <- 이때는 순서대로 넣어짐
- (3) f'내 이름은 {name} 점수는 {score}'
여러줄 작성시에는 아래와 같음
print(f"""
내 이름은 {name}
점수는 {score}
""")

4. 현재 날짜 및 시간 표현, 반복문
- (1) import datetime
today = datetime.datetime.now()
print(today)
-> 현재 날짜와 시각(초단위까지) 나옴

ex. print(f'오늘은 {today:%Y}년 {today:%m}월 {today:%d}일 {today:%H:%M:%S} {today:%A}요일')
-> 오늘은 2026년 07월 23일 10:37:18 Thursday요일
-> : 작성 후, 넣을 수치에 대한 것들(%Y, %m, %d 등)만 표시해 기입하면 나눠져서 나옴

- (2) 숫자의 경우에는 {score:.2f}혹은 {score:^10.2f} 등으로 쓰면 단위 및 표시 지정 가능
ex. pi = 3.141592
radius = 2
print(f'반지름이 {radius}일 때 원의 넓이는 {pi*radius**2:.6f}.')
-> 파이*반지름의 제곱을 소숫점 6자리까지 구하는 것
-> 반지름이 2일 때 원의 넓이는 12.566368.

- (3) 반복문(for 뒤에 해당 요청내용 작성 후 ":"로 마무리)

radius = 2
for radius in range(1, 6):   -> 뜻 : radius를 1이상부터 6미만까지 넣어 아래의 문장 반복
    print(f'반지름이 {radius}일 때 원의 넓이는 {pi*radius**2:.6f}.')

반지름이 1일 때 원의 넓이는 3.141592.
반지름이 2일 때 원의 넓이는 12.566368.
반지름이 3일 때 원의 넓이는 28.274328.
반지름이 4일 때 원의 넓이는 50.265472.
반지름이 5일 때 원의 넓이는 78.539800.

5. 즉, 문자열은 변경할 수 없고(immutable), 순서가 있고(ordered), 순회(순차적 반복)(iterable) 가능
- (1) 조회/탐색 시 사용 : find()
a = 'party'
find_p = a.find('p')
print(find_p)
-> 첫번째에 p가 있으므로 '0' 반환
find_z = a.find('z')
print(find_z)
-> z가 없으므로 '-1' 반환

- (2) 위치 값 확인 : index()
a = 'asap'
a.index('p')
-> p의 위치값 '3' 반환
a.index('z')
-> ValueError: substring not found. 즉, 오류 발생

- (3) 대체(교체) : replace(기존 문자, 새 문자) // replace(기존 문자, 새 문자, 시행 갯수(앞에서부터 바꿈))
ex. b =  'bootcamp' 
b.replace('o', '_', 1)
-> 'b_otcamp'
count 부분에 따로 표시 안하면, 전부 바꿈
b.replace('o', '_')
-> 'b__tcamp'

- (4) 공백제거 : strip()
그냥 strip() : 양쪽 끝의 공백 제거
lstrip() : 왼쪽공백 제거
rstrip() : 오른쪽공백 제거
특정글자 제거도 가능
ex. b = '    반갑   습니다   hi'
b.rstrip('hi')
-> '    반갑   습니다   '
하지만, hi뒤에 공백이 있다면, 지워지지않음 -> 이경우에는 b.rstrip(' hi'), 즉 조건 안에 공백도 넣으면 같이 삭제 가능

- (5) split() : 문자열을 특정 단위로 나누어 리스트로 반환
그냥 split() : 문자열 사이 공백을 기준으로 구분하여 리스트 반환
ex. nums = input()
nums.split()
3 4 5 6 
-> [3, 4, 5, 6]

a = 'python_java_rust_c++'
a.split('_')          -> '_'기준으로 구분
-> ['python', 'java', 'rust', 'c++']

리스트 반환 후, nums[0]과 같이 index(위치값) 도출 가능

** 참고
nums = input()
nums.split()
print(nums)
이 경우에는, nums는 입력값만 남아있음

nums = input()
nums = nums.split()
print(nums)
이렇게 해야 list형으로 변한 값 반환(이때, 리스트 내 모두 문자열로 반환)
int(nums[2])
이런식으로 써야 최종 숫자 형태로 도출 (그냥 쓰면 list형 전체가 들어오는 것이므로 문자열로 인식불가)

- (6) 'separator'.join(iterable) : 요소 사이사이에 separator가 들어간다는 뜻
ex. word = 'python' -> 글자 하나하나 요소로 봄
words = ['ai', 'campus', 'boot', 'camp'] -> 각 문자를 요소로 봄
'!'.join(word) 
-> 'p!y!t!h!o!n'

ex. ''.join(words)
-> 'aicampusbootcamp'

- (7) capitalize, title, upper, lower, swapcase

capitalize : 맨 앞만 대문자
title : 각 글자 앞 대문자
upper : 전부 대문자
lower : 전부 소문자
swapcase : 대소문자 변경

-> 이걸하더라도 기존 값은 변경되지 않음. (print(a) 쓰면 원래값 반환)

- (8) 참/거짓 반환 메소드
.isalpha, .isdecimal(순수 int로 변환 가능한 문자열인지), 
.isdigit(문자열에서 사용한 글자들이 숫자인지 확인), .isnumeric(분수의 특수기호도 True로 판정), 
.isspace, .isupper, .istitle, .islower

- (9)  dir() : 문자열이 가지고 있는 매소드(함수 방법) 확인 가능


6. 컨테이너 : 여러개의 값을 저장할 수 있는 것
시퀀스형 : 순서대로 나열된 것. 특정 위치의 데이터를 가리킬 수 있음
리스트, 튜플, 레인지, 문자형 등등이 있음
- (1) 리스트형 : [] or list()
ex. locations = ['서울', '부산', '대구', '광주', '제주']
locations[0]
-> '강릉'

-  (2) .append('') : 해당 값을 리스트에 추가(하나의 값 추가)
값을 그대로 반환(문자형은 문자형으로, 리스트형은 리스트형으로)

- (3) .extend("") : 여러 값을 리스트에 한꺼번에 추가 ('+=' 로도 추가 가능)
* 그냥 +만 할 경우, 실제 리스트에는 반영되지 않음
값을 리스트 내 각 요소로 반환(즉 리스트형 -> 문자형으로 넣어줌, 문자형은 각 철자를 요소로 반환)
ex. cafe = ['starbucks', 'tomntoms', 'hollys']

cafe.extend(['whcafe', '빽다방'])
print(cafe)
-> ['starbucks', 'tomntoms', 'hollys', 'whcafe', '빽다방']

cafes.extend('ediya')
print(cafe)
-> ['starbucks', 'tomntoms', 'hollys', 'whcafe', '빽다방', 'e', 'd', 'i', 'y', 'a']

- (4) insert(index(위치), x(값)) : 정해진 위치에 값 추가
* len() : 현재 요소 갯수
cafes.insert(len(cafes), 'end') : len()가 결국, '요소 갯수 = 마지막 요소 위치'를 나타내므로, 마지막 값을 추가할 때에도 사용
-> ['start', 'starbucks', 'tomntoms', 'end']

.insert(요소 갯수보다 큰 수, 값)으로 작성 시에는, 마지막 쪽에 값 추가해줌
(= 리스트의 길이를 넘어서는 인덱스는 마지막에 아이템이 추가)

마지막 인덱스에 접근하는 방법 : 'len(변수이름) - 1' <- 요소 갯수에서 -1을 하면, 마지막 요소의 위치값이 됨
따라서 print(cafes[len(cafes) - 1]) 하면, 마지막 요소 반환
print(cafes[-1])를 해도 마지막 요소 반환  <- 파이썬에서만 가능

요소 길이를 벗어나면, indexerror발생(out of range)
 
- (5) remove() : 값을 지워주는 것
중복된 값에 대해 지울 때에는 가장 앞에것 1개만 지워짐
ex. numbers = [1, 2, 3, 1, 2]
numbers.remove(1)
numbers
-> [2, 3, 1, 2]

- (6) pop(i=위치값) :  정해진 위치에 있는 값 삭제 후 반환
따로 위치값이 없으면, 마지막 삭제 후 반환

- (7) clear() : 리스트의 모든항목 전부 다 삭제


7. 탐색 및 정렬
- index() : 위치값 반환. list내 해당 값이 없으면, 위치값 반환하지 못함(오류 발생)
- count() : 값의 개수 반환
- sort() : 정렬 (원래 리스트를 정렬)
sort() 자체는 정렬을 하나, 새로 리스트를 만드는게 아니므로 None 값 반환
- sorted() : 값 정렬하여, 새로운 리스트 반환  (새로운 리스트 생성. 원본 유지)
- .sort(reverse=True)하면 역순 정렬 가능
- reverse() : sort()와 마찬가지로 원래 리스트를 역순으로 하는 것이므로, 그 자체로는 "None"값이 나옴(새로운 리스트 생성. 원본 유지)
- list(reversed())로 하면, 역순으로 된 리스트 반환
ex. classroom = ['Tom', 'David', 'Jason']

result = list(reversed(classroom))
print(result)
print(classroom)

-> ['Jason', 'David', 'Tom']
['Tom', 'David', 'Jason']


8. 튜플(tuple)
()로 묶어서 표현. 수정 불가능.
파이썬 내부적으로 사용하는 경우가 많음
소괄호로 묶지 않아도 파이썬 내부에서는 튜플로 묶을 수 있음(기본이 튜플)
ex. x, y = 1, 2   =>>>> x, y = (1, 2) 와 같음

하나의 값일 경우, '(3, )'이런 식으로 작성(값 뒤에 반드시 "," 붙이기)
그냥 (3)으로 하면, 함수 수식 중 '우선순위 계산식'으로 여겨짐 
쉼표 없이 문자가 쓰여도 str으로 인식

함수는 하나의 값만 반환(튜플)
튜플은 인덱스(위치값) 반환 가능
그러나 값 변경은 되지 않음


9. range(숫자의 시퀀스, 연속 시행)
range(n, m, s) : n부터 m-1까지 +s만큼씩 출력
ex. a= range(0, 11, 2)
print(list(a))
-> 0, 2, 4, 6, 8, 10

시퀀스에서 활용할 수 있는 함수
in
not in
+
*
s[i]
s[i:j]
s[i:j:k]
len
min
max
s.count



*** 과제 수행 시
vs code - workspace에서
주피터 노트북 만들어서
코드 작성, 테스트, 확인

TIL 폴더에 오늘날짜_과목.txt
생성 후,
1. 정답 코드
2. 정답 코드

그것을 과제 제출 파일로 업로드
TIL은 git add commit push까지 진행


