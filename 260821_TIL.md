### 260821 학습내용

### 웹크롤링 4일차 - 동적웹 ###

POST · 헤더· 쿠키· 페이징, 그리고 멈출줄 아는 판단

1. POST 방식 API (주소에 조건이 보이지 않을 때)

 - get을 했는데도 주소가 그대로인 경우(제대로 실행되지 않는 경우)
 - 조건이 주소가 아니라 요청 본문에 실려있다는 것
 - POST 요청을 해야함 (body가 달라짐). payload를 봐야함

 (1) payload의 2종류

 - body(request URL)에 키=값 형태로 들어가있는 경우 : . Network의 Payload가 항목별로 나뉘어보이고, Content-Type은 x-www-formurlencoded

 - JSON BODY 형태로도 되어있음(중괄호 덩어리) : Payload가 JSON 트리 형태. Content-Type은 application/json. 최근 사이트 트헨드

 - 구분하는 법 :  Payload 탭에서 “view source”를 누르면 실제 전송 원문이 보임. **중괄호**로 시작하면 JSON

 (2) data= 와 json= : requests에서 둘은 완전히 다른 인자

 |  | data= | json= |
 | :--: | :--: | :--: |
 | 보내는형태 | key=value&key=value | {"key": "value"} |
 | Content-Type | x-www-form-urlencoded (자동) | application/json (자동) |
 | Network 표시 | Form Data | Request Payload |
 | 잘못쓰면 | 400 또는빈결과 | 400 또는빈결과 |
 
 ** payload - view source상에서 보이는 형태에 따라 data를 쓰거나 json 사용

 (3) Content-Type 맞추기 : 맞추지 않으면 서버가 해석하지 못함

 - data= 와j son= 은 Content-Type을 알아서 붙여줌

 * 올바른조합
 - requests.post(URL, data=payload) # form
 - requests.post(URL, json=payload) # js

 * 흔한실수— 직접헤더를쓰면서형태를안맞춤
 - headers = {"Content-Type": "application/json"}
 - requests.post(URL, data=payload, headers=headers) # 불일치. 결과가 비어있으면 불일치를 먼저 의심

 (4) Form Data 재현 : Payload 항목을 그대로 딕셔너리로 옮김

 - POST 응답이 JSON이 아니라 HTML인 경우, BeautifulSoup으로 파싱
 - 페이지 번호 바꿔 반복하면 전체 수집됨

 (5) JSON Body 재현 : 중첩 구조는 딕셔너리를 그대로 옮김

 <예시>
 payload = {
 "filter": {
 "category": "IT",
 "region": ["서울", "경기"]
 },
 "page": 1,
 "size": 20
 }
 r = requests.post(URL, json=payload)
 data = r.json()

 * 리스트·중첩 딕셔너리도 json=이 알아서 직렬화

2. 헤더 좁히기

 (1) 403 오류 이유

 - User-Agent가 없거나 python-requests 그대로면, 프로그램임을 대놓고 알리는 셈
 - 브라우저는 어느페이지에서 왔는지(Referer)를 알림. 그것없이 API만 직접부르면 거부하는 서버가 있음
 - X-Requested-With 처럼 “이건 화면안에서 나간요청”임을 표시하는 관례적헤더가 필요한 경우가 있음

 (2) User-Agent

 - 가장 먼저 붙여야 할 헤더 : Network의 Request Headers에서 그대로 복사하면 됨

 (3) Referer

 - 어느페이지에서 이 요청이 나갔는지 알림 : Network에서 해당요청의 Referer 값을 그대로씀

 (4) 그 밖의 단골 헤더

 |코드명 |넣을 값| 설명|
 | :--: | :--: | :--: |
 | X-Requested-With | XMLHttpRequest | 화면 안에서 나간 비동기 요청임을 표시. 이 값이 없으면 거부하는 서버가 있음 |
 |Origin | 요청출처 | POST 요청에서 함께 검사하는 경우가 있음. Referer와 짝으로 넣음 |
 |Accept | 원하는형식 | application/json 을 명시해야 JSON을 주는 서버가 있음 |
 |Accept-Language | 언어 | 한국어 응답을 받으려면 ko-KR이 필요한 경우가 있음 |

 (5) Authorization과 토큰

 - Bearer 토큰이 붙어 있다면 인증이 필요한 API
 - 토큰이 고정값이면 헤더에 넣어 재현할 수 있음 (예. Authorization: Bearer eyJhbGciOi...)
 - 자바스크립트가 매번 새로 만들어내는 값이면, 재현이 어려움
  * 예. X-Signature: 8f3a... (요청마다 변함)

 (6) 최소 조합 찾기

 - 전부 복사(cURL의 헤더를 통째로 넣고 성공 확인)
 - 하나씩 제거(위에서부터 지우기)
 - 지웠더니 깨지는(실패하는) 헤더가 '필수헤더'
 - 남은 것만으로 코드 정리

3. 쿠키와 페이징

 (1) 쿠키와 세션 : 서버가 "왔다간 사람"을 기억하는 방법

 - HTTP는 각 요청을 서로 다른 모르는 것으로 취급
 - 그래서 서버는 첫 응답에 쿠키를 심어 다음요청에서 중복여부 확인
 - 검색조건, 장바구니, 로그인 상태가 이런 방식으로 유지됨

 <예시>
 ** 첫요청
 서버→ Set-Cookie: SESSIONID=abc123
 ** 다음요청
 나 → Cookie: SESSIONID=abc123
 * 이걸 자동으로 해주는 것이 Session

 (2) requests.Session() : 쿠키를 알아서 달고다님

 - 헤더도 한번만 설정하면 이후 요청에 모두 적용됨
 - 커넥션을 재사용해 속도도 빨라짐
 - 순서가 중요할 떄가 많음

 <예시>
 ** s = requests.Session()
 ** s.headers.update({"User-Agent": "Mozilla/5.0 ..."})
 ** 1. 목록페이지를 먼저 방문해 쿠키를 받는다 : s.get("https://example.com/list")
 ** 2. 같은 세션으로 API를 부른다 : r = s.get("https://example.com/api/items?page=1")

 (3) 페이징 4가지 (형태구분)

 - page 번호형 : page=1, 2, 3.....
  * 빈 리스트를 종료 신호로 삼는 것이 가장 안전(break 설정)
  * 안전장치로 최대페이지 상한을 함께 걸어두는게 좋음
  * 양이 많을 경우, 끝페이지 내용을 확인해봐야 한다

 - offset-limit형(sql과 유사하게) : offset=0&limit=20 → 20씩 건너뜁
  * limit을 키우면 요청 수가 줄어 서버부담도 줄어듦(대신 limit 최대치가 있음)
  * offset을 limit이 아닌 다른 값으로 올리면 데이터 오류 발생

 - cursor형 : 응답으로 나온 값을 다음 요청에 넣게 설정 (수식으로 값이 자동 업데이트되도록 설정해놓은 것)
  * 처음부터 끝까지 수집(페이지 번호를 정할 수 없음)
  * nextCursor가 None이 되는 시점이 유일한 종료신호

 - 스크롤 트리거형 : 스크롤할때마다 요청이 나가게 설정한 것 (내리면 계속 새로 생기는 것)
  * 겉모습만 다를 뿐, 위의 세 유형중 하나에 해당함
  * Network를 열고 두어번 스크롤해보면 정체가 드러남

 (4) 종료 조건 설계 : 빈 응답 확인, 중복 감지, 상한선 설정

4. 종료 방법

 (1) 동적으로 만들어지는 토큰 : 자바스크립트가 매번 계산해 붙이는 값
 - 요청마다 값이 달라지고, 규칙이 코드 안에 숨어있음
 - 복원하려면 난독화된 스크립트를 해독해야하므로, 비용이 급격히 증가함

 (2) 멈춰야 하는 신호
 - 응답이 암호화되어있는 경우 : Preview에 읽을수 없는 문자열만 보인다면, 복호화 키가 클라이언트 코드 안에 있다는뜻
 - 캡차가 나오는 경우 : 사람인지 확인하는 코드가 있다는 것은 자동 수집을 원치 않는다는 것
 - 로그인이 필요한 경우

 (3) 그러면 멈춰야 하는 신호는 어떻게 판단하는가
 - 다른 경로가 있는가
 - 약관은 무엇이라고 얘기하고 있는가
 - 비용가치가 있는 데이터인가
 - Selenium이면 되는가

 (4) robots.txt 확인 : 수집하기 전 확인
 - 사이트의 명사적 의사표시 부분
 - Disallow된 범위는 수집하지 않는 것
 - crawl_delay가 있으면 그 값을 지연시간으로 씀

 <예시>
 ** from urllib.robotparser import RobotFileParser
 ** rp = RobotFileParser()
 ** rp.set_url("https://example.com/robots.txt")
 ** rp.read()
 ** rp.can_fetch("*", "https://example.com/api/items")   # True / False
 ** rp.crawl_delay("*") # 권고대기시간

 (5) 수집 전 체크리스트

 - robots.txt의 해당경로 : disallow가 아닌 것을 확인
 - 이용약관의 자동수집 조항 : 금지 문구가 없을 것
 - 공식 API 존재 여부 : 앖거나 필요한 데이터를 못 주는지 확인
 - 요청 간격과 동시 연결 : 0.5초 이상, 2~3연결 이하로 설정
 - 수집 데이터의 성격 : 개인정보가 아닐 것

 (6) 막혔을 때 순서
 - Payload 확인(get인가, post인가)
 - 필수 헤더 찾기
 - 세션 유지(세션으로 쿠키 묻기)
 - 페이징 판별
 - 멈춤 판단