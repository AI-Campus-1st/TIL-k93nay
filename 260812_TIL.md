### 260812 학습내용

### Python으로 DB 다루고 분석하기 ###

1. 연결과 CRUD

 (1) Python에서 DB 사용

- 재현가능성 : 코드로 남으면 다음달에 똑같이 다시 돌릴수 있음
- 자동화 : 스케줄링을 통해 정해신 떄에 자동시행 가능
- charset : utf8mb4. 여기서도 예외없음

 (2) 보안 - 비밀번호 코드에 쓰지 않기
 깃(github)에 올라간 비밀번호는 보안사고

*** .env (깃에올리지않는 내용을 담는 곳)
DB_HOST=127.0.0.1
DB_USER=analyst
DB_PASSWORD=analyst1234!
DB_NAME=shop_db

*** .gitignore (env파일은 git에 올라가지 않음)
.env  # 접속정보는 코드가 아니라 환경(환경변수 파일)에 둡니다


 (3) DictCursor – 조회 결과를 딕셔너리로
 튜플 인덱스는 확인하기 어렵기 때문에, 키:값 모두 가져오는 dictcursor 사용

 - fetchone() : 딕셔너리 하나만 가져옴 (홍길동, 이연걸 딕셔너리 내역 중 하나만)
 - fetchall() : 딕셔너리 모두 가져옴 (홍길동, 이연걸 딕셔너리 내역 모두)
 - fetchmany() : 딕셔너리 다수 가져옴


 (4) SQL Injection – 문자열로 SQL을 만들지않기
 %s 는 포매팅이 아니다: 타입과 무관하게 항상 %s만 사용
 값이 하나여도 튜플로 넘깁니다 : (name, ) 의 쉼표를 빠뜨리지 마세요

 (5) CRUD 실습 (9_3_Pythonn_DB분석.ipynb)

2. 트랙잭션과 대량적재

 (1) commit을 잊으면, 화면에는 보이는데 저장되지 않은 상태가 됩니다

<예시>
conn = get_connection()           # autocommit=False 가기본
with conn.cursor() as cur:
    cur.execute("UPDATE tb_accountSET balance = 999999 where acc_no= ‘A-001’")
    cur.execute("SELECT balance FROM tb_accountwhere acc_no= ‘A-001’")
    print(cur.fetchone()) # 999999로 보입니다
conn.close() # commit 없이 닫으면 롤백
→ 다시 조회하면 원래값 입니다

- 커밋 전 변경은 자기자신에게만 보입니다
- 이것이 '고립성' (다른사람에게는 옛날 값이 보임)

 (2) 롤백(rollback) – 중간에 실패하면 되돌린다

- try / except / rollback / finally — 이 네 줄이 한세트

<예시>
conn = get_connection()
try:
    with conn.cursor() as cur:
        cur.execute("UPDATE tb_accountSET balance = balance -%s WHERE acc_no= %s AND balance >= %s", (amount, from_acc, amount))
        if cur.rowcount== 0: raise ValueError('잔액부족')
        cur.execute("UPDATE tb_accountSET balance = balance + %sWHERE acc_no= %s", (amount, to_acc))
        conn.commit()
except Exception as e:
    conn.rollback()
    print('이체실패: %s', e)
finally:
    conn.close()

 (3) BULK – 1만 건을 넣는 네 가지 방법

 방법                         설명                    1만건소요            왜
execute + 매번commit          한 건 넣고 바로커밋       매우느림             커밋마다 디스크 동기화
execute + 1회commit           다 넣고 마지막에 커밋     느림                네트워크 왕복이 1만번
executemany                  한 문장으로 묶어 전송      빠름                왕복 횟수가 크게 줄어듦
청크executemany               1천건씩 나눠 커밋         빠름+ 안전           메모리·락·재시도 통제

 - 나눠서 넣어야함
  (메모리, 락 시간, 재시도 문제)

3. Pandas 연동

 (1) DB와 DataFrame 사이

<형태>
import pandas as pd

***  DB → DataFrame
df = pd.read_sql(sql, conn)

***  파라미터도그대로
df = pd.read_sql(sql, conn,
params=(5000, 4))

- 데이터 자체를 테이블로 바꿔서 넣는건 잘 하지 않음(to_sql)

 (2) SQL이 할 부분, Pandas가 할 부분

SQL 로
데이터를 작게 만들어 가져오기
필터링· 조인· 그룹 집계
대용량 정렬과 순위 계산
반복 실행되는 표준집계→ VIEW·MART

PANDAS 로
가져온 뒤에 하는일
복잡한 결측치 보간· 문자열 파싱
형태변환(pivot · melt)과 시각화
여러소스(엑셀+ DB + API) 합치기

- Pandas로 대량 데이터를 받아 거르는 것은 비추천

 (3) 대용량은 나눠 읽는다

***  나눠읽기
total = 0
for chunk in pd.read_sql(sql, conn,
chunksize=100_000):
total += chunk['amount'].sum

***  그런데 이게 더 빠릅니다
SELECT SUM(amount)
FROM tb_big_order;

→ 한줄만 받아옵니다

- chunksize를 쓸 때 : 행마다Python 로직이 필요할때(파싱·API 호출등)
- SQL로 끝낼 때 : 합계·평균·건수처럼 집계로 답이 나올때

 (4) 구조화
 연결·커밋·롤백을 매번 쓰지 않습니다

01 식별자검증 테이블·컬럼명은 %s로 바인딩 할수없어 화이트 리스트로 검사
02 자동재연결 ping(reconnect=True)로 끊긴 커넥션을 되살립니다
03 느린쿼리로그 일정 시간을 넘기면 경고로 남깁니다

- with문을 쓰면 닫는 것을 잊지 않음

 (5) Handler 조회

- with문으로 열고 닫기 (반드시 __enter__, __exit__ 있어야함)


4. 탐색적 분석

① 규모   데이터의 총 개수는 몇개인가
② 결측   비어있는 컬럼이 있는가, 몇퍼센트인가
③ 분포   범주형에 예상 못한 값이 섞여있지 않은가
④ 요약   수치형의 범위와 사분위수가 상식적인가
⑤ 이상치 지우기전에 몇건인지 센다
⑥ 관계   조인이 안되는 행이 있는가

<예시>
SELECT COUNT(*) AS 행수,
    COUNT(customer_name) AS name_notnull,
    COUNT(grade) AS grade_notnull,
    COUNT(DISTINCT country) AS country_종류,
    MIN(signup_dt) AS 최초가입,
    MAX(signup_dt) AS 최근가입
FROM tb_customer;

=> COUNT(컬럼) NULL을 세지 않으므로 결측개수가 바로 나옵니다
=> 결측        COUNT(*) -COUNT(컬럼)       — 이 계산만 pandas에서
=> 하지말것     SELECT * 로 데이터를 다가져와서 df.isnull().sum() 하기

 (1) value_counts를 SQL로
 전체 데이터에서 변수들을 중복없이 정리한 다음, 해당 변수들의 COUNT결과와 비중까지 구함

<형태> 
SELECT IFNULL(country, '(NULL)') AS 값,
COUNT(*) AS 건수,
ROUND(COUNT(*) / SUM(COUNT(*)) OVER () * 100, 1) AS 비중
FROM tb_customer
GROUP BY IFNULL(country, '(NULL)')
ORDER BY 건수 DESC;

=> SUM(COUNT(*)) OVER () 그룹 집계 위에 다시 전체합계를 얹습니다
=> IFNULL로 감싸기 : NULL도 하나의 값으로 세어야 놓치지 않습니다

 (2) 사분위수까지 SQL로
Q1(25%), Q2(중위수), Q3(75%) 나누기

<형태>
SELECT MAX(CASE WHEN pct <= 0.25 THEN unit_priceEND) AS Q1,
    MAX(CASE WHEN pct <= 0.50 THEN unit_priceEND) AS 중위수,
    MAX(CASE WHEN pct <= 0.75 THEN unit_priceEND) AS Q3
FROM (SELECT unit_price,
    PERCENT_RANK() OVER (ORDER BY unit_price) AS pct     # 전체 범위 중 몇% 위치에 있는가
FROM tb_product) t;

- 평균 <> 중위값
- IQR = Q3 - Q1

 (3) 지우기 전에 정상데이터인지 확인

<예시>
SELECT '가격0 이하' AS 항목, COUNT(*) FROM tb_product WHERE unit_price <= 0
UNION ALL SELECT '수량0 이하', COUNT(*) FROM tb_order_item WHERE qty <= 0
UNION ALL SELECT '미래날짜주문', COUNT(*) FROM tb_order WHERE order_dt > NOW()
UNION ALL SELECT '주문시점가≠현재가', COUNT(*)        # 주문시점 예전 가격과 현재가가 다를 수도 있음 (지우면안됨, 의미를 위한 중복)
FROM tb_order_item i JOIN tb_product p ON i.product_id = p.product_id
WHERE i.unit_price <> p.unit_price

 (4) 조인이 안되는 행 찾기

 - 상세주문내역이 없는 주문, 분류 없는 상품, 참조가 깨진 행 등등

<예시>
SELECT '주문없는고객' AS 항목, COUNT(*) AS 건수
FROM tb_customer c LEFT JOIN tb_order o ON c.customer_id = o.customer_id
WHERE o.order_id IS NULL
UNION ALL
SELECT '팔린적없는상품', COUNT(*)
FROM tb_product p LEFT JOIN tb_order_item i ON p.product_id = i.product_id
WHERE i.order_item_id IS NULL;

5. 세그먼트와 리포트 자동화

 (1) 고객을 세 축으로 보기

R : RECENCY    -  마지막 구매로부터 며칠 지났는가. 작을수록 좋음
F : FREQUENCY  -  기간내 구매횟수. 클수록 좋음
M : MONETARY   -  누적구매금액. 클수록 좋음

<확인 형식>
SELECT customer_id,
    DATEDIFF('2024-06-01', MAX(order_dt)) AS recency_days,
    COUNT(DISTINCT order_id) AS frequency,
    SUM(qty * unit_price) AS monetary
FROM ... GROUP BY customer_id

 (2) 점수로 바꾸기
절대 값이 아닌 상대 순위로 나누기 (집단마다 특성이 특정 값에 몰려있을 수 있으므로)

- 정렬방향주의 : R은 작을수록 좋으므로 ASC(오름차순), F·M 은 DESC(내림차순)
- 1이가장좋음 : NTILE(3)은 1·2·3을 주므로 1등급이 최상
- 기준일 고정 : NOW()를 쓰면 매번 결과가 달라 비교할 수 없음

<예시>
NTILE(3) OVER (ORDER BY recency_daysASC) AS r_score
NTILE(3) OVER (ORDER BY frequency DESC) AS f_score
NTILE(3) OVER (ORDER BY monetary DESC) AS m_score

 (3) 세그먼트 관리
- 대형 휴면고객(SLEEPING_BIG)을 찾는 것이 RFM의 목표

<예시>
세그먼트                  조건                     무엇을할까
CHAMPION                 R=1 · F=1 · M=1         혜택을유지한다
LOYAL                    R=1 · M≤2               업셀대상
NEW                      R=1 · F=3               온보딩을강화한다
SLEEPING_BIG             R=3 · F=1               자주왔던큰고객— "최우선복귀대상"
CHURN_RISK               R=3                     이탈방지캠페인
NORMAL                   그외                    관찰

 (4) 분석 결과를 테이블로

- 적재 방식 : ON DUPLICATE KEY UPDATE + 청크커밋 — 두번돌려도안전

<형태>
CREATE TABLE tb_mart_customer_rfm (
    snapshot_date DATE NOT NULL,            --  기준일을 PK에 포함 (덮어쓰지 않도록 예방)
    customer_id INT NOT NULL,
    recency_days INT, frequency INT, monetary DECIMAL(14,2),
    r_score TINYINT, f_score TINYINT, m_score TINYINT,
    segment VARCHAR(20) NOT NULL,
    PRIMARY KEY (snapshot_date, customer_id));

 (5) 시트 여러개를 한번에
- 차트까지넣으려면 openpyxl로 이미지삽입
- 저장한 png를 워크시트에 add_image로 붙이거나 또는 엑셀네이티브차트를 씁니다

 (6) 실패할 떄 알수 있게 logging 해야함
- 시각·레벨·모듈이 함께남고, 파일로도 떨어짐
- 처리건수· 소요시간· 실패원인 세 가지는 반드시 남길 것