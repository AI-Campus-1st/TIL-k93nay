### 260828 학습내용

### 웹크롤링 9일차 - 데이터 파이프라인 ###


#### 1. 3계층 구조

 (1) 원본의 중요성

 - 원본을 저장해놓으면, 따로 수집을 반복하지 않아도 파싱만하면 쉽게 데이터를 불러올 수 있음
 - 그렇기때문에 원본이 있으면 비용이 저렴. 시간도 단축

 (2) ERD의 정보화 과정

 - Raw(원본, 절대수정x) - clean(표준화된 단일소스로 정리. 타입, 포맷, 중복 정리) - mart(집계, 파생) 과정
 - Raw는 절대 건들지 않고, 업데이트가 필요하면 테이블을 수정/삭제/생성하여 진행

 (3) 재실행 정책 

 - Raw : upsert를 통해 새로 수집된 것만 업데이트
 - Clean : 전체 재생성 (replace)
 - Mart : 전체 재생성
 - 클린과 마트는 버려도 복구할 수 있어야함


#### 2. 품질이슈

 (1) 품질이슈 6가지

 - 결측 : 값이 비어있음. 없는 것과 0으로 들어가있는 것은 다름

 - 중복 : 같은 것이 여러번

 ** 전체 컬럼이 같아야 중복— 거의 안걸립니다
 ** df.drop_duplicates()

 ** 업무적으로 같은것— 이쪽이 적합
 ** df.drop_duplicates(subset=["obs_dt", "stn_id"], keep="last") 
   * keep="last" : 나중에 수집한 값을 남김

 ** 중복이 많다면 수집 로직에 문제가 있다는 뜻

 - 포맷 불일치 : 같은 뜻인데 다른 표시, 다른 형태
 
 ** 날짜 포맷이 다를 경우
 ** API : 2025-01-01 포털CSV : 2025.01.01
 ** api["obs_dt"] = pd.to_datetime(api["tm"], format="%Y-%m-%d", errors="coerce")
 ** csv["obs_dt"] = pd.to_datetime(csv["일시"].str.replace(".", "-", regex=False), format="%Y-%m-%d", errors="coerce")

 ** errors="coerce" 는 실패를 NaT로 만들고 멈추지 않음
 ** 그래서 변환후 최종 NaT 수를 확인하는 것이 필요
 ** print(api["obs_dt"].isna().sum())

 ** 숫자 파싱(, 등으로 안맞는 경우)
 ** 정규표현식으로 숫자만 남기면 단위가 붙어도 처리됨(단위나 쉼표 걷어내는 처리)
   * def to_num(s):
   * return (s.astype(str)
   * .str.replace(r"[^\d\.\-]+", "", regex=True)
   * .replace("", pd.NA)
   * .pipe(pd.to_numeric, errors="coerce"))

 ** 함수 하나를 만들어 여러 칼럼을 처리하는게 좋음
 ** 빈 문자열은 0이아닌 NaN이 나옴

 - 타입 오염 : 숫자가 문자열인 것 같은 타입 오류
 - 인코딩 : 글자 깨짐
 - 잔여물 : 태그, 공백, 말머리


#### 3. pandas 정제

 (1) Raw 불러오기(payload를 펼침)

 - import pandas as pd, json
 - from sqlalchemyimport create_engine   (접속주소로 SQL 접속)

 - payload는 문자열을 딕셔너리로 되돌림
 ** items = raw["payload"].map(json.loads)    # map(lamda x:json.loads(x))한 것과 동일
 ** df = pd.json_normalize(items)
 ** df["raw_id"] = raw["raw_id"].values    # 원본 연결고리

 - 건수가 많으면 betchsize, chunksize 사용
 - 오류 역추적을 위해 raw_id를 들고가면 좋음

 (2) 타입변환, 칼럼 정리

 - 데이터 정제 전, 미리 칼럼 이름을 설정해놓는게 좋음
 - 출처 칼럼을 남겨야, 나중에 어느쪽 값인지 구분됨

 (3) 문자열 정규화

 - 한글, 기타 문자열이 깨지지 않도록 설정 필요

 (4) 정제함수 재사용

 - 정제 함수는 한 모듈(함수 저장파일)에 모아놓고 사용하는 것이 좋음
 - 파이프라인 코드 사이에 넣어 흩어놓는 것은 좋지 않음
 - 규칙이 바뀌면 모듈에서 수정하면 됨

 (5) 이상치 점검

 - 분포를 먼저 보고, 도메인 상식을 벗어난 이상값이 있으면 파싱 오류
 - describe()로 최소, 최대를 먼저 훑어보는게 좋음

 (6) clean테이블 적재

 - clean은 통째로 갈아끼우는 방식
  *  if_exists="replace" 는 테이블을 지우고 다시 만드는 것

 - 적재 후 인덱스를 거는게 좋음
 - to_sql로는 만드는 것 비추
 - 증분이 필요한 만큼 커지면, 그 때 append + UPSERT

 cf. 한꺼번에 바꿀 때는 ctrl + shift + L 혹은 마우스 + alt + 하나씩 클릭

#### 4. Mart와 검증

 (1) Mart는 질문의 답 같은 것 

 - 미리 계산하고 요약해 둔 데이터 테이블
 -  "지점별월평균기온", "일자별이동평균" 같은 것을 미리 계산하여 정리해두고 가져오는 것
 - 틀리면 clean(원 자료)에서 다시 만들면 되므로, 부담없이 여러개를 만들 수 있음

 (2) 집계 Mart 만들기

 ** monthly = (clean
 **     .assign(ym=clean["obs_dt"].dt.to_period("M").astype(str))
 **     .groupby(["stn_id", "stn_nm", "ym"], as_index=False)
 **     .agg(avg_ta=("avg_ta", "mean"),
 **         max_ta=("avg_ta", "max"),
 **         sum_rn=("sum_rn", "sum"),
 **         days=("obs_dt", "count")))

 (3) 파생지표

 - group by : 하나로 합치는 것
 - transform : 하나로 합친 것을 다시 행단위로 풀어주는 역할 (원 데이터 형태로 풀어줌)

 (4) 품질 검증 리포트

 |항목 |확인 방법 |기준|
 |:--:|:--:|:--:|
 |총 건수 | len(df) | 예상범위 안쪽 인가 |
 |기간커버리지 |min·max 날짜 |빠진 구간이 없는가|
 |결측률 |isna().mean() | 핵심 컬럼이5% 미만인가 |
 |중복률 |duplicated().mean() | 0인가 |
 |변환실패 |NaT · NaN 개수 | 0에 가까운가 |
 |이상치 |describe() | 음수 가격 등이 없는가 |

 (5) 검증을 코드로 진행

 - 지표를 출력만 하면 사림이 확인해야하지만, 경고를 남기면 자동화할 수 있음
 - 사람은 나중에 경고가 뜬 것만 확인하는 것이 실무 방식

 (6) 파이프라인 통합

 - 전 과정(수집, 원본저장, 정제, 분석, 검증, 결과출력, 저장 등)을 한줄로 실행할 수 있게 하는 것
 - Raw만 증분이고, 나머지는 재생성(가장 단순한 멱등 구조)
 ** def run():
 ** load_raw()    # 증분— UPSERT로중복방지
 ** build_clean()    # 전체재생성
 ** build_mart()    # 전체재생성
 ** report()    # 검증지표출력
 ** if __name__ == "__main__":
 ** run()

 - 검증은 두번 실행하고, 건 수를 비교
 - 예. SELECT COUNT(*) FROM mart_monthly;

 (7) 파이프라인 구성 이후 

 - 스케줄 구성 : 자동 실행하도록 구성(cron, Aitflow)
 - 스케일 : 수집은 Scrapy, 저장은 파티셔닝, 처리는 배치분할 등 규모 확대
 - 신뢰유지 : 품질 지표를 실행마다 기록하고, 경고알림 설정

 (8) 파이프라인 요약

 - 수집 > Raw(뭔본보관) > Clean(표준화) > Mart(집계와 파생) > 검증


