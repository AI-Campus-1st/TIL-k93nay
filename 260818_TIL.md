### 260818 학습내용

### 웹크롤링 1일차 ###

1. 데이터 수집과 웹

 (1) 왜 직접 모아야하는가

- 이미 공개된 데이터셋에서 찾지 못하는 데이터 존재
- 질문이 구체적일수록, 갱신주기가 짧을수록, 결합이 자유로울수록 유리
- 필요한 경우, 데이터셋을 찾아와 결합(JOIN)하여 새로운 데이터셋 생성 가능

 (2) 수집 파이프라인 단계(7단계)

- 기획
- 탐색
- 수집 : 웹 크롤링 단계
- 정제
- 저장
- 검증
- 활용

 (3) 어디서 깨지는가

- 탐색 단계 : 구조가 꺠질 수 있음
- 수집 단계 : 중간에 멈출 수 있음
- 정제 단계 : 형식이 어긋나서 되돌릴 수 없음(원본이 없어지는 것)

 (4) 크롤링 역사

- ARPANET : 1969년 미국 국방부산하 고등연구국이 최초의 패킷 교환 네트워크를 구축
- TCP/IP 도입 : 1983년 통신규약 재정비
- World Wide Web : 1989년, 팀버너스리가 문서와 문서를 잇는 하이퍼텍스트 정보검색시스템을 제안
- HTML, HTTP : 1991년, 문서의 형식과 그 규약이 공개되며 웹 오픈

 (5) 클라이언트와 서버 

- 클라이언트가 요청하면 서버가 응답
- 브라우저는 요청을 보내고, 받은 HTML을 사람이 볼 수 있게 그려줌
- 크롤러는 요청을 보내고, 필요한 값만 받아 꺼내줌

 -- # 브라우저가 하는 일 : 요청 > 응답 > 렌더링 > 화면
 -- # 크롤러가 하는 일 : 요청 > 응답 > 파싱 > 데이터

2. HTTP요청과 응답

 (1) HTTP : 웹의 공용어

- HyperText Transfer Protocol — 문서를 주고받기 위한 전송규약
- 요청과 응답은 정해진 순서와 형식을 따름
-- # 요청 : 클라이언트 > 서버
-- # 응답 : 서버 > 클라이언트 
-- 둘 다 요청라인, 헤더, 바디 세부분으로 이루어짐

 (2) 요청의 구조

- 요청라인 : "무엇을 어떤 방식으로 원하는가". 매서드(GET/POST)와 경로가 여기 해당
- 헤더 : 요청에 대한 부가정보. "어떤 프로그램인지(User-Agent), 어떤 형식을 원하는지(Accept)"
- 바디 : 서버에 실어보낼 내용. POST에서 본격적으로 쓰임

 (3) 응답의 구조

- 상태줄 : 요청이 어떻게 처리됐는지 알려주는 숫자. 가장 먼저 확인해야할 값
- 헤더 : 응답 본문의 형식, 인코딩, 쿠키 등 본문 정보
- 바디 : 실제 내용. 우리가 파싱할 대상

https://finance.naver.com/sise/sise_quant.nhn?sosok=0 에서 F12를 눌러
화면 내 구성내역들 확인(network - 가장 첫번쨰 누르고, Headers 확인)
-- General, Response Headers, request headers 존재
-- General : URL, 매서드, 코드잘받아와졌는지(상태), IP주소 등 확인 가능

-- Payload : Quary String parameter 확인 가능. (바디 부분. URL 맨 뒷부분에 있는 문자열)
-- Response : 실제로 응답받은 바디 부분.(함수, 명령어 등 세부데이터)

 (4) GET : URL에 싣는 것

- 주소 중 '?' 뒤에 붙는 값들이 서버에 전달할 정보
- ? 뒤의 변수= 값을 Query Parameter 라고합니다
- 여러개는 & 로 잇습니다 → ?page=4&sort=new

-> 주소만 보면 무엇을 요청했는지 알 수 있습니다
-> 그래서 목록·검색·페이지 이동에 주로 쓰입니다
-> 길이 제한이 있고, 주소창에 그대로 노출됩니다

 (5) POST - BODY에 싣는 것

- 주소에 드러나지 않는 자리에 값을 담아 보냄

-> 주소에 값이 남지않아 길이 제한이 사실상 없습니다
-> 로그인·검색조건 전송·데이터생성에 주로 쓰입니다
-> 주소만으로는 재현할 수 없어, 크롤링 난이도가 올라갑니다
-> Network 탭의 Payload를 봐야 무엇을 보냈는지 알 수 있습니다

 (6) 요약
| GET | POST |
| 값을싣는곳 | URL의Query Parameter | 요청Body |
| 주소에노출 | 된다 | 되지않는다 |
| 길이제한 | 있다 | 사실상없다 | 
| 주로쓰이는곳 | 목록· 검색· 페이지이동 | 로그인· 조건전송· 생성 |
| 크롤링난이도 | 낮다— 주소만바꾸면된다 | 높다— Payload를재현해야한다 |

 ** GET은 SELECT, POST는 CREATE와 유사

 (7) 상태코드 5가지

- 2XX : 성공 (200 OK)
- 3XX : 다른 곳으로 이동 (리다이렉트를 따라가되 무한 루프를 조심해야함) (301 MOVED)
- 4XX : 요청이 잘못됨. 재시도해도 소용없음. 헤더를 고치거나 건너뛰어야함 (404 NOT FOUND 등)
- 5XX : 서버 문제. 잠시 뒤 재시도해야함 (502, 503)

3. 개발자도구와 requests

 (1) 정적 웹 : 서버가 데이터까지 채워넣은 HTML을 그대로 보내줌

- 응답으로 받은 HTML 안에 우리가 원하는 값이 이미 들어있습니다
- requests로 받은 문자열을 그대로 파싱하면 끝
- 예시) # 서버가보내준HTML
<div class="post">
<span>파이썬으로배우는웹수집</span>
<span>2026-08-06</span>
</div>
# 값이 이미 문서안에 있습니다

 (2) 동적 웹 : HTML을 먼저 받고, 데이터는 나중에 따로 가져옴

- 처음받은 HTML에는 빈껍데기만
- 브라우저가 자바스크립트를 실행해 데이터를 추가로 요청
- requests는 자바스크립트를 실행하지 않으므로 빈껍데기만 봅니다
** API등을 가져올 때 동적 웹 사용
- 예시) # 처음받은HTML
<div id="boxoffice_list">
</div>
# 데이터가 없습니다 (영화 목록만 있음)
# 브라우저가 나중에 채워넣습니다 (실제 예매가능 내역 등은 나중에 다른서버에서 불러옴)
** JSON등의 포맷으로 불러옴
** 데이터를 주는 서버에 직접 요청해야 데이터를 불러올 수 있음

 (3) F12 내 일곱개 탭

- Elements : HTML 구조를 트리로 보여줌
 * 화면의 한 조각이 문서의 어느자리에 있는지 확인
 * 태그를 클릭하면 화면에서 그영역이 강조돼 대응관계를 확인
 * 우클릭→ Copy → Copy selector 로 선택자를 그대로 가져올 수 있음
 * Elements가 보여주는 것은 브라우저가 자바스크립트까지 실행한 뒤의 결과
 ** requests가 받은 HTML과 다를 수 있음. 둘이 다르다면, 그 사이트는 동적

- Network : 페이지가 요청한 모든 파일 확인가능. 숨은 API 찾는 곳(동적 웹 사용시 활용)
 * 페이지가 무엇을 주고받았는지 전부 기록됨
 * 
 | Name | 요청한파일 | HTML · CSS · JS · 이미지· 그리고 데이터 요청이 시간순으로 쌓입니다.|
 | Status | 응답코드 | 어떤 요청이 실패했는지 한눈에 보입니다. |
 | Headers | 주고받은헤더 | 요청URL, 메서드, User-Agent, Referer 등 재현에 필요한 모든 정보가 있습니다. |
 | Response | 응답본문 | 서버가 실제로 보낸 내용. 여기에 데이터가 있으면 requests로 그대로 가져올 수 있습니다 |

- Console : 자바스크립트 디버깅. 선택자 즉시 시험 가능
- Application : 쿠키, 세션, 스토리지, 로그인 상태를 볼 수 있음

 (4) requests 

- 파이썬으로 브라우저와 똑같은 요청 보낼 수 있음
- status_code 를 먼저 확인하는 습관 필요
- 200이 아니면 text를 파싱해봐야 의미가 없음
- response 객체 하나에 응답의 모든정보가 들어있음

 (5) 헤더를 붙인다

- 서버가 헤더를 사용해 상대가 브라우저인지 프로그램인지 확인함
- User-Agent가 없으면 곧바로 차단하는 사이트가 많음

** User-Agent: python-requests/2.11.0 > bot으로 오해받기 쉬움(차단 기본 설정)
** ===== 아래처럼 쓰면 됨 =======
** headers = {
    'User-Agent' : 'Mozilla 5.0'  # 사람(브라우저)로 인식
}

 (6) params · timeout · 인코딩

- params를 쓰면 주소를 문자열로 이어붙이지 않아도 됩니다.
- timeout이 없으면 응답없는 서버에서 영원히 멈춤 (시간제한을 둬서, 그 안에 응답없으면 넘어감)
- 한글이 깨지면 encoding을 직접 지정해야함 (파이썬, SQL 사용시와 유사)

<예시>
response.text # str — 인코딩을 추정해 디코딩
response.content # bytes — 원본 그대로
response.encoding = "utf-8" # 한글이 깨질때

4. HTML과 선택자

 (1) 구조, 꾸밈, 동작 (웹페이지 구성)

- HTML : 문서의 구조. 크롤러가 읽는 부분
- CSS : 문서의 꾸밈. 색, 크기, 배치 담당. 크롤링에서는 선택자 문법만 빌려 씀
- JAVASCRIPT : 문서의 동작. (프로그래밍 코드) 클릭에 반응, 데이터 불러옴.


 (2) 자주 만나는 태그

- container : div, section. 데이터 묶음의 경계
- Text : span, p, h1-h6. 글자를 담는 태그. 제목과 본문이 들어가는 곳
- LINK, MEDIA : a, img. href(하이퍼링크)와 src 속성에 주소가 들어가있음. 상세페이지로 가는 열쇠(속성값이 들어가 있음)
- List, Table : ul, li, table, tr. 반복되는 데이터의 전형적인 형태. **크롤링 표적**

 (3) 속성 : 데이터가 숨겨진 곳

- id : 문서에서 유일한 이름
- class : 여러 요소가 공유하는 분류 (여러번 등장 가능)
- href, src : 이동하거나 불러올 주소 (속성)
- data-* : 개발자가 심어둔 임의의 값
- onclick : 자바스크립트 함수 호출

 (4) DOM트리 : document tree

- HTML은 부모-자식 트리구조 ( HTML > a 로 갈수록 범위 작아짐. '파싱')

- HTML : 문서 전체
- BODY : 화면에 보이는 영역
- DIV, LIST : 목록을 감싸는 상자
- li : 반복되는 한 항목
- a : 우리가 원하는 값

 (5) 선택자는 주소 : 특정 요소를 가리키는 표기법

- 원래는 CSS가 어디를 꾸밀지 지정하는 문법
- 크롤링은 이 문법을 빌려 어디를 가져올지 지정
- 좋은 선택자는 짧고, 구조가 조금 바뀌어도 살아남음

<예시>
** 너무 취약한 선택자
body > div:nth-child(3) > div > ul > li

** 견디는 선택자
.store-list li

 (6) 태그, ID, Class

- 태그는 태그선택자만 쓰면 됨(해당태그 모두 선택)
- ID선택자는 앞에 #붙여서 (유일값이므로 select_one)
- class선택자는 앞에 .붙여서(같은 분류요소 모두 선택)

 (7) 속성 선택자 5가지
  
| 표기 | 조건 | 예시 |
| [속성="값"] | 정확히일치 | a[target = "_blank"] |    
| [속성~="값"] | 해당단어를포함 | div[class ~= "active"] |
| [속성^="값"] | 해당값으로시작 | a[href ^= "/detail"] |
| [속성$="값"] | 해당값으로끝 | a[href $= "ref=public-recruit"] |
| [속성*="값"] | 해당값을포함 | img[src *= "cover200"] |

 (8) 후손과 자식

- .list a       # 후손 — 아무리 깊어도 찾습니다 → 찾음 (구조 변경에 잘 견딤)
- .list > a     # 자식 — 바로 아래만 봅니다 → 못찾음 (정확하지만, 구조변경에 취약)

5. BeautifulSoup : HTML 문자열을 다룰 수 있는 객체로 변경해주는 라이브러리

- response.text 는 그냥 긴 문자열
- BeautifulSoup에 넣는 순간 탐색가능한 트리가 됨
- html.parser는 기본내장, lxml 은 더 빠름

 (1) find · find_all : 이름과 속성으로 태그를 찾는 것

- soup.find("title")               # 가장 위의 것 **하나만** 가져옴
- soup.find("li", class_="type1")  # class로 좁히기(class는 파이썬예약어라 class_ 로 씀)
- soup.find(id="contentarea")      # id 로찾기
- soup.find("a", attrs={"class":"thumb"})
- soup.find_all("li")              # 조건에 맞는 것 전부(리스트) 가져옴

- 못 찾으면 None 반환. 바로 텍스트를 부르면 오류가 남

 (2) selectt · select_one : css 선택자를 그대로 쓸 수 있음

- 복잡한 조건은 find보다 select가 훨씬 짧음(더 유리)

 (3) 태그를 찾았으면, 내용과 속성을 가져옴

- result.text            # 태그 안의 글자 가져옴
- result.attrs['속성명']   # 속성값 가져옴

 (4) 정적 크롤링 단계

- 관찰 > 요청 > 확인 > 파싱 > 추출