# 260731 학습내용
```md
1. 에러 & 예외처리
예외 처리(Exception Handling) 시, 260730 과제에서와 같이 특정 결과를 출력하게 할 수 있음
```py

<예시>
except ValueError as e:
    print(f"결제 실패: {e}")

(1-1). 문법 에러
> 문법 에러가 있는 프로그램은 실행되지 않습니다.

* 에러 발생 시 `SyntaxError`라는 키워드와 함께, 에러의 상세 내용을 보여줌
* `파일이름`과 `줄번호`, `^` 문자를 통해 파이썬이 코드를 읽어 들일 때(`parser`) 문제가 발생한 위치를 표현
* `parser` 는 줄에서 에러가 감지된 가장 앞의 위치를 가리키는 캐럿(caret)기호(`^`)를 표시

(1-2). 예외(exception)
문법적으로는 옳지만, 실행할 때 발생하는 에러
 * ZeroDivisionError : 0 으로 나누면 에러 발생
 * NameError : 정의되지 않은 변수를 사용하면 에러 발생
 * TypeError : 자료형이 올바르지 못한 경우 에러 발생
 * ValueError : 자료형은 올바르나, 값이 적절하지 않은 경우 에러 발생 
 * IndexError : 존재하지 않는 index(위치값)로 조회할 경우
 * KeyError : 딕셔너리에 존재하지 않는 키 값으로 접근하면 오류 발생
 * ModuleNotFoundError : 모듈을 찾을 수 없는 경우 에러 발생
 * ImportError : 모듈을 찾았으나 가져오는 과정에서 실패하는 경우 (존재하지 않는 클래스/함수 호출)
 * KeyboardInterrupt : 주피터 노트북에서는 정지 버튼이지만, 실제로 우리가 돌릴 때는 ctrl+c를 통해 종료하였을 때

```md
(1-3). 예외 처리
### 기초 문법

```py
try:                 # 일단 시도해보고 예외가 발생되지 않으면 실행 종료
    <코드 블럭 1>
except (예외):        # 안되면, except 실행
    <코드 블럭 2>
```md
* else문은 try-except문과 같이 잘 쓰지 않음. try-except이 끝나고 나서 따로 사용하는 경우 많음

(1-4). finally 
반드시 실행되어야하는 코드를 정의하는데 사용
예외 발생여부와 상관없이 try문을 떠날 때 항상 실행

```py
try:
    <코드 블럭 1>
except 예외:
    <코드 블럭 2>
finally:
    <코드 블럭 3>

```md
(1-5). as
except 구문에서 발생하는 에러 메세지를 코드 블럭에 넘겨줌
```py
try:
    <코드 블럭 1>
except 예외 as err:
    <코드 블럭 2>

<예시>
except ValueError as e:
    print(f"결제 실패: {e}")

```md
2. 예외 발생 시키기
예외를 강제로 발생시킬 수 있음

(2-1). raise
```py
raise <에러>('메시지')

(2-2). assert
상태 검증 시 사용, 무조건 AssertError 발생

assert Boolean expression, error message

assert len([1, 2]) == 1, '길이가 1이 아닙니다.'  # 앞이 False이므로, '길이가 1이 아닙니다.'출력 
```


### 파일 관리 코드 ###   (신규 파일 받은 후 정리)

- 'open('파일경로', 'mode'(r, w, a 중 하나))' : 파일을 여는 내장함수
  ** mode : r(읽기), w(쓰기), a(수정) / wb, rb (바이너리 쓰기, 읽기)
  ** encoding(인코딩 설정 utf-8)

<예시>
```py
f = open('data.csv', 'r')

# 한 줄 읽기 (커서가 이동)
columns = f.readline()        # 한줄씩 읽고 넘기므로, 전에 읽었던 줄을 다시 읽으려면, f.close() 후 다시 열어야함
예) 품목명,주문개수,주문일시,총액

# 여러 줄 읽기 (현재 커서부터 끝까지)
lines = f.readlines()         # 힌즐씩 하나의 요소로 하여 리스트 형태로 반환
예) ['초코우유,4,2026-07-31 11:03:10,3200\n', '매운새우깡,2,2026-06-30 10:02:35,3000\n', '육개장,1,2026-07-15 20:03:15,1200\n']

columns = columns.rstrip().split(',')  # columns의 오른쪽 줄바꿈이나 공백을 없애고, ','로 각각 구분하여 리스트 생성
예) 반환 시, ['품목명', '주문개수', '주문일시', '총액']
```
- '<변수명>.close'하게되면, 변환된 것으로 파일 마무리 가능
- 이후 columns를 실행하면, 최종 변환 값(리스트)로 출력됨

```py
(1-1). json(Javascript Object Notation) 쓰기 / 읽기(list, dict, str, int 가능)
웹 통신에서 데이터를 주고 받을 때 사용하는 포맷

(1-2). CSV (Comma-Separated Variables) 
데이터를 주고받을 때 쓰는 확장자