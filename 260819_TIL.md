### 260819 학습내용

### 웹크롤링 2일차 - 정규표현식 ###

1. 왜 정규표현식인가

- 선택자를 사용하지 못할 때, 원하는 걸 꺼내기 위해 사용

 (1) 한 덩어리 안의 일부 : “문의: 02-123-4567 (담당김철수)” > 02-123-4567만 뽑아내는 것
 (2) 형태만 아는 값을 뽑아내고 싶을 때
 (3) 지워야 하는 것을 지정하는 것

<형태>
/(?=")https:.*(?=")/g
=> 큰따옴표(")로 둘러쌓인 형태의 https: URL 주소들을 텍스트 전체에서 모두 찾아내기 위한 패턴

- 값을 찾는 대신, 값의 모양(패턴)을 적어 찾는 것

-- # 찾고 싶은 것
-- 02-123-4567 031-9876-5432
-- # 모양으로 적으면
-- [0-9]{2,3}-[0-9]{3,4}-[0-9]{4}

- re 모듈지도
파이썬에서 정규표현식을 다루는 창구는 하나

| :---: | :---: | :---: |
| 검색 | match · search | 조건에 맞는 첫 항목을 찾아 Match객체를 돌려줍니다 |
| 수집 | findall · finditer | 조건에 맞는 것을 모두 모읍니다. 리스트이거나 반복가능한 객체입니다 |
| 변경 | sub | 조건에 맞는부분을 다른 문자열로 바꿉니다. 정제의 핵심 도구입니다 |
| 분해 | split | 패턴을 구분자 삼아 문자열을 나눕니다 |

2. 검색 함수

 (1) match : 처음부터 검색. 시작 부분만 검사하므로 중간에 있는 값은 못찾음

 (2) search : 전체에서 하나 검색. 처음에서부터 가장 먼저 나온 객체를 찾아서 반환(존재여부 확인)
  * 없으면 None을 써야함. 바로 .group()(매치된 텍스트 부르기)를 하면 오류가 남
   => if문으로 써서 오류가 나지 않게 해야함

 (3) findall : 매치되는 모든 문자열을 전부 리스트로 반환해주는 함수
  * 못 찾아도 빈 리스트라 반복문에 넣어도 안전함

 (4) finditer : 위치까지 필요할 때, match 객첵(iterator 객체) 반환

 (5) match : 검색에 성공하면 돌려받게 하는 함수
 * for m in re.finditer('orange', text):
 * print(m.group()) - orange 출력
 * print(m.span()) - 매치된 단어의 시작 인덱스와 끝 인덱스를 '튜플' 형태로 한 번에 출력
 * print(m.start()) - 매치된 단어가 시작되는 시작 위치(인덱스)만 출력
 * print(m.end()) - 매치된 단어가 끝나는 끝 위치(인덱스)만 출력

3. 메타 문자 (정규표현식 문법)

 (1) 특별한 기호가 없으면 적은 그대로 찾음. 한글도 찾음 (단, 대소문자 구분)
   * re.IGNORECASE를 주면 대소문자를 구분하지 않고 찾음

 (2) ^ 과 $ — 시작과 끝 : 문자의 경계 지정
  * ^ : ^바로뒤의 문자로 시작하는 문자열 찾음
  * $ : $바로앞의 문자로 끝나는 문자열 찾음

 (3) . : 모든 글자가 하나씩 잡힘. '...' 갯수(3개)에 맞게 글자를 묶어 추출 가능
  * HTML을 다룰때는 . 보다 [^>] 처럼 좁혀쓰는 편이 안전
  * re.DOTALL을 주면 줄바꿈까지 포함

 (4) [] : 범위 지정. 해당범위 내의 문자를 찾을 때 사용
 * 표현식: [orn] # o 또는 r 또는 n 한글자
 * 표현식: [0-9]{3,4} # 숫자 0~9 사이 3~4개
 * 표현식: [0-9][0-9] # 숫자 0~9 사이 2개
 * 표현식: [가-힣] # 한글 범위 중 한글자
 * 표현식: [A-Za-z] # 영문한글자

 (5) [^] : 대괄호 안의 것 빼고 전부 찾음
 * [^a-z] : a-z를 "제외하고"    # ^ 가 대괄호 “안” 에 있으면 부정, “밖” 에 있으면 시작
 * [\.\*] : 기호처리는 "\" 사용 
 * 표현식: [^A-Za-z] # 영문이 아닌것
 * 표현식: <[^>]+> # 태그 전체
 * [^ ]+ : 공백이 아닌 문자열 전체 찾을 수 있음

 (6) ( ) 그룹과 | 선택
 * 표현식: (orange | like)  # orange "또는" like

 (7) 수량자
 * '*' : 0번이상  예. a[bd]*c -> ac · abc · abdc
 * '+' : 1번이상 예. [^>]+ -> 태그 안의 모든 글자
 * '?' : 0번 또는 1번 예. https? -> http · https

 (8) {} : 갯수 지정
 * 표현식: .{5}   # 다섯글자
 * 표현식: [abc]{3}    # a·b·c 중 세글자
 * {2,} : # 2개 이상 가져옴

 (9) 축약형 (대소문자 서로 반대 의미)
 * \d : 숫자형. [0-9]  <-> \D : [^0-9]
 * \w : 문자열. [a-zA-Z0-9_]  <-> \W : [^a-zA-Z0-9_]
 * \s : 공백· 탭· 줄바꿈  <-> \S : 공백이 아닌것

 (10) 이스케이프 : 기호 자체를 찾고 싶으면 \ 사용
 ** r"..." 을 쓰면 역슬래시를 두번 적지 않아도 됨
 ** re.escape() 로 문자열 전체를 안전하게 만들수도 있음

4. 실전과 정제

 (1) ?= : 전방탐색
 
 예. 표현식: oran(?=ge!) : ()안의 글자는 포함하지 않고, 그 앞의 "oran"만 찾음

 (2) ?! : 후방탐색   (예전버전 : ?<=)

 예. 표현식: (?!ora)nge! : "nge!" 찾는 것

 - 전방탐색, 후방탐색을 같이하면 가운데 값을 정확히 뽑을 수 있음

 (3) ?! : 부정형 (자주 쓰이지는 않음)
 - 필요없는 문자형을 제거하고 싶을 때에도 사용

<형식>
--# span안에 br태그가 없는것만
--<span>((?!<br>).)*</span>

 (4) sub : 변경, 치환하는 함수 (replace와 비슷)
  * 빈 문자열로 바꾸면 “삭제”
  * 태그· 엔티티· 말머리를 걷어내는데 씀
  * 치환할 자리에 함수를 넣어 값에 따라 다르게 바꿀수도 있음

 (5)  데이터 수집 흐름상에서 Regex 역할 (정규표현식의 역할)

* Cleansing – 필요없는 부분을 삭제하고 깔끔하게 정제하는것

* fetch() : 요청을 보내고 응답
-> parse() : HTML에서 필요한 걸 꺼냄
-> clean() : 꺼낸 값을 정제함
-> save() : 결과를 파일이나 DB에 씀

 (6) 재사용할 정제함수
* 자주 사용할만한 함수를 한번만들어두고 재사용

<예시>
** import re, html

** def clean_text(s):
** s = re.sub(r"<[^>]+>", "", s)        # 남은태그
** s = html.unescape(s)                 # &nbsp; 같은엔티티
** s = re.sub(r"\s+", " ", s)           # 연속공백
** s = re.sub(r"\[[^\]]*\]", "", s)     # [단독] 말머리
** return s.strip()