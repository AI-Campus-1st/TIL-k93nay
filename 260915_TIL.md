# 260915 학습내용

## 머신러닝 2회차

### 1. 로지스틱 회귀

 (1) 분류 모델

 - 이메일 분류, 종양 분류 같이 분류할때 사용
 - 분류방식에 따라 아래와 같이 모형을 나눌 수 있음
  * 분류모형

     (가) 판별함수 모형 vs 조건부확률 모형
     - 판별함수 모형 : 퍼셉트론, 서포트벡터머신, 신경망 등
     - 조건부확률 모형 : 판별 모형, 생성 모형

     (나) 판별 모형 vs 생성 모형
       - 판별 모형 : 로지스틱 회귀분석, 의사결정 나무, KNN
       - 생성 모형 : 나이브 베이지안

 (2) 확률적 선형 분류 모형

  - 서로 다른 변수 군집을 **구분짓는 경계선**을 만드는 것이 선형으로 분류한다는 것
  - 해당 선은 0.5(무작위)로, 0,1을 구분짓게 됨

 (3) 로지스틱 회귀분석의 가설함수

  - 일반적인 선형함수로는 숫자가 발산하기 때문에, 적합하지 않음.
  - 0-1사이의 값이 나오도록 함수 설정 필요
  - 그 함수가 **시그모이드 함수** (0-1 사이의 실수값만 가지는 함수)
  - 시그모이드 함수는 유한한 구간(a,b) 사이의 한정된 값과 양의 기울기를 가지는 함수를 말함
    * $$ \text{logistic}(z) = \frac{1}{1 + \exp(-z)} $$

```mermaid
xychart-beta
    title "g(z)"
    x-axis [-6, -4, -2, 0, 2, 4, 6]
    y-axis "Value" 0 --> 1
    line [0.00, 0.02, 0.12, 0.50, 0.88, 0.98, 1.00]
```


 (4) 로지스틱 회귀분석의 cost

 - $$ \text{Cost}(h_\theta(x), y) = \begin{cases} -\log(h_\theta(x)) & \text{if } y = 1 \\ -\log(1 - h_\theta(x)) & \text{if } y = 0 \end{cases} $$

 - 이를 일반화하면, $$ \text{Cost}(h_\theta(x), y) = -y \log(h_\theta(x)) - (1 - y) \log(1 - h_\theta(x)) $$

 - 이렇게 계산되는 cost값들에 대해 평균을 내는 것이 cost 함수임
   $$ J(\theta) = -\frac{1}{m} \sum \text{Cost}(h_\theta(x), y) = -\frac{1}{m} \sum -y \log(h_\theta(x)) - (1 - y) \log(1 - h_\theta(x)) $$

 - 이를 gradient descent하면 결정 경계를 찾을 수 있음


 (5) 다중분류

 - 일반적인 선형 분류 모형의 경우, **일대다 방식**으로 다중 분류문제 해결
 - 로지스틱 회귀분석의 경우, **softmax**라는 방식으로 해결할 수 있음
   $$ S(y_i) = \frac{e^{y_i}}{\sum_j e^{y_j}} $$

 - 소프트맥스(softmax)
     * 예측 결과를 '확률'로 바꿔줌
        - 딥러닝 모델이 계산을 끝내면 [2.0, 1.0, 0.1] 처럼 애매한 점수(Logit)를 뱉어냅니다. 소프트맥스는 이 점수들을 모두 더하면 1(100%)이 되는 확률 데이터로 전환
        - 예: [2.0, 1.0, 0.1] ➡️ 소프트맥스 변환 ➡️ [0.65, 0.24, 0.11] (개일 확률 65%, 고양이 24%, 호랑이 11%)
    
     * 공식의 의미
        - 분자 ($e^{y_i}$): 예측한 특정 클래스의 점수를 지수함수(\(e\))에 넣은 값
        - 분모 ($\sum_j e^{y_j}$): 존재하는 모든 클래스의 지수함수 값을 싹 다 더한 총합
        - 즉, "전체 점수 총합 중에서 이 클래스가 차지하는 비율"을 구하는 간단한 비율 공식

     * 지수함수(e)쓰는 의미
        - 음수 점수 해결: 모델이 -1.5 같은 음수 점수를 내더라도, \(e^{-1.5}\)은 항상 양수가 되므로 확률 계산이 가능
        - 큰 값 강조 (Softmax의 진짜 이유): 지수함수는 값이 커질수록 가파르게 증가합니다. 그래서 높은 점수는 더 돋보이게(Soft) 만들고, 낮은 점수는 확실하게 깎아내려(Max) 모델이 정답을 더 확실하게 확신할 수 있도록 만들어 줌

### 2. 서포트벡터머신

 (1) SVM도 선형 분류모형. 2개 카테고리 데이터를 분류하는 선형 결정경계를 찾는 것이 목적

  - 마진 : 경계선에서 가장 가까운 점까지의 거리. 마진이 클수록 좋음(경계가 명확해짐) = **결졍경계는 안정적**
  - 서포트 벡터 : 마진을 결정하는 기준이 되는 X값을 서포트벡터라고 함(가장 가까운 점)
  - 초평면 : 결정 경계선을 기준으로 각 긍정/부정 클래스의 데이터와 가장 가까운 곳에 위치한 경계선
    * 서포트 벡터(가장 가까운점) 기준으로 결정경계와 **수평**
    * 긍정 초평면과 부정 초평면 사이의 거리 = 마진

 (2) SVM의 가설함수

 - 0 또는 1로 예측 : $$h_\theta = \begin{cases} 1 & \text{if } Z \geq 0 \\ 0 & \text{otherwise} \end{cases}$$

 - SVM의 COST 
    * SVM의 cost는 z=1 or -1이면 값이 0이 된다. 이를 hinge loss라고 하는데, 비확률적 분류모형인 SVM의 cost로 사용하기에 적합함
    $$Cost(h_\theta(x), y) = \begin{cases} \max(0, 1 - \theta^T x) & \text{if } y = 1 \\ \max(0, 1 + \theta^T x) & \text{if } y = 0 \end{cases}$$

```mermaid
xychart-beta
    title "Cost for y = 1"
    x-axis [-3, -2, -1, 0, 1, 2, Z]
    y-axis "Cost" 0 --> 4
    line [4.0, 3.0, 2.0, 1.0, 0.0, 0.0, 0.0]
```
```mermaid
xychart-beta
    title "Cost for y = 0"
    x-axis [-3, -2, -1, 0, 1, 2, Z]
    y-axis "Cost" 0 --> 4
    line [0.0, 0.0, 0.0, 1.0, 2.0, 3.0, 4.0]
```

 - Cost식을 일반화하면, $$J(\theta) = C \left[ \sum_{i=1}^{m} y^{(i)} \text{Cost}_1(\theta^T x^{(i)}) + (1 - y^{(i)}) \text{Cost}_0(\theta^T x^{(i)}) \right]$$


### 3. 퍼셉트론

 (1) 사람의 뉴런을 모방한 형태인 퍼셉트론. 뉴런은 다수의 입력이 합산되어 하나의 통로로 출력됨
  * 뉴런 : 일정 값 이상으면 다음 뉴런으로 신호를 보내고, 그 이하면 아무것도 하지 않음
  * 퍼셉트론 : 입력값이 있고, 각 입력치*가중치를 모두 더한 다음 바이러스(b)를 더한 총합을 0 또는 1로 출력하는 방식

 (2) 활성화함수 : 이떼, 들어온 가중합을 0으로 보낼지 1로 보낼지 판단하는 함수. (총합이 활성화를 일으키는지 판단)
  * 기본 퍼셉트론은 활성화함수로 계단함수 사용

 (3) 퍼셉트론의 가설함수
  $$y = \begin{cases} 0 & , \quad x_1 w_1 + x_2 w_2 + b \leq 0 \\ 1 & , \quad x_1 w_1 + x_2 w_2 + b > 0 \end{cases}$$

 (4) 퍼셉트론의 cost
  $$J(\theta) = \frac{1}{2} \sum_{j=1}^{m} (\widehat{y}_j - y_j)^2$$

### 4. 비선형 모델

 (1) 멀티레이어(다층) 퍼셉트론

  - 원래 퍼셉트론은 AND, OR, NAND와 같은 선형 분류는 해결 가능. 그러나 XOR과 같은 비선형(박스표 형태)은 해결할 수 없음
  - 퍼셉트론을 여러개 합치면 비선형 문제를 풀 수 있음. 그게 **다층 퍼셉트론**
  - 다층 퍼셉트론은 단일 퍼셉트론과 달리 여러개의 퍼셉트론을 그려 비선형 경계선을 만듦. 하지만 모든 비선형 문제를 해결하는 것은 아님

 (2) 다층 퍼셉트론 = 비선형 경계선

  - 다층 퍼셉트론은 원래 X_feature값들을 이용해, 새로운 A_feature들을 만들어낸 다음, A_feature 공간에서 선형 분류를 하는 것
  - 즉, 비선형 문제를 A_feature로 옮김으로서 선형문제로 바꾼 다음 선형 경계선을 그어 문제를 푸는 것

 (3) 다층 퍼셉트론의 가중치 개수

  - 가중치는 직관적으로 모델의 복잡도를 보여줌
  - 각 층(입력층 > 은닉층 > 출력층 사이) 사이에 있는 연결선이 가중치
  - 총 몇 층인지, 각 층의 노드는 몇 개인지 등을 확인해야함

 (4) 다층 퍼셉트론의 가설함수

  - 단일 퍼셉트론 가설함수의 중첩 구조
  - 뒤에서부터 한층씩 읽어보는 것이 전체 가설함수를 이해하는데 수월함

 (5) 다층 퍼셉트론의 cost 

  - 멀티 클래스 분류 모형의 함수와 유사
  - $$J(\Theta) = -\frac{1}{m} \left[ \sum_{i=1}^{m} \sum_{k=1}^{K} y_k^{(i)} \log(h_\Theta(x^{(i)}))_k + (1 - y_k^{(i)}) \log(1 - (h_\Theta(x^{(i)}))_k) \right]$$


### 5. 커널 SVM

 (1) SVM이 풀지 못하는 비선형 문제를 해결하는 커널SVM

  - 데이터셋의 독립변수를 변환한다. 이렇게 변환한 새로운 독립함수를 **기저함수**라고 부른다.
  - 단, 모든 기저함수는 변환한 벡터를 내적한 값으로 사용됨. 중요한 것은 기저함수 혼자 사용되지 않는 다는 것. 이를 **커널**이라고 부른다.
    *  $$k(x_i, x_j) = \phi(x_i)^T \phi(x_j)$$

 (2) 자주 사용되는 커널

  - 대응하는 기저함수가 존재할 경우, 기저함수를 일일이 정하지 않고 직접 커널을 정의하여 사용할 수 있다.
   (가) 커널X : $$k(x_1, x_2) = x_1^T x_2$$
   (나) 다항 커널 : $$k(x_1, x_2) = (\gamma(x_1^T x_2) + \theta)^d$$
   (다) 가우시안 커널 : $$k(x_1, x_2) = \exp(-\gamma ||x_1 - x_2||^2)$$
   (라) 시그모이드 커널 : $$k(x_1, x_2) = \tanh(\gamma(x_1^T x_2) + \theta)$$

### 6. 트리 모델

 (1) 의사결정나무

 - 2가지 경우(선택)로 계속 나눠져서 트리형태를 보이는 모델
 - 경계선이 축에 수직으로 만들어지는게 특징
 - 트리를 결정짓는 기준이 **정보이득**. 즉, 불순도가 낮도록 트리를 결정함
  * 엔트로피(무질서한 정도)를 낮춘 정도 = **정보이득**
  * 엔트로피 (Entropy) 공식
  $$Entropy(A) = -\sum_{k=1}^{m} p_k \log_2 (p_k)$$
  * 이때의 지니 불순도 (Gini Impurity) 공식
  $$Gini(t) = 1 - \sum_{i=1}^{c} p(i \vert t)^2$$

 (2) 의사결정나무의 반복적 분할

  - Recursive Partitioning(반복적 분할) 방법론으로 학습을 진행
  - 엔트로피를 낮추는 방식으로 Featrue를 계속 분할하는 것
  - 엔트로피가 0이 될때까지 진행하기 때문에, 학습데이터셋이 과적합이 일어나기 쉽다

 (3) 가지치기

  - 이 과적합을 방지하기 위해 가지치기(Prunning) 진행
  - 사전/사후 가지치기 존재
  - 세부 가지를 병합한다는 개념으로 이해하는 것이 수월

 (4) 의사결정나무의 Cost

   $$J(\theta) = \frac{1}{m} \sum_{i=1}^{m} \ell(h_\theta(x^{(i)}), y^{(i)})$$

  - '데이터가 들어왔을 때 정답(0 또는 1)이 나올 확률'을 수학적으로 예쁘게 한 줄로 표현한 식(우도)
   $$p(y \vert h_\theta(x)) = h_\theta(x)^y (1 - h_\theta(x))^{1-y}$$

 (5) 회귀 vs 분류

  - 의사결정나무는 두 가지 모두에 활용 가능
  - 회귀로 사용할 경우, 리프(최종노드)에 놓인 데이터 Y값의 평균값으로 예측값을 만들고,
  - 분류로 사용할 경우, 리프(최종노드)에 놓인 데이터 Y값의 최빈값을 예측값으로 만든다.

 (6) 앙상블 기법

  - 약한 학습기를 여러 개를 합쳐 강한 학습기를 만든다는 개념
  - 배깅, 부스팅, 스태킹 등이 있음
  - 배깅 : 단순하게 약한 학습기를 독립적으로 합치는 것
  - 부스팅 : 연계적으로 이전 학습기를 다음 학습기로 업데이트하는 방식