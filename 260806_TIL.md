### 260806 학습내용

### Pandas ###
- 테이터프레임(엑셀과비슷)과 시리즈라는 자료형
- 데이터 분석을 위한 다양한 기능을 제공하는 라이브러리
- R의 데이터프레임에 영향
- 내부적으로 numpy를 사용하므로 함께import

자료형
Pandas  |   Python
object  |   string
int64   |   int
float64 |   float
datetime64 |

```py
Pandas의 Series는 numpy의 1차원 array와 비슷
하지만, 모든 자료형을 다 포함해서 사용 가능(모든 요소를 객체화)
numpy의 1차원 array도 취급가능
그외 인덱스, 슬라이싱, 건너뛰기, boolean, 연산 모두 가능(numpy의 특징을 거의 다 갖고있음)

<예시>
x = [1, 2.0, '3', 4, 5]
series_x = pd.Series(x) 
series_x 

0      1
1    2.0
2      3
3      4
4      5
dtype: object

-----------------------

x[0] # 1
x[1: 3] # 2, 3
x[::-1] # 5, 4, 3, 2, 1
x[x > 3] # 4, 5
x + 1 # 2, 3, 4, 5, 6
x * 10 # 10, 20, 30, 40, 50
x + x # 2, 4, 6, 8, 10
```
1. Series Index
- Series는 기본적으로 index와 value로 구분
print(x.index) # 인덱스
print(x.values) # 값

- 인덱스명을 변경할수있다.
```py
x = [1, 2, 3, 4, 5]
x = pd.Series(x, index=['a','b','c','d','e’])

print(x['a'])       # 명시적 인덱스접근           # 1
print(x[0])         # 묵시적 인덱스접근           # 1
print(x[['a','e']]) # 팬시색인, 한번에 여러값 접근  (a의 묵시적 값, e의 묵시적 값, 데이터타입 모두 반환)
# a 1 
# e 5 
# dtype: int64
print(x.a)          # 또다른 접근방법  (a의 묵시적 값)  # 1  # 인덱스명을 속성으로 취급하여 가져오는 방법

```
2. Dictionary to Series
- 딕셔너리를 사용해 Series를 만들면 key값을 index로 사용
```py
x = {"수학":90, "영어":80, "과학":95, "미술":80}
x = pd.Series(x)
print(x)

수학    90
영어    80
과학    95
미술    80
dtype: int64
```
- 인덱싱, 슬라이싱, 인덱스값을 지정해 일부만 series로 생성하는 것 가능

3. Multi Index
- 인덱스를 여러 개 가질 수 있음
```py
<예시>
student_1 = {"수학": 90, "영어": 80, "과학": 95, "미술": 80}     # 홍길동
student_2 = {"수학": 70, "영어": 90, "과학": 100, "미술": 70}    # 이몽룡

=> # stu_1의 90점은 '홍길동'의 '수학'점수를 뜻함 (인덱스 2개)

- 첫 번째 인덱스
    index_1 = ['홍길동' for i in range(len(student_1))] + ['이몽룡' for i in range(len(student_2))] # 이름
- 두 번째 인덱스
    index_2 = [i for i in student_1] + [i for i in student_2] # 과목명

- 값(value)
    values = [value for value in student_1] + [value for value in student_2]

sr_scores = pd.Series(values_all, index=[index_1, index_2])   # index_1의 index_2에 대한 value값 모음
=>
홍길동  수학     90
       영어     80
       과학     95
       미술     80
이몽룡  수학     70
       영어     90
       과학    100
       미술     70
dtype: int64

```
4. 결측값 (NaN, None) 처리
- 비어있는 값 처리
- 결측값을 무시하고(제외하고) 계산
- 결측값 개수 확인, 제거, 다른값으로 채우기 가능
x.isnull().sum()    # 결측값 갯수 합계
x.notnull().sum()   # 결측값뺴고 갯수 합
x.dropna()   # 결측값 제거
x.fillna(0)  # 결측값에서만 다른값 넣기

5. concat
- 데이터 연결, numpy concatenate 기능
- 그냥 합치면, 인덱스 번호가 겹칠 수 있음
```py
- pd.concat([x, y, z], ignore_index=True)를 사용해 합치면서 인덱스 번호를 다시정리
- pd.concat([x, y, z], axis=1, ignore_index=True)를 사용해 축 변경, 인덱스 값들을 모아 순서대로 정리(재배열)
  * 결측값이 있을 경우, NaN으로 보여줌(그리고 다른 값들은 float로 바뀜)
<예시>
	0	1	2
0	0	4.0	7.0
1	1	5.0	8.0
2	2	6.0	9.0
3	3	NaN	NaN

- pd.concat([x, y, z], ignore_index=True, axis=1, join='inner')은 교집합만 출력 # 합집합은 join='outer'

```
6. 연산 및 집계함수
- print(x.count())
- print(x.min())
- print(x.max())
- print(x.mean())
- print(x.median()) # 중간값
- print(x.sum())
- print(x.std()) # 표준편차
- print(x.var()) # 분산
- print(x.mad()) # 절대표준편차
- print(x.describe()) # 기초통계모두
- print(x.head(2)) # 앞의 일부데이터 확인
- print(x.tail(2)) # 뒤의 일부데이터 확인




### DataFrame ###

1. DataFrame
- 2차원 테이블 데이터 구조, 엑셀(스프레드시트)과 비슷
- # columns으로 데이터가 들어감  # index - 인덱스칼럼
```py
	연도	판매량	매출	순이익
0	2015	103	500000	370000
1	2016	70	300000	190000
2	2017	130	400000	300000
3	2018	160	550000	480000
4	2019	190	700000	600000
5	2020	230	680000	590000

- sales_data['판매량']    # columns 기준으로 조회

- 행기준(인덱스 기준)으로 데이터 조회
    print(sales_data.iloc[1])    # 하나는 Series 형태로
    print(sales_data.iloc[1:3])  # 여러줄은 DataFrame 형태로

- pd.DataFrame(sales_data[['판매량','매출','순이익']].values, columns=['판매량','매출','순이익'], index=sales_data['연도'])
    # 원하는 columns만 조회, 연도를 index로 치환
		판매량	매출	순이익
연도			
2015	103	500000	370000
2016	70	300000	190000
2017	130	400000	300000
2018	160	550000	480000
2019	190	700000	600000
2020	230	680000	590000
```

2. CSV 읽기
```py
# 파일읽기
sales_data = pd.read_csv('sales_data.csv', index_col='연도', header=0, sep=',')
# index_col='연도' == index_col=0

# 읽기 옵션
index_col - 인덱스 칼럼
header - csv 에 헤더가 있을 경우 0, 없으면 None
sep - 데이터를 분리하는 기호 (기본값 : , )

# 파일쓰기
sales_data.to_csv('sales_data_save.csv', encoding='utf-8-sig')

# 쓰기 옵션
endoding - 인코딩
```
3. column 추가
- 특정 dataframe이 주어졌을 때,
sales_data['순이익률'] = sales_data['순이익'] / sales_data['매출'] * 100와 같이
그냥 새로운 column값과 식을 넣으면 됨
만약, 아래와 같이 함수가 주어진다면, apply해서 넣으면 됨
```py
<예시>
def check(n):
    if n > 80:
        return '높음'
    else:
        return '낮음'

sales_data['순이익률_비교'] = sales_data['순이익률'].apply(check)

```
- 그리고 특정 index조건의 dataframe만 가져올 수도 있음
예. sales_data[sales_data['순이익률_비교'] == '높음']
 => dataframe 중 True 부분만 빼냄
- 여러 조건을 같이 걸어서(&) 뺴낼 수도 있음

- 해당 조건에 일치하는것만 데이터 변경 가능 (np.where(조건, True일때 값, False일 때 값))
- 해당 조건에 일치하는것만 바꾸고 나머지는 NaN으로 나오게 할수도 있음
예. sales_data['테스트2'] = sales_data[sales_data['판매량'] < 100]['판매량'] + 50
 => 위 조건에 해당하는 값 외에는 NaN으로 반환
```py
 - 행 추가도 가능
예. df.loc[len(df)] = [2000, 250, 720000, 630000]  # 마지막행 밑에 신규행 추가 및 연도, 판매량, 매출, 순이익 순서대로 들어감
```
 ** loc : 명시한 index로 접근
 ** iloc : 묵시적 index(순서)로 접근

4. column 삭제
- del 사용
- drop 함수사용, inplace 를 True 로 하여 원본에 적용   # 가장 많이 씀. 해당 부분을 리스트 형태로 넣어야함
- 팬시를 활용하여 한번에 여러개 지우기 가능
```py
예. sales_data.drop(sales_data.columns[[0, 2]], axis='columns', inplace=True)
```
- 행삭제 가능
```py
예. sales_data.drop([3, 4, 5], inplace=True) # 3,4,5행 지워지는 것

```
5. DataFrame MultiIndex
<예시>
```py
df = pd.DataFrame(
    np.random.randint(1, 100, size=(4, 4)),
index=[['A' , 'A', 'B', 'B'],['a', 'b', 'a', 'b']],        # index끼리의 수가 같아야함
columns=[['가가','가가','나나','나나'],['가','나','가','나']]
)

=> 상위 인덱스(A, B) 그리고 거기에 각 하위 인덱스(a, b) 생성
=> 상위 column(가가, 나나) 그리고 거기에 각 하위 column(가, 나) 생성

		가가	나나
        가	나	가	나
A	a	18	77	93	67
    b	13	26	9	74
B	a	50	47	66	3
    b	12	38	53	79

```
6. groupby
- 지정한 특정 index나 column명으로 묶어 통계/함수 처리 하는것
```py
예.  df.groupby('창고명').sum()  # 창고명 기준으로 A창고의 판매합, 재고합, B창고의 판매합, 재고합 도출 
```
7. sort_values
- 지정한 특정 index나 column명 기준으로 오름차순, 내림차순 정렬 가능
```py
예. df.sort_values(['판매', '재고'], ascending=[True, False])   # 판매는 오름차순, 재고는 내림차순