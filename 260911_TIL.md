# 260911 학습 내용

## EDA 및 통계 분석

### 1. EDA의 이해

 * EDA란 : 탐색적 데이터 분석. 도표와 통계로 데이터를 살피는 방법
 * 수집한 데이터에서 모든 변수의 분포를 **도표화하고, 시계열을 그래프로 표현**
 * **변수를 변환**하거나 **변수간 상관관계를 파악**하고, 통계정보를 생성
 * **변수간 트렌드· 패턴· 관계**를 찾는 것이 목적
 * 분석 프로젝트 **초기, 가설 수립단계**에서 수행
 * 어떤 모델과 기법을 쓸 것인지 선정하는 것을 보조함

 cf. 보고용 시각화와 시점 차이
   * EDA : 정제(cleansing) 직후, 모델링 직전
   * 보고용 시각화 : 분석결과에 대한 커뮤니케이션 목적으로 연구 마지막이나 의사결정 직전에 행해짐

 (1) 왜 모델링 전에 진행?

  * EDA는 전처리의 설계도 : 무엇을 어떻게 처리할지, 그 결정의 근거를 남기는 것

  | EDA에서 놓치면 | 모델링에서 벌어지는일 |
  | :--: | :--: |
  | 결측을 무심코 평균으로 대체   | 분산과소추정 → 과적합, 성능지표왜곡  | 
  | 이상치 방치   | 회귀계수 급변, 스케일링 붕괴  | 
  | 왜도 큰 변수 방치   | 거리기반 모델(KNN, SVM) 성능저하  | 
  | 다중공선성 미확인   | 회귀계수 부호가 뒤집혀 해석불가  | 
  | 타깃불균형 미확인   | 정확도95%인데 쓸모없는모델  | 
  | 데이터누수(leakage) 미발견   | 검증 성능만 좋은 가짜모델  | 

 (2) EDA 표준 절차

```text
  * 문제 정의 
  - 데이터 개관 
  - 단변량 분석(분포, 빈도, 이상값 등) 
  - 데이터 품질(결측값, 이상값, 중복, 타입오류 등) 
  - 이변량 분석(상관, 교차표, 그룹 비교 등) 
  - 다변량 분석(3개 이상/ 조건부 관계, 교란변수) 
  - 가설 도출 - 통계 검정(가설을 수치로 확인)
```
 (3) 변수 유형

 | 유형 | 타이타닉예 | 요약통계 | 시각화 | 검정 |
 | :--: | :--: | :--: | :--: | :--: |
 |수치형-연속  |Age, Fare  |평균· 중앙값· 표준편차  |히스토그램· 박스플롯  |t-검정· ANOVA |
 |수치형-이산  |SibSp, Parch  |빈도· 최빈값  |막대  |카이제곱 |
 |범주형-명목  |Sex, Embarked  |빈도· 비율  |막대· 교차표  |카이제곱 |
 |범주형-순서  |Pclass  |중앙값· 빈도  |정렬막대  |Kruskal-Wallis |
 |식별자  |PassengerId, Ticket  |—  |—  |분석제외 |
 |텍스트  |Name, Cabin  |파싱대상  |파생변수화후  |— |

 (4) 관통 데이터셋 : 타이타닉

 [실습] titanic_eda_practice.ipynb 활용

### 2. 데이터 품질 - 결측치와 이상치

 (1) 결측치 탐색

 * Missing map : 분홍색 = 결측값 (결측패턴 시각화 : missingno as msno)

 (2) 결측 매커니즘

 * MCAR : 완전무작위결측
 * MAR : 다른 변수로 인한 결측
 * MNAR : 결측값 자체가 원인

 (3) 결측처리 방법

 | 방법 |내용 | 쓸때 | 주의 | 
 | :--: | :--: | :--: | :--: |
 | 행삭제  |결측이 있는 행제거 | 결측이 소수이고 MCAR일 때  |표본손실· 편향위험 |
 |열삭제  |결측률이 높은 변수 제거 | 결측40% 초과 등 기준선 설정  |정보를 통째로 버림 |
 |단일대체  |평균· 중앙값· 최빈값 | 빠른처리가 필요할때  |분산과소추정 |
 |그룹별대체  |Pclass ×Sex 그룹중앙값 | MAR이고 설명변수가 있을때  |그룹이 작으면 불안정 |
 |앞뒤값  |ffill · bfill  |시계열데이터  |추세를 왜곡할수있음 |
 |모델기반  |KNN Imputer · 회귀· MICE | 결측이많고 관계가 뚜렷할때  |계산비용· 과신위험 |
 |결측을피처로  |is_missing 플래그생성  |MNAR일때  | — |

 (4) 평균 대체의 함정
 
 * 분산 과소추정 : 표준편차가 실제보다 작아지므로, 신뢰구간이 좁아지고, 검정 결과가 과대평가된다
 * 상관관계 왜곡 : 다른 변수와의 관계가 없어지거나, 상관이 커질 수 있음
 * 분포 변형 : 평균지점에 집중되어 분포형태가 바뀜

 (5) 이상값 탐지 방법

 | 방법 |기준 |적합상황|
 | :--: | :--: | :--: |
 | IQR  | Q1 − 1.5 ×IQR 미만, Q3 + 1.5×IQR 초과  | 비대칭 분포에도 견고— 기본권장 | 
 | Z-score  | |z| > 3  | 정규분포를 가정할 수 있을때 | 
 | Modified Z (MAD)  | 중앙값 절대편차 기반  | 이상치가 이미 많을때 | 
 | 백분위수  | 상하위1%  | 단순하고 실무적 | 
 | 도메인규칙  | 나이> 130, 금액< 0  | 가장 신뢰도가 높다 | 
 | 모델기반  | Isolation Forest · LOF  | 다변량 이상치 | 

 (6) 이상치는 제거 대상이 아님

 * 제거/수정이 필요한 건 '오류값' : 입력실수, 오작동, 단위 혼동 등(물리적으로 불가능한 값)
 * 보존이 필요한 건 '진짜 극단값' : VIP고객, 재난일 매출, 최고가 승객 등(드물지만 실재하는 현상)
 * 제거, 윈저라이징, 로그 변환, 별도 분석 등을 진행할 수 있음

### 3. 기술통계와 분포, 관계 분석

 (1) 중심경향과 산포 

 * 평균보다는 중앙값(순위기반), 최빈값(범주형)이 극단값에 덜 민감하므로, 항상 같이 확인하는 것이 중요
 * 범위, IQR, 표준편차, CV(변동계수) 등을 통해 산포도 확인 (IQR은 이상치에 견고, CV는 단위가 다른 변수 비교용)
 * 표준편차만으로는 형태를 알 수 없으므로, 다른 것과 함께 사용 필수

 * df.describe(percentiles=[.01,.05,.95,.99]) : 95%, 99% 신뢰수준에 들어가는 비율 확인 가능
 * df['Embarked'].value_counts(normalize=True) : 전체대비 비율 확인
 * pd.crosstab(df['Pclass'], df['Survived'], normalize='index') : 각 인덱스별 비율 보여줌(범주형과 범주형으로 크로스해 데이터프레임 생성)


 (2) 왜도와 첨도

 * 평균과 중앙값이 벌어지는 방향이 왜도
 * 양의 왜도(왜도>0): 소득, 요금, 체류시간 등
 * 첨도 : 극단값이 얼마나 자주 발생하는가 (첨도가 크면 뾰족하고 끝이 두꺼움)
 * 왜도가 크면 로그, 제곱근, box-cox 변환 등을 검토

 (3) 단변량 분포 시각화

 * 왜도 구하는 순서
    * 1. skew(numeric_only=True), 내림차순 정렬
        * skew = df.skew(numeric_only=True).sort_values(ascending=False).drop(['PassengerId'])
    *  2. 데이터프레임 생성 (skew, feature을 칼럼으로하는)
        * skew_df = pd.DataFrame(skew, columns=['skew'])
        * skew_df['feature'] = skew_df.index
    *  3. display(), sns.catplot(막대그래프 생성)
        * display(skew_df)
        * g = sns.catplot(skew_df, x='feature', y='skew', kind='bar')

 * bin이 결론은 바꿈 : 너무 작으면 봉우리가 사라지고, 너무 많으면 잡음이 패턴처럼 잡힘
 
 * 첨도 구하는 순서
    *  1. kurt(numeric_only=True), 내림차순 정렬
        * kurt = df.kurt(numeric_only=True).sort_values(ascending=False).drop(['PassengerId'])

    *  2. 데이터프레임 생성 (kurt, feature)
        * kurt_df = pd.DataFrame(kurt, columns=['kurt'])
        * kurt_df['feature'] = kurt_df.index

    *  3. display(), sns.catplot()
        * display(kurt_df)
        * g = sns.catplot(kurt_df, x='feature', y='kurt', kind='bar')

 (4) 박스플롯과 바이올린

  * 박스플롯 : 사분위수로 그려진 상자형태 그래프. 5가지 수 + 이상치 보여줌. 그룹 간 비교에 효과적 (하지만 이봉분포 등을 놓칠 수 있음)
  * 바이올린 플롯: 박스플롯에 분포까지 그려진 그래프. 이봉분포 등도 확인 가능. (표본이 적으면 과장됨)
  * EDCF : 백분위 확인 등에 유용

 (5) 그룹 간 분포 비교

 * 비교할 때는 반드시 축 범위 통일 필수(sharex, sharey)
 * 눈으로 본 차이는 아직 '가설'

 * **네 가지 그래프의 차이**

 | 종류 | 보여주는 것 | 한계 |
 |:--:|:--:|:--:|
 | `box` | 5수 요약 + 이상치 | 봉우리가 두 개인 분포를 놓친다 |
 | `violin` | 분포의 **형태**까지 | 표본이 적으면 과장된다 |
 | `strip` | 개별 점 전부 | 점이 겹친다 |
 | `swarm` | 겹치지 않게 배치한 개별 점 | 데이터가 많으면 느리고 경고가 뜬다 |

 (6) 타켓 분포와 클래스 불균형

 * 분류 문제에서 가장 먼저 '타겟 비율'을 확인해야함
 * 정확도의 착시가 존재하므로, 클래스 불균형 확인 필수
 * 평가지표 변경(F1-score, Precision/Recall · ROC-AUC · PR-AUC)이나,
 * 오버샘플링, 언더샘플링, SMOTE 등을 적용하거나,
 * 가중치를 부여해 손실함수 단계에서 조정할 수 있다

 (7) 상관계수 고르기

 * 변수 유형이 상관계수 결정한다
 
  |계수 |언제쓰나 |특징|
  |:--:|:--:|:--:|
  |Pearson(피어슨) |두 변수 모두 수치형이고 관계가 직선에 가까울때 | 가장 흔하게 쓰이지만 이상치에 민감하다 |
  |Spearman(스피어만) | 순위만 의미있거나 이상치가 있을때  |값이 아니라 순위로 계산 |
  |Cramér's V(크래머 V 계수) |두 변수 모두 범주형일때 0~1 범위.  |교차표와 함께 본다 |

 * 상관계수와 표본크기를 늘 같이 쓴다.
 * 피어슨과 스피어만을 같이 구해서 차이를 보면, 이상치, 비선형 관계 등 파악 가능

 (8) 상관관계는 인과관계가 아니다

 * 교란변수, 역인과, 선택 편향, 생존자 편향 등은 상관관계일뿐, 인과관계가 아님
 * 산점도는 관계를 보여줄 뿐이다

 (9) 상관행렬과 다중공선성 

 * 히트맵 행렬 같은걸로 상관관계를 파악하고, 모델링 위험을 미리 확인
 * 분산팽창계수(VIF) > 10이면 경고 (독립변수간 상관관계가 강하다는 것 - 신뢰성 저하)
 * 히트맵 상관행렬은 발산형, -1~1로 고정해 표시하도록 한다
 
### 4. 통계적 검정과 리포트

 (1) 왜 통계검정이 필요한가

 * 관측된 차이 결과가 우연인지 아닌지 판단하는 절차
 * 표본은 항상 조금씩 다르게 나옴. 관측된 차이가 이러한 차이로 설명되는 수준인지, 그것을 넘어서는 것인지를 판단하는 것이 통계검정

 (2) 가설검정 3단계

 * 가설을 세운다 : 귀무가설과 대립가설
 * 검정을 돌린다 : p-value를 얻는다
 * 판단한다 : p-value가 유의수준보다 작으면, 귀무가설 기각

 (3) p-value

 * 귀무가설이 참이라고 가정할 때, 관측된 것만큼 극단적인 값이 우연히 나올 확률
 * 오해 : '차이가 없을 확률'이 아닌, '차이가 없다면 우연히 이런 결과가 나올 확률'이라는 뜻
 * 'p가 작으니 효과가 크다'가 아닌, 우연 가능성만 말하는 것
 * 'p가 크니 차이가 없다'가 아닌, 차이를 확인하지 못했다라는 뜻

 (4) 상황별 검정 고르기

 | 묻고싶은 것  | 변수유형  | 쓰는검정  | 타이타닉예 | 
 |:--:|:--:|:--:|:--:|
 | 두 그룹의 평균이 다른가? |  그룹(2개) × 수치  | 독립표본t-검정  | 생존/ 사망그룹의 평균Fare | 
 | 세 그룹 이상의 평균이 다른가? |  그룹(3개+) × 수치  | ANOVA  | Pclass 3개 그룹의 평균Age | 
 | 두 범주가 서로 관련 있는가?  | 범주×범주  | 카이제곱검정 |  Sex ×Survived 교차표 | 
 | 두 비율이 다른가?  | 그룹(2개) × 비율  | 비율검정  | 남녀생존률차이 | 