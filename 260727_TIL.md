# 260727 학습내용

제어문

1. 조건문

IF 조건문 구성

if <expression>:     <- 참/거짓에 대한 조건문 들어감
    <코드 블럭>      <- 참인 경우 문장 수행
elif <expression>:   <- 위 조건이 거짓인 경우, 다음 수행 (복수 조건 시, elif 사용)
    <코드 블럭>
else <expression>:
    <코드 블럭>

위 조건문을 작성할 때, 들여쓰기 매우 중요(4space)

<예시>
score = int(input('점수를 입력하세요 : '))
if score >= 90:
    print('A')
elif score >= 80:     # 굳이 'score < 90' 이라는 내용을 쓰지 않아도 False일 경우 다음식이 수행되므로, 생략 가능
    print('B')
elif score >= 70:
    print('C')
elif score >= 60:
    print('D')
else :
    print('F')


이때, 조건문은 위에서부터 순서대로 조건식을 검증하므로, 순서가 매우 중요

1-1. 중첩 조건문

<예시>
if score >= 90:
    print('A')
    if score >= 95:           # 특정 조건 안에 일부조건으로 더 넣을 내용은 이렇게 중첩형으로 작성하는게 좋음
        print('참 잘했어요.')
elif score >= 80: 
    print('B')

1-2. 조건 표현식(=삼항 연산자)

표현방법 : "참일 때 값 if <조건식> else 거짓일 때 값"

<예시>
value = num if num >= 0 else -num       # 절대값 구하는 표현식(abs())



2. 반복문(loop statement)

2-1. while 반복문
조건식이 참인 경우, 반복적으로 코드 실행(멈추라고 지시하기 전까지 무한 반복 가능)

while <조건식>:
	<코드블럭>

<예시> 합계 구하기
num = int(input('자연수를 입력해주세요: '))
n = 1
ans = 0

while n <= num:
    ans += n
    n += 1

<예시> 가로 5, 세로 9의 별상자 만들기
n = 5
m = 9

while m > 0:
    print('*'*n + '\n')
    m -= 1
    if m == 0:
        break

print(ans)


2-2. for 반복문
for <임시변수> in <순회가능데이터(iterable)>:
	<코드 블럭>

range, list, tuple, str 다 가능

for 반복문의 경우, 임시변수에 다른 요소값을 넣어도 반복시 초기화되어 반복구문에 영향을 주기 못한다.

<예시>
for i in range(10):
    print(i)
    i = 'hello'
    print(i)

0
hello
1
hello
2
hello

(1) range(리스트의 길이)로 인덱스 조작 가능

<예시>
lunch = ['짜장면', '초밥', '피자']
for i in range ( len (lunch) ):
    print( f ' { i+1 }번째 메뉴 : { lunch [i] } ')

1번째 메뉴 : 짜장면
2번째 메뉴 : 초밥
3번째 메뉴 : 피자

(2) enumerate() : 내장함수. 인덱스와 값을 합께 활용 가능

<예시>
lunch = ['짜장면', '초밥', '피자']

list ( enumerate (lunch) )

[(0, '짜장면'), (1, '초밥'), (2, '피자')]

 - 그냥 enumerate (lunch)만 하면, 위치값만 나옴(메모리 주소값)
 - list로 변형 시, 각 값은 tuple로 나옴
 - enumerate에 의해 반환되는 인덱스 시작번호를 바꿀 수도 있음

 <예시>
for idx, menu in enumerate(lunch, start=1):
    print(idx, menu)

1 짜장면
2 초밥
3 피자


(3) break : 반복문 종료하는 구간에 넣으면 됨
for이나 while문에서 사용

(4) continue : 이후의 코드를 수행하지 않고, 다음요소로 조건문 다시 반복

(5) pass : 아무것도 하지 않음. 그냥 지나가는 문구




3. zip(*iterables) : 복수의 iterable 객체를 모아 zip() 해줌
튜플 모음으로 구성된 zip object 반환
각 리스트의 인덱스별로 합쳐서 튜플을 구성한다고 보면 됨

<예시>
girls = ['jane', 'ashley', 'mary']
boys = ['jason', 'eric', 'david']

list ( zip (girls, boys) )

[('jane', 'jason'), ('ashley', 'eric'), ('mary', 'david')]

key와 value값을 지정하고 사용하면, dict으로 형변환도 가능

<예시>

results = []
for i in full_info:
    keys = ['names', 'email', 'address']
    temp = dict( zip (keys, i) )
    results. append (temp)
print (results)

[{'names': 'Jack D Cock', 'email': 'jackcock@gmail.com', 'address': 'dongsack-dong'}, ~~]

