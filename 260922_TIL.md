# 260922 학습내용

## 추천시스템

### 1. 추천시스템 개요

 (1) 개념 

 - 정보 필터링의 일종으로 특정 사용자가 관심을 가질만한 정보를 추천하는 것
 - 협업필터링(collaborative filtering) : 같은 종류의 정보를 읽은 유사한 유저에게 다른(비슷한) 유저가 읽은 정보 추천
 - 콘텐츠기반 필터링(content-based filtering) : 어떤 정보를 읽은 유저에게 유사 정보를 추천하는 것

 (2) 사례

 - 유튜브(영상추천), 넷플릭스(영상추천), 아마존, 쿠팡, 네이버(상품추천), 애플뮤직, 스포티파이(음악 추천)

 (3) 무엇을 vs 어떻게

 - 추천시스템 (1) : 무엇을 추천할 것인가. 추천하는 것은 달라도 원리는 같음 => 콘텐츠를 일련의 숫자목록으로 표시하는 **벡터화**가 핵심 
 - 벡터화를 하면, 두 콘텐츠 사이의 거리(유클리디언/ 가까울수록 유사), 각도(코사인 유사도/ 같은 방향일때 유사) 계산 가능

 - 추천시스템 (2) : 어떻게 추천할 것인가. 
 - DTM (document to matrix): 문서정보를 벡터 행렬로 바꿔서 정리할 수도 있음 (term document matrix라고도 함)
 - 기본적으로 벡터화에는 어떤 정보든 활용할 수 있다(표현하고자 하는 정보 = 차원)
 - 기본 정형데이터 외 범주형 데이터(원-핫 인코딩 등), 텍스트 데이터(문서 행렬화) 등도 정리할 수 있다
 - 추천시스템은 상품, 콘텐츠를 벡터화(=숫자)하여 벡터간 유사도를 계산한 다음, 유사도가 높은 순서대로 정렬하여 추천한다

 - 벡터는 직선상(1차원), 평면상(2차원), 공간상(3차원) 위에 점으로 표현된다.

 (4) 유사도

 - 유클리디안 : 제곱합(직선거리). $$d(p, q)^2 = (q_1 - p_1)^2 + (q_2 - p_2)^2$$

 - 코사인 유사도
    $$\text{similarity} = \cos(\theta) = \frac{A \cdot B}{\|A\| \|B\|} = \frac{\sum_{i=1}^{n} A_i \times B_i}{\sqrt{\sum_{i=1}^{n} (A_i)^2} \times \sqrt{\sum_{i=1}^{n} (B_i)^2}}$$

### 2. 추천시스템 종류

 (1) 지도학습법 (잘 쓰지는 않음)

 (2) 클러스터링(군집화)

 - 콘텐츠 기반 필터링, 협업 필터링, 하이브리드 필터링

    1. 콘텐츠기반 필터링(content-based filtering)
    - 사용자가 특정한 아이템을 선호하는 경우, 그 아이템과 비슷한 콘텐츠를 가진 다른 아이템을 추천하는 방식
    - 이미지, 음성, 태그, 상품 설명서, 리뷰 등과 같은 콘텐츠에 대한 설명으로부터 콘텐츠의 특징 벡터를 추출

    2. 협업 필터링(collaborative filtering)
    - 사용자와 아이템 간의 상호 상관 관계를 분석하여 새로운 사용자-아이템 관계를 찾아주는 것으로 사용자의 과거 경험과 행동 방식(User Behavior)에 의존하여 추천하는 시스템
    - 사용자가 남긴 아이템에 대한 평가(리뷰)를 벡터로 사용
    - 유저와 아이템으로 리뷰 행렬 생성, 유저간 유사도 계산
    - 이렇게 사용자 A와 사용자 B가 유사하다고 할 때, 구매하지 않은 제품이 있다면, 해당 상품을 추천해주는 것이 협업 필터링
    - 잠재의미분석 SVD (유저에 대한 잠재 벡터, 아이템에 대한 잠재 벡터 생성)
    - 혹은 리뷰행렬을 전치하여 아이템에 대한 벡터행렬로 정리하면, 콘텐츠 기반 필터링도 적용할 수 있음

    3. 하이브리드 필터링
    - 콘텐츠 기반 + 협업 필터링
    - 예. 리뷰가 없는 신규 아이템(콜드 스타트 문제) -> 콘텐츠 기반 필터링 사용
    - 반대로 오래된 제품에는 협업 필터링 사용