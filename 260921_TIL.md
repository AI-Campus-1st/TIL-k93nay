# 260921 학습내용

## 딥러닝 6회차

### 1. Auto encoder

 (1) 인코딩, 디코딩

 - 비지도 학습 중 비지도 변환 모델
 - 데이터 X값을 보고, **비슷한 X'값**을 만들어내는 것이 목표 (원본과 최대한 유사하게 만들어내는 것이 목표)
 - 인코딩 레이어와 디코딩 레이어로 나뉘어져 있는데, 인코딩레이어는 "인풋레이어(입력층)-히든레이어(은닉층)"까지고, 디코딩레이어는 "히든레이어(은닉층)-아웃풋레이어(출력층)"까지를 의미힌다
 - 이때, 오토인코더는 인코딩과 디코딩이 서로 대칭. 레이어 개수는 제한 없으나, 반드시 대칭이어야함
 - 그런데 만약, X의 특성보다 은닉층 특성이 많으면 과적합이 일어날 수 있음 (반대의 경우, 과소적합이 일어날 수 있음)

### 2. GAN (생성형 적대 신경망)

 - generator(생성자), discriminator(판별자) 존재
 - 생성자를 완전연결계층으로 구성
 - 실제 데이터와 유사하게 Fake 데이터를 만들어내는 generator와 실제데이터와 구별하는 판별자(discriminator) 사이의 경쟁 학습 방식
 - 이를통해 실제 데이터와 유사한 데이터를 만들어내는 것이 목표
 - G(생성자)가 X와 완벽하게 똑같은 X'를 만들어내면, D(판별자)의 예측 성능은 **1/2**
 - 오토인코더와 달리 노이즈(Noise)로부터 Fake데이터를 만들어냄
 - 이진분류 문제임과 동시에 비지도학습 

### 3. DCGAN

 - UNSUPERVISED REPRESENTATION LEARNING WITH DEEP CONVOLUTIONAL GENERATIVE ADVERSARIAL NETWORKS” 논문을 바탕
 - **Deep Convolutional GAN**이라는 뜻. 즉, 생성자에 CNN모델을 접목한 GAN 모델
 - 완전연결계층과 풀링계층이 없는 것이 특징
 - 안정적인 DCGAN을 위한 가이드라인
    1) pooling layers 대신 stride를 적용한 convolution layer를 사용하자.
    2) Batch Normalization을 사용하자.
    3) 완전 연결된 hidden layer를 없애자.
    4) Generator에서는 output layer만 제외(여기서는 tanh)하고, ReLU를 사용하자.
    5) Discriminator에서는 모든 layer에서 Leaky ReLU를 사용하자.

 (1) batch normalization

 - 정규화, 스케일링 개념
 - 네트워크에서 x속성값들을 train set 전체가 아닌 미니배치에서의 평균과 분산으로 정규화해주는 작업
 - 그 다음, 스케일링&shift과정을 거쳐 새로운 값을 만듦
 - 특정 히든레이어(은닉층)에서 Activation Function을 수행하기 전에 수행함

 (2) CNN에서의 batch normalization

 - 일반적인 batch normalization과 CNN의 batch normalization는 조금 다름
 - CNN의 경우, convolution 성질을 유지하기 위해, 각 Channel마다 각 batch normalization 변수를 만듦
 - 즉, batch size = m, n_channel = n, feature_map size = pxq라면 channel별로 각각 m * pxq개의 feature에 대해 평균과 분산을 구한다는 말 (n개의 평균과 분산을 구하는 것이고, gamma와 beta 또한 n개)
 - 이는 Vanishing/Exploding Gradient(기울기 소실/기울기 폭발)와 같은 불안정성을 해결하기 위해서 시행함
 - 불안정성의 원인이 각 층의 Activation마다 다른 input의 distribution에 있다고 보는 것
 - 그래서 distribution을 mini batch의 평균과 분산으로 normalize하는 것

### 4. Object Detection (객체 탐지)

 (1) R-CNN(2-stage) 기본 개념

    1. 이미지 분류 > 오브젝트 위치 인식(localization)
    - 이미지 분류는 이미지가 어떤 라벨인지 예측하는 것
    - 오브젝트 위치인식을 가능하게 하려면, 이미지와 이미지 오브젝트 라벨, 좌표값을 데이터로 학습시켜야함

    2. 오브젝트 위치인식(Localization)
    - 찾아야 할 오브젝트 수(N개)만큼 classification, localization 네트워크를 각각 구성해서 학습

    3. Object detection
    - 이미지내에 존재하는 모든 카테고리에 대해서 classification과 localization을 수행하는 것
    - 이미지에 따라 존재하는 물체 개수가 일정하지 않기 때문에, 위치인식보다 디텍션이 어려운 문제

 (2)  슬라이딩 윈도우 (Sliding Window)

 - 물체가 존재할 수 있는 모든 크기의 영역(different scale & ratio)에 대해 슬라이딩 윈도우 방식으로 이미지를 모두 탐색하면서 분류 작업을 수행하는 것
 - 즉, 특정 크기의 네모칸(윈도우)를 슬라이딩하면서 구역 전체를 훑고 내려가는 형식
 - 탐색해야하는 영역 수가 너무 많기 때문에, classifier가 충분히 빠르지 않다면 연산시간이 오래 걸림

 (3) Region Proposal

 - 슬라이딩 윈도우의 비효율성을 개선하기 위해 등장
 - 물체가 있을법한 영역을 빠른 속도로 찾아내는 방법
 - 가장 많이 쓰이는 방법이 selective search와 Edge boxes

 (4) Region Proposal with CNN (R-CNN)

 - region proposal을 통해 물체가 존재할 영역을 제안해준 다음, (1-stage)
 - CNN을 통해 Image classification을 수행하는 object detection (2-stage)
 - 진행과정
    1. 이미지 입력
    2. 이미지로부터 Selective search를 활용해 region proposal 추출(약 2,000개 정도)
    3. 각 region proposal 영역을 잘라내고 동일한 크기로 만든 후, CNN을 활용해 feature-map 계산
    4. feature-map에 대한 classification을 수행

### 5. Fast R-CNN (2-stage)

 (1) Fast R-CNN의 핵심

 - 기존의 R-CNN의 “속도”를 개선한 모델
 - 기존 CNN은 한장의 이미지에 대해 2,000개의 region이 제안, 2,000번의 CNN연산을 진행
 - Fast R-CNN은 1번의 CNN으로 region proposal을 수행한 다음, feature map을 region으로 잘라내는 방식으로 바꾼 것

 (2) ROI Pooling

 - Spatial Pyramid Pooling :  제안된 Region으로부터 Classification을 하려면 모든 region을 일정한 크기로 맞춰줘야 하는데, 이를 가능하게한 방법
 - Fast R-CNN에서는 SPPlayer의 single level pyramid만을 사용하며, 이를 RoI Pooling layer라고 명칭
    * SPP layer는 feature map 상의 특정 영역에 대해 일정한 고정된 개수의 bin으로 영역을 나눈 뒤, 
    * 각 bin에 대해 max pooling 또는 average pooling을 취함으로써 
    * 고정된 길이의 feature vector를 가져올 수 있음

### 6. Faster R-CNN (2-stage)

 (1) Faster R-CNN의 핵심

 - 기존의 Fast R-CNN에 Region proposal network을 추가한 것
 - Fast R-CNN에서는 Region proposal 알고리즘으로 인해 생기는 bottleneck으로 속도 문제 여전히 존재
 - Faster R-CNN에서는 이를 해결하기위해 conv feature map으로부터 region proposal을 “직접 생성” 할 수 있는 region proposal network(RPN)를 제안
 - 즉, CNN 내부에 region proposal을 생성할 수 있는 network를 설계함으로써 더욱 빠르면서도 정확한 region proposal을 생성
    * RPN : Region Proposal Network
    * feature map 정보를 활용해 물체가 존재하는 위치를 출력으로 가지는 네트워크

    * window 영역 크기는 미리 정의된 여러 크기와 비율의 reference box를 정해놓는 anchor라는 방식을 사용
    ( 3가지의 크기(128, 256, 512)와 3가지의 비율(2:1, 1:1, 1:2) )
    * feature map 위의 작은 window 영역을 입력으로 받고, 해당 영역에 물체가 존재하는지/존재하지 않는지 수행
    * 하나의 feature map 에서 모든 영역에 대해 물체의 존재여부를 확인하기 위해서 sliding window 방식으로 탐색

### 7. YOLO (1-stage)

- 1 stage object는 regional proposal과 classfication을 동시에 진행 (위의 R-CNN과 다르게)
- 속도는 빠르지만, 정확도가 낮다는 단점 존재
- 대표적인 모델이 YOLO와 SSD (2-stage는 VGG, Resnet, googlenet)

 (1) YOLO 개념

 - You Only Look Once. 이미지 내의 경계박스와 class probability를 단일 회귀문제로 간주
 - 이미지를 한 번 보는 것으로 물체의 카테고리와 위치를 추정
 - single network를 통해 multiple bounding box에 대한 class probablility를 계산하는 방식
 - googLe Net 기반의 응용된 형태

### 8. KNN

 - 데이터셋에서 자신과 가장 가까운 이웃 데이터 K를 찾는 알고리즘
 - 분류와 회귀 모두 가능
 - 분류 : 자신과 가장 가까운 K개의 이웃이 가장 많은 카테고리로 분류
 - 회귀 : 자신과 가장 가까운 K개의 이웃의 y값(Target-Value)의 평균값으로 예측
 - 둘다 K값에 따라 값이 크게 달라질 수 있는데, 이건 사람이 미리 정함(초매개변수)
 - KNN은 학습하는 부분이 없음. 데이터간 유사도만 계산하여 답을 얻을 수 있음 (=lazy learning)

 (1) 유사도 계산법
 
    1. Euclidean Distance (유클리디안)
    2. Manhattan Distance (맨하튼)
    3. Mahalanobis Distance (마할라노비스)
    4. Rank Correlation Distance (순위상관)

 (2) 유사도 거리에 따른 가중합

 - 결과 예측 시, 단순 다수결이 아닌 유사도 거리에 따른 가중합을 계산하여 결과 예측
 - 즉, KNN을 시행할 때는 사전확률을 고려해야함
 - 또한, x값들 간 단위 차이가 있기 때문에 속성간의 정규화작업을 해주어야함