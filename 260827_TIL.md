### 260827 학습내용

### 웹크롤링 7일차 - OPEN API ###


#### 1. OPEN API

 (1) API

 - 기관이 데이터를 주고받는 공식 창구
 - 프로그램을 위한 데이터로 넘겨줌(HTML이 아님. JSON, XML같은 형식)
 - 구조가 문서로 약속되어있음
 - 차단당할 걱정이 없고, 이용 조건이 명시되어 있음
 - HTML을 받아, 파싱해서, 데이터로 고치는 과정이 필요없음

 (2) 크롤링 과 Open API

 | | 크롤링 | Open API |
 | :--: | :--: | :--: |
 | 합법성 | 약관 확인필요 |명시적으로 허용됨 |
 | 안정성 | 구조가 바뀌면깨짐 | 버전으로 관리됨 |
 | 속도 | 파싱비용 있음 | 빠름 |
 | 데이터범위 | 화면에 보이는 전부 | 제공하기로 한 것만 |
 | 제약 | 차단위험 | 쿼터제 |

 (3) API읽는 순서

 - 엔드포인트 : 읽는 위치(요청을 보낼 주소)
 - 요청 변수 : 필수변수(인증키, 페이지, 조회 조건 등), 선택변수
 - 출력 결과 : 응답 결과
 - 에러 코드 : 코드마다 어떻게 처리해야하는지 확인

 (4) 인증 방식

 - API KEY : 쿼리 파라미터나 헤더에 실어 보내는 고유 키. 공공데이터포털, 기상청, 네이버 등이 사용
 - TOKEN : 로그인 후 받는 토큰. 유효기간이 있어 만료되면 갱신해야함. 헤더의 Authorization에 넣음
 - OAUTH : 개인 데이터에 접근할 때 쓰는 방식

 (5) 키 관리

 - OPEN API 키도 .env(환경변수, gitignore)에서 관리해야함 : git에 공유되거나 하면 안되기 때문에 공유되지 않는 곳에 저장
 - 작성하는 부분에서는 '코드'만 작성(키를 가져오는 코드 등). 값은 넣어놓으면 안됨
 - 보통 %등으로 인증키가 만들어져 있으면, unqoute로 풀어준뒤, env에 저장함
  * 예. 공공데이터포털 : [개발계정]기상청_지상(종관, ASOS) 일자료 조회서비스 인증키 - L8TkPzWgGvk7Dj%2Bs53QAFrLzKvuCisogyZuewqoZ39sFLUBzoyCMGHUJZJR%2BvZHG%.........
    => unqoute로 푼 뒤, 나온값은 env에 저장하고 실제 인증키는 코드안에서 삭제
    => 대신 load_dotenv로 env에서 값을 가져오는 코드 작성

 (6) 공공데이터 포털에서 가져오기

 - 오픈API자료 다운로드 후, 자료 내 call back URL을 base_url로 설정
 - load_dotenv()로 인증키 불러오기
 - requests get을 써서 불러오면 됨(기본이 XML이면, JSON으로 변환해야함)
 - parmas에는 필수 값, 필터 조건, pagination에 해당하는 키값들을 가져올 것
  * 자료 내 항목구분이 1인 항목들이 필수값이므로, params에 설정(항목설명에 맞게)
  * params에 paginaation(한 페이지 결과 수, 페이지 수)도 설정할 것
  * 'resultcode : 00, resultMSG : Normal Service'면 제대로 응답된 것
 - key값이 잘못됐을 때, 그외 오류는 오류코드가 다르므로 유의하여 볼 것
 - 키 에러 및 그 외 에러 처리 코드 작성하고(OpenAPIKeyError, OpenAPIError) 
 - 최종 하나의 함수로 묶기 (return까지 작성)
 - size설정에 따라 요청 수를 조정할 수 있음 (size=10은 8번, size=100은 1번만에 가져올 수 있음) (size는 수가 제한되어 있음)

#### 2. 공공데이터포털 실전

 (1) 이용절차

 - 회원가입 > 이용신청 > 키 확인 > 문서 확인(코딩을 위해 필요한 세부 내역) > 호출
 
 (2) 서비스키 인코딩 함정

 - 디코딩해서 원본 Key로 만든다음에, requests 사용해야함 (requests는 자동으로 인코딩하므로, 인코딩된 값을 넣으면 오류가 남)    #  "SERVICE KEY IS NOT REGISTERED ERROR"면 대부분 디코딩하지 않아 생기는 오류
 - URL 문자열에 직접 붙일때만 인코딩 키 사용

 (3) 요청 파라미터

 - dataType=json 을빼면 XML이 돌아오므로 기본적으로 넣는 것 추천
 - numOfRows·pageNo는 포털API의 사실상 표준

 <예시>
 URL = ("http://apis.data.go.kr/1360000/"
 "AsosDalyInfoService/getWthrDataList")
 params = {
  "serviceKey": KEY,
  "dataType": "json",
  "numOfRows": 100,
  "pageNo": 1,
  "dataCd": "ASOS", “dateCd“: “DAY”,
  "startDt": "20250101","endDt": "20251231",
  “stnIds”: 108
 }

 (4) 응답구조와 오류 판정

 - raise_for_status()는 200인데, 오류가 나는 경우가 있음
 - 응답 본문의 resultCode 확인 필요
 - 코드 00을 제외하면, 거의 다 오류 코드. 따라서 에러코드 처리 코드가 반드시 필요함

 (5) 전체 페이지 순회

 - totalcount를 읽어 반복 횟수를 계산
 - 다 돌면 break하고 빠져나올 수 있게 설정해야함
 - totalCount가 없으면 빈 응답을 종료 조건으로 삼음
 - 무한 루프 방지를 위해 최대 페이지 상한도 설정해놓는 것이 좋음
 - items가 1건일 때 리스트가 벗겨지는 상황도 막아야함 (1건이면 딕셔너리로 오기때문에)

 (6) 쿼터 안에서 설계하기

 - 조회 단위를 너무 작게 설정하면, 호출 수가 곧 쿼터를 넘김
 - 하루동안 호출할 수 있는 쿼터를 넘기지 않도록 size 조절이 필요
 - 코드를 쓰기 전에 호출 수를 계산하는 습관이 필요함(numOFRows를 키워 한번에 많이 받는 것이 좋음)

#### 3. JSON과 저장 준비 (원본자체 저장)

 (1) 중첩구조 접근

 - preview트리를 그대로 대괄호로 옮김
 - 리스트가 나오면 반복문, 딕셔너리가 나오면 키접근
 - .get()은 키가 없어도 오류대신 기본값을 돌려줌

 (2) 평탄화 -  json_normalize

 - 중첩JSON을 한줄로 표로 만드는 것
 - key들을 칼럼으로 해서 표로 만들어줌(=dataframe으로 만들어주는 것)
 - 칼럼이름이 축약형이라 추가 확인 필요(원본자료와 비교)

 (3) CSV는 왜 안되는가

 - 중복검사 : csv는 중복검사하려면 파일 전체를 다시 읽어야함
 - 부분 갱신 : csv는 한 행만 바꾸려고 해도 파일 전체를 다시 갱신해야함
 - 동시 접근 : 여러 워커가 같은 파일에 쓰면 꺠짐. csv는 동시성에 적절하지 않음

 (4) API는 파싱한 결과만 저장하면, 나중에 되돌릴 수 있음

 - 파싱 로직은 계속 바뀜
 - 응답원문을 함께 보관하고, 파싱 결과를 파생물로 두고 여러번 사용할 수 있음(파싱 비용은 저렴하므로)
 - 결국 Raw(원본) -> Clean(데이터 정제) -> Mart(데이터 분석 저장소)로 확장됨

#### 4. RDB 기본기

 (1) 자주쓰는 칼럼 타입

 |타입 |쓰는 곳 |비고|
 |:--:|:--:|:--:|
 |BIGINT |고유번호, 큰정수 |AUTO_INCREMENT와 함께|
 |VARCHAR(n) |짧은 문자열 |길이를 정해야함|
 |TEXT · LONGTEXT |본문, 응답 원문 |인덱스를 걸기 어려움|
 |DATE · DATETIME |날짜, 일시 |문자열로 담지말것|
 |DECIMAL(p,s) |금액, 정확한수치 |FLOAT는 오차가생김|
 |CHAR(n) |해시처럼 길이고정 |VARCHAR보다 빠름 |

 (2) PK · UNIQUE · 인덱스

 - PK : 행을 식별하는 고유 키(테이블당 하나)
 - UNIQUE : 중복을 막는 것. 수집에서는 고유키에 거는게 핵심
 - INDEX : 빠르게 찾게 도와줌. 중복을 막지는 않음

 (3) 테이블 만들기

 -- CREATE DATABASE IF NOT EXISTS crawl
 -- DEFAULT CHARACTER SET utf8mb4;
 -- USE crawl;

 -- CREATE TABLE raw_item(
 -- raw_id BIGINT AUTO_INCREMENT PRIMARY KEY,
 -- source VARCHAR(50) NOT NULL,                # 수집 출처
 -- url TEXT,                                   # 요청URL
 -- collected_at DATETIME NOT NULL,             # 수집 일시
 -- payload LONGTEXT NOT NULL,                  # 응답 원본 데이터
 -- content_hash CHAR(64) NOT NULL,             # 중복 체크할 Unique한 값
 -- UNIQUE KEY uq_hash(content_hash),
 -- KEY idx_source_date(source, collected_at)   # 수집출처를 key로 설정해두면, 정렬할때 유용함
 -- ) ENGINE=InnoDBDEFAULT CHARSET=utf8mb4;

 (4) 조회와 집계

 - SELECT, FROM, WHERE, GROUP BY 등 사용

 (5) JOIN 

 - 정리한 표들을 연결

 (6) 인덱스 : 조회는 빨라지지만, 삽입은 느려짐

 - 인덱스는 별도로 정렬된 구조를 함께 유지함
 - 행을 넣을때마다 구조가 갱신되므로 삽입이 느려짐
 - 적재 후 인덱스를 부여하는게 좋음

#### 5. 적재

 (1) pymysql로 파이썬 연결

 -  finally에서 close를 보장해야 커넥션이 새지 않음

 (2) UPSERT

 - 중복 누적되지 않게 처리해줌. 없으면 넣고, 있으면 넣지않음
 - SELECT는 매번 확인해야하므로 느리고, 동시 실행에서 깨짐

 (3) ON DUPLICATE KEY UPDATE

 - 만약 넣으려는 데이터의 고유 키(기본키 또는 UNIQUE 인덱스)가 이미 테이블에 존재해서 충복 오류가 발생한다면, 새로 삽입하는 대신 기존 데이터를 업데이트하라는 명령어

 <예시>

 INSERT INTO raw_item(source, url, collected_at, payload, content_hash)
 VALUES (%s, %s, %s, %s, %s)
 ** raw_item 테이블의 지정된 컬럼들에 값을 집어넣겠다는 뜻. %s는 나중에 실제 데이터가 들어갈 자리 표시자(Placeholder)

 ON DUPLICATE KEY UPDATE collected_at= VALUES(collected_at), payload = VALUES(payload);
 ** 중복이 발생했을 때, 기존에 있던 collected_at과 payload 컬럼의 값을 방금 입력하려고 했던 새로운 값(VALUES(...)에 있던 값)으로 갱신

 => [결과] 데이터가 없으면 새로 방을 파고(Insert), 있으면 새 정보로 교체(Update)하는 일명 업서트(UPSERT) 동작을 수행

--아무것도하기싫다면
--INSERT IGNORE INTO raw_item...

--PostgreSQL
--ON CONFLICT (content_hash) DO NOTHING

 (4) executemany 배치 삽입

 - 묶음 단위. 500~1,000건 단위로 하는 경우가 많음

 (5) API 응답을 원문 그대로 담기

 - json.dumps : 원문 보존
 - Json.loads : 저장 원문 문자열에서 파이썬 객체로 바꿔줌

 (6) OPEN API 과정 요약

 - 신청, 키 관리 (.env) > 호출 (쿼터 안에서 설계) > 파싱(json_normalize) > 스키마(UNIQUE와 인덱스) > 적재(UPSERT, executemany)