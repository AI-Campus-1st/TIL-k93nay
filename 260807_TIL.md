### 260807 학습내용

### 데이터베이스 개요와 개발환경 셋팅 ###

1. 분석가는 왜 DB를 배우는가
- 엑셀로는 데이터를 전부 담기에 부족
- DB를 알고있으면, '데이터 요청 리드타임'이 줄어듦
- 쿼리를 만들면, 엑셀처럼 수정작업을 계속할 필요가 없음
=> SQL을 배우면, 효율적인 업무 가능

2. 엑셀파일과 무엇이 다른가? (DBMS 특징)
- 엑셀, csv 파일과 다르게 ##"동시에", "여럿이"## 한꺼번에 작업 가능
- 원본이 1개로 모두가 동일 값을 보고 업무 진행
- 인덱스가 잘 설정되어있으면, 용량이 커도 빠르게 작업 가능
- 잘못된 작업은 되돌릴 수 있음(롤백)
(실무에서는 PostgreSQL을 많이 사용)

3. DBMS가 하는 일
'무엇을 원하는지(요구)'를 전달하면, '방법'을 정리해줌

- 디스크 저장관리 : 어디에 어떻게 쓸지 정함
- 동시성 제어 : 여러사람이 수정작업을 할 때, 수정 순서(순위)를 정해줌
- 무결성 보장 : 음수 나이, 없는 고객 주문 같은 오류값을 막음
- 보안/권한 : 각 테이블 읽기/쓰기 권한 제어
- 장애 복구 : 연속성 특성과 관련. DB에 한번 저장되면, 증발되지 않음

4. 데이터 베이스 언어 = SQL(Structured Query Language: 구조화된 질의어)
- 분석가는 DML의 ##"SELECT(조회=읽기)'##에 집중

- DDL - 데이터 정의어 : 데이터베이스· 테이블같은구조(스키마)를 만들고(정의하고) 바꿈 
    * CREATE · ALTER · DROP · TRUNCATE

- DML - 데이터 조작어 : 데이터를 추가하고(Insert=create), 조회하고(Select=read) 수정하고(Update) 삭제함(Delete)
    * INSERT · SELECT · UPDATE · DELETE  (*가장 많이 씀)

- DCL - 데이터 통제어 : 누가 무엇을 할 수 있는지 권한을 부여하고, 회수함
    * GRANT · REVOKE 

- TCL - : 변경을 확정하거나 되돌림
    * COMMIT · ROLLBACK

5. 트랙잭션 
- 쪼갤 수 없는 한 묶음
- "전부" 아니면 "전무" : 중간 단계까지만 진행되고 멈추고 저장되는 중간상태를 허용하지 않음
- 이 규칙을 지키기 위한 성질이 ##"ACID"##임

** ACID : 
ATOMICITY   - 원자성 : 전부 반영되거나 전부 취소된다
CONSISTENCY - 일관성 : 데이터는 미리 정의된 규칙에 의해서만 수정이 가능하다
ISOLATION   - 고립성 : 한 트랜잭션을 수행할 때, 다른 트랜잭션은 끼어들지 못한다
DURABILITY  - 지속성 : 성공적으로 수행된 트랜잭션은 영원히 반영된다

6. RDB(관계형 데이터베이스)와 NoSQL의 특징

|             SQL, RDB             |             NoSQL             |
|              RDBMS               |        문서· 키값· 검색엔진      |
|  표(테이블) 모양이 미리 정해져 있음   |     구조를 미리 정하지 않아도됨   |
|   구조가 명확해 유지보수가 쉬움      |   서버를 늘려 수평확장하기 쉬움    |
|        ACID로 정확성을 보장        |  중복데이터의 일관성은 보장 어려움  |
|    조인이 많아지면 느려질 수 있음    |         읽기 속도가 빠름         |
** 최근에는 이 두가지를 혼합해서 쓰는 경우가 많음

7. DBMS 종류
|   계열   |                     대표제품                         |                 특징                  |
|  관계형  |  Oracle · MySQL · MariaDB · PostgreSQL · SQL Server |       표구조 + SQL. 업무시스템표준       |
|  문서형  |                     MongoDB                         |    JSON 형태로 데이터를 자유롭게 저장     |
|  키-값   |                      Redis                          |        매우빠른 임시저장소·캐시          |
| 검색엔진 |                   Elasticsearch                     |            로그·텍스트 전문검색          |
| 분석전용 |           BigQuery · Snowflake · Redshift           |    대용량 집계에 특화된 클라우드DW        |
** MySQL 문법을 익히면 다른 RDBMS 사용 가능

8. 데이터 모델링 
- 어떻게 데이터를 저장할지 설계하는 것(구조화)

- 같은 사실(내용)이 여러 줄에 중복저장되면, 처리하는데 시간이 오래 걸리므로,
  => 중복된 데이터를 어떻게 정리하고, 다른 데이터들과 연결시킬지 구상하는 것이 '데이터 모델링'

<예시>
현실
홍길동이6월11일에우유1개를샀다.
홍길동이6월13일에식빵2개를샀다.

한장의 표로 적으면
 이름 · 전화번호 · 구입일· 상품· 수량
홍길동010-1234-5678 6/11 우유 1
홍길동010-1234-5678 6/13 식빵 2

=> [문제] : 같은사실이 여러줄에 중복 저장됩니다
=> [위험] : 위험 전화번호가 바뀌면 모든줄을 고쳐야합니다
=> [해결] : 해법 고객표와 구매표로 나누고, 이름으로 연결합니다

|이름 전화번호           |
|홍길동 010-1234-5678   |

|이름 구입일 상품 수량    |
|홍길동 2024-06-11 우유 1|
|홍길동 2024-06-13 식빵 2|

9. 데이터 베이스 설계 단계
 1 - 개념적 모델링  |  무엇을 할지 정한다  | 데이터 요구사항을 분석하고, 중요한 자료의 저장유무를 결정 |
   * 중요한 골격이 되는 엔터티와 관계위주로 모델링하고, 세세한 속성보단, 현실세계를 골격화하는데 집중

 2 - 논리적 모델링 (ERD 그리는 것) | Entity – Relation Diagram 설계 | 정보의 구조, 규칙을 명확하게 도식화 |
   * 정규화하여 모델을 일관성있게 관리하고, 중복을 제거하며, 속성이 적절한 엔터티에 배치되도록 진행

 3 - 물리적 모델링  |  실제 DB를 생성     |  MariaDB 문법, 데이터, 인덱스 등 저장 |
   * MariaDB 문법과 데이터타입, 인덱스까지 확정, 실제 저장할 수 있도록 DB를 생성
 
10. MariaDB
- MySQL에서 갈라져 나온, 완전히 무료인 관계형 데이터베이스 (MySQL과 문법 비슷)

11. 서버
- 정보나 서비스를 네트워크를 통해 클라이언트에게 제공하는 컴퓨터시스템

[서버] : 내 컴퓨터 - [클라이언트] : MariaDB 클라이언트 - [포트] : 3306
 * 컴퓨터에는 65536개의 포트(문)이 있음. 각 포트에는 서버가 담겨있음
 * 포트가 막히면(방화벽 설정) 서버 진입 불가

12. MySQL 접속경로
- MySQL 클라이언트에서 실행하거나,
- 명령프롬프트에서 C:\Program Files\MariaDB 12.3\bin> -u - root - p를 실행해 패스워드를 입력하고 실행하거나,
- 환경변수(시스템변수)에 경로를 넣어 추가하는 방법이 있음
** GUI환경 : HeidiSQL - 결과를 표로 보여주고 여러 쿼리 저장 가능

- 명령을 끝낼때는 꼭 ';'을 써야함
SHOW DATABASES; 데이터베이스 목록조회
USE db_name; 작업할 데이터베이스를 고른다
SHOW TABLES; 테이블 목록조회
exit; 접속종료



13. < vs code에 db_intro.sql 생성 >  < 실습 진행 >

--- 데이터 베이스 생성

create database study_db
    character set utf8mb4       # 한글, 이모티콘까지 가능
    collate utf8mb4_unicode_ci;

--- 데이터 베이스 목록 조회

show databases;

--- 데이터 베이스 사용

use study_db;

--- 사용자 생성
create user 'analyst'@'localhost' identified by '1278025kwon@';  # 맨 뒤는 패스워드
--- 주소 자리에 올 수 있는 것 : 특정 IP, 전체주소(%), 특정 컴퓨터(localhost)

--- 권한 부여
grant all privileges on study_db.* to 'analyst'@'localhost';
-- all privileges: 모든권한
-- select : select 권한만

--- 테이블 == 엑셀의 시트

--- 테이블 구성요소
- 칼럼(열) 이름(공백, 한글은 안됨) + 데이터 타입 + 제약조건

<예시>
customer_id INT NOT NULL AUTO_INCREMENT
customer_nameVARCHAR(50) NOT NULL;

** "varchar(문자열), not null(비울 수 없다)"

--- 숫자타입 (범위를 넘으면 저장되지 않음)

| 타입 | 크기 | 표현범위 | 주사용컬럼 |
|TINYINT | 1 byte -128 ~ 127 | 상태코드, 참/거짓플래그 |
|SMALLINT |  2 byte  | -32768 ~ 32767 | 나이, 개수같은작은수 |
|INT |  4 byte | -2,147,483,648 ~ 2,147,483,647 | 일반적인ID |
|BIGINT |  8 byte  | 약±922경 | 로그ID, 대용량테이블PK |
|DECIMAL(p,s) | 가변 | 정확한소수 | 금액, 비율 |
|FLOAT · DOUBLE | 4·8 byte | 근사값 | 과학계산(금액에는 사용하지않음) |
** UNSIGNED를 붙이면 음수 부호를 버리고 양수로 두배까지 저장

--- 금액에 float을 쓰지 않는 이유
float,double은 오차 발생 -> decimal은 정확(돈, 비율 계산에 용이)

--- 문자타입
대부분은 varchar면 됨. 나머지는 이유 있을 때 사용

|타입   |  최대길이   |  특징  |  주사용 컬럼  |
|CHAR(n) | <= 255  | 고정길이. 남는자리를 공백으로 채움  | 국가코드, 우편번호처럼 길이가 같은값  |
|VARCHAR(n) |  <= 65,535 |  입력한 만큼만 저장  |  이름, 주소, 이메일— 기본선택  |
|TEXT |  <= 65,535  |  인덱스 제약이 있음 |  상세설명, 리뷰본문  |
|LONGTEXT  |  <= 4,294,967,295  |  매우 큼  |  로그원문, JSON 덩어리  |
|ENUM |  정해진 값만 |  값추가시 ALTER 필요  |  실무에선 코드테이블을 더선호  |

--- 날짜타입
datetime, timestemp가 있음  => datetime이 유리

DATETIME (1000-01-01 ~ 9999-12-31)
8 byte · 타임존 영향없음(TZ)
저장한 값이 그대로 보입니다
로그·주문 시각의 기본 선택

TIMESTAMP(1970-01-01 ~ 2038-01-19)  # 날짜에 기한이 있음
4 byte · 타임존 자동변환
서버 타임존에 따라 값이 달라보입니다
생성/수정시각 자동기록에 편리

-- 날짜 시간 관련 함수 정리
select now() as now_dt,     # 현재 일시 가져옴
    curdate() as today,     # 오늘 날짜
    date_format(now(), '%Y-%m-%d %H:%i') as dt_format,     # 데이터 형식 지정
    date_add(now(), interval 7 days) as next_7day,     # 7일 뒤 시간
    datediff('2026-12-25', curdate()) as christmas_day,    # 특정날짜까지의 d-day
    year(now()), month(now()), day(now()),
    hour(now()), minute(now()), dayofweek(now())    # dayofweek은 일요일부터 셈
    ;



--- SELECT - 조회할 칼럼 결정

SELECT   무엇을볼지 ← 컬럼(열 == 요소) 조회
FROM     어디서 ← 테이블 정하기
WHERE    어떤조건으로← 행 필터
ORDER    BY 어떤순서로
LIMIT    몇개만

--- DISTINCT - 중복없이 조회

--- ORDER BY - 정렬하기

--- LIMIT - 몇개만 보기    /  OFFSET - 몇개 건너뛰고 보기