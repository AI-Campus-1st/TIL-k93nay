### 260811 학습내용

### 쿼리분석과 ERD 설계 ###

1. INDEX 원리

 (1) 인덱스에 따른 처리시간
 책의 색인과 같아서, 찾아보기가 쉬움(처리시간 단축, Index Seek)

 INSERT, UPDATE, DELETE 같은 것은 데이터 변경작업(인덱스 계산도 들어감)
 SELECT는 인덱스를 활용해서 빠른 조회 진행

 (2) B-Tree 구조
 행이 백만개여도 보통 3~4단계로 탐색 가능(빠른 탐색 가능)
 [리프노드] : 정렬된 값 + 실제 행을 찾아갈 위치(PK)를 담고있음

 (3) PK
 기본적으로 정렬되어있고, 유니크한 특성이므로, PK를 거쳐 찾아가면 빨리 찾아갈 수 있음

 (4) 값의 종류가 적으면, 인덱스가 의미 없음
 하나의 인덱스로 분류되는 값들이 너무 많으므로


2. 인덱스 설계

 (1) 인덱스 사용 순서

 - 등호 먼저 : 조건 컬럼을 앞에, 범위(>, BETWEEN) 조건을 뒤에 둡니다
 - 선택도 높은 것 먼저 : 값의 종류가 많은 컬럼을 앞에 둡니다

 (2) 인덱스 잘 활용하는 법

 - 컬럼에 함수 적용  : WHERE DATE(order_dt) = '2023-05-01'    - [범위 조건으로 바꾼다]
 - 앞부분 와일드카드 : WHERE email LIKE '%@example.com'       - [Full-Text Index 검토]
 - 타입 불일치      : 문자 컬럼에 숫자 비교                     - [타입을 맞춰준다]
 - 부정 조건        : WHERE status <> 'PAID'                 - [긍정조건 IN으로 바꾼다]
 - OR 남용         : WHERE a = 1 OR b = 2                   - [UNION ALL로나눈다]

 (3) 인덱스 종류
 많아지면 읽기에는 유리하지만, 쓰기에는 느려진다(INSERT, UPDATE, DELETE마다 모든 인덱스를 갱신하므로)


3. EXPLAIN
실행하기 전에 물어보기

(1) 어떤 칼럼을 보는가
컬럼           의미                 볼때

type          테이블접근방식         ★  ALL이면 풀스캔
key           실제선택된인덱스       ★ NULL이면 인덱스 미사용
rows          읽을것으로추정한행수    ★ 클수록 의심
filtered      걸러진뒤남을비율(%)    낮으면 헛읽는중
Extra         추가로일어나는작업      ★ filesort · temporary 주의
possible_keys 후보인덱스목록         key와 다르면 왜인지 확인

(2) type 등급표
type      의미                    판정

const     PK·UNIQUE로한행확정      최상
eq_ref    조인에서상대테이블한행씩   좋음
ref       비고유인덱스로여러행      실무목표
range     인덱스범위스캔            양호
-----------------------------------------------------
index     인덱스전체를훑음          주의
ALL       테이블전체를훑음          튜닝대상


(3) Extra를 해석한 튜닝
Extra                  뜻                     대응

Using index            커버링인덱스적중         그대로두면됩니다
Using index condition  인덱스에서미리걸러냄      양호
------------------------------------------------------------------
Using where            서버가추가로필터         인덱스가조건을다못걸렀다
Using filesort         정렬을따로수행           ORDER BY 인덱스검토
Using temporary        임시테이블생성           GROUP BY·DISTINCT 재검토
Using join buffer      조인컬럼에인덱스없음      조인키에인덱스추가

4. Analyze와 쿼리 리팩터링(성능개선)

 (1) 추청과 실측
 EXPLAIN rows - 통계 기반 추정값
 ANALYZE는 r_rows - 실제로 읽은 행 수

 (2) 비용 확인
 FORMAT=JSON은 단계별 비용을 보여줌

- r_table_time_ms 테이블 데이터와 인덱스를 읽는 소요시간
- r_other_time_ms 테이블생성, 정렬 등 기타 가공처리 소요시간
- r_total_time_ms 전체 데이터 조회 소요시간

 (3) 실측 세 컬럼
추정값과 나란히 놓고 비교

컬럼                   뜻                     이렇게읽는다

rows → r_rows         추정행수→ 실제행수       10배 이상 차이나면 통계가 낡았다
filtered → r_filtered 추정통과율→ 실제통과율    실제가 훨씬 낮으면 헛읽고있다
r_total_time_ms       이단계에실제로걸린시간    가장 큰단계가 병목이다

- 괴리가 크면 ANALYZE TABLE로 통계를 갱신한뒤 다시 시행
- 시간이 몰리면 그 테이블의 인덱스부터 수정해야함

 (3) 진단명령, 관리명령

 - ANALYZE <쿼리>
   진단 명령
   쿼리를 실행하고 실행계획 + 실측치를 보여줌
   데이터는 바뀌지 않음
   튜닝할때 씀

 - ANALYZE TABLE <테이블>
   관리 명령
   테이블의 통계정보를 다시 계산합니다
   실행 계획의 품질이 좋아집니다
   대량 적재 후에 씁니다

 (4) 진단 순서
 ① 느린쿼리특정         슬로우쿼리로그또는사용경험
 ② EXPLAIN            type이ALL인가, key가NULL인가
 ③ ANALYZE            r_rows와r_total_time_ms로 진짜병목단계를찾는다
 ④ 가설세우기           인덱스부재· 함수적용· 조인순서· 통계오류
 ⑤ 고친다              인덱스추가또는쿼리재작성
 ⑥ 다시잰다★          ANALYZE로전후를수치로비교한다
 ⑦ 부작용확인          쓰기성능과디스크사용량도함께본다

 (5) 칼럼을 가공하지 않아야함 (그래야 처리속도가 빠름)

 -- 개선전
SELECT COUNT(*) FROM tb_big_order
WHERE DATE_FORMAT(order_dt,'%Y-%m')
= '2023-07';
[type: ALL]
[100만행전부변환후비교]

 -- 개선후
SELECT COUNT(*) FROM tb_big_order
WHERE order_dt>= '2023-07-01'
AND order_dt< '2023-08-01';
[type: range]
[인덱스범위스캔]

 (6) 필요한 칼럼만 인덱스 칼럼으로, 서브쿼리 대신 조인 사용

----- 칼럼 전체 -----
SELECT * → 커버링
SELECT * FROM tb_big_order
WHERE order_dtBETWEEN ...;

----- 특정 인덱스칼럼만 ------
SELECT order_dt, total_amount
FROM tb_big_order
WHERE order_dtBETWEEN ...;


상관서브쿼리→ 조인

멤버별, 주문건수를구할때
(SELECT COUNT(*) FROM tb_big_order o
WHERE o.member_id= m.member_id)
→ 회원마다 반복실행

LEFT JOIN + GROUP BY
→ 한번에 집계

 (7) OR 나누기· 커서페이징
 읽는 양을 줄이는 방법
 - OR → UNION AL
 - 깊은OFFSET → 커서
 페이지가 깊어질수록 OFFSET은 기하급수적으로 느려집니다

 (8) 정렬을 인덱스에 맡기기
 filesort를 없애는 것이 목표

 [개선전] Extra에 Using filesort.
 - 조건에 맞는 행을 모두 모아 정렬

 [개선후] (member_id, order_dt) 복합인덱스 추가
 - 결과 인덱스가 이미 정렬돼 있어 앞에서 10개만 읽고 끝

5. ERD 설계(계층과 정규화)

[[[[[ERDCLOUD(github연결) 사용]]]]]

각 테이블의 KEY(PK, FK 등 포함)를 보여주고, 연결관계를 보여주는 관계도


 (1) Zero or one or many 키로 식별관계도 설정

 - 1) 식별 관계 (Identifying Relationship)
부모 테이블의 기본키가 자식 테이블의 기본키(PK) 및 외래키(FK)로 지정되는 관계.
특징: 자식은 부모 없이 독립적으로 존재할 수 없습니다.
ERD 표기: 실선(—)으로 표시합니다.
예시: 사원 테이블의 사원번호(PK)가 인사기록 테이블의 사원번호(PK/FK)가 되는 경우입니다. 사원이 없으면 인사기록도 존재할 수 없습니다.

 - 2) 비식별 관계 (Non-Identifying Relationship)
부모 테이블의 기본키가 자식 테이블의 기본키가 아닌 일반 속성(FK)으로만 지정되는 관계입니다.
특징: 자식은 부모가 없어도 독립적으로 존재할 수 있으며, 외래키에 NULL(빈 값)이 들어올 수 있습니다.
ERD 표기: 점선(- - -)으로 표시합니다.
예시: 부서 테이블의 부서코드(PK)가 사원 테이블의 부서코드(FK)가 되는 경우입니다. 신입사원은 부서가 아직 정해지지 않아도(NULL) 사원으로 존재할 수 있습니다.

cf) M:N관계 - 중간에 중개테이블을 생성해 1:N관계를 2개 만들어 연결한다 



 (2) 왜 계층을 나누는가

- 01 재적재가능성 정제로직이 틀려도 원본이 남아있으면 다시 만들수 있습니다
- 02 장애추적    숫자가 이상할 때 어느 단계에서 틀어졌는지 좁힐 수 있습니다
- 03 역할분리    적재·정제·분석 담당이 서로의 코드를 건드리지 않습니다
- 04 성능격리    대시보드 조회가 원본 적재 작업과 자원을 다투지 않습니다



 (3) 4개의 계층
원본에서 대시보드까지 한 방향으로만 진행
(아래 계층은 위 계층만 참조. 거꾸로 참조하면 순환 발생)

[Raw > clean > mart > dashboard] 순서 예시 : tb_raw_bike_rental → tb_station · tb_rental → tb_mart_rental_daily → tb_dash_kpi_daily

- 1) RAW [적재만] 

변환하지 않는다.
실패하지 않는 것이 최우선
=> 예. 공공데이터 CSV. 자료형 혼합, 반복문구, NULL 등이 존재하나, 일단 다 담는다

01 전부 문자열 : INT로 받으면 '-' 하나에 적재가 통째로 실패합니다
02 제약최소화  : NOT NULL·FK 없음. 인덱스도 배치키 하나만
03 PK는 대리키 : 의미 없는 AUTO_INCREMENT. 원본에 키가 없을수 있음


- 2) CLEAN [정제]

타입 확정· 이상치 처리
정규화· 불량행 격리

먼저 문서(표, 규칙)로, 나중에 SQL(코드)
raw data가 먼저 만들어져 있어야함

** 정규화 : 반복되는 것을 밖으로 뺴내는 것. 3NF는 '키가 아닌 것에 딸린 것'을 뗴어내는 일
<예시>
-- 정규화 후
tb_station: 대여소번호(PK) · 이름 · 자치구· 주소· 위경도
tb_rental : 대여번호(PK) · 자전거· 대여일시· 대여소번호(FK)
→ 이름은 한 곳에만

<예시2>
tb_station                         |       tb_rental

station_id    (PK)                 |     rental_id    (PK)
station_nm                         |     bike_no
gu_nm                              |     rent_station_id  (FK)
addr                               |     rent_dt
lat / lon                          |     use_min
rack_cnt                           |     anomaly_flag
                                   |     raw_id         → RAW

** 타입확정       DATETIME · DECIMAL · SMALLINT
** UNIQUE       (bike_no, rent_dt) 중복적재차단
** anomaly_flag 버리진않되표시
** raw_id       원본역추적링크

만약 유니크 키가 중복된다면, 키 업데이트로 진행(= 몇번을 실행해도 결과가 같은 '멱등성')
즉, 재실행해도 결과가 같도록 진행
검증 : raw 건수 = clean 건수 + reject 건수 인지 확인


- 3) MART [집계]

분석 질문에 맞춰 테이블을 먼저 그리지 않고,
비즈니스 목적, 설계할 테이블, 행의 의미 등을 미리 계산해 둔다
(PK가 곧 그레인 입니다. PK를 보면 한 행의 의미가 보입니다)

팩트와 디멘전
팩트 : 분석하려는 수치(숫자 데이터). 측정값과 외래키. 행이 매우많다
디멘전 : 그 수치를 '바라보는 관점이나 기준(텍스트 데이터)'. 속성. 행이 적고 자주 바뀌지 않는다

날짜 테이블은 미리 만들어둔다
왜 미리만드나 : 매 쿼리에서 DAYOFWEEK()를 쓰면 인덱스를 못탑니다
공휴일       : is_holiday는 별도로 채워넣습니다. 계산으로는 안 나옵니다


- 4) DASHBOARD [역정규화]

- 조인 없이 한번에 읽도록 넓게편다
- 화면 하나에 테이블 하나
- 성능과 의미 때문에 중복을 발생시키는 과정

01 조인 0회   이름과 좌표를 복제해 넣어 지도까지 한번에 그립니다
02 사전계산   전주 순위·증감까지 미리 계산해 둡니다
03 갱신흔적   refreshed_at으로 언제 만든 숫자인지 밝힙니다
04 증분키     stat_date 기준으로 하루치만 다시 만듭니다

CREATE TABLE tb_dash_station_rank (
stat_date DATE,
rank_no SMALLINT,
station_id INT,
[ station_nm VARCHAR(100), --복제 ]    -- JOIN하지 않고 가져오는 것(역정규)
[ gu_nm VARCHAR(30), --복제 ]
[ lat / lon DECIMAL, --복제 ]
rental_cnt INT,
prev_rank_no SMALLINT,
refreshed_at DATETIME,
PRIMARY KEY (stat_date, rank_no));

- 언제 정규화를 깨는가 : 조인 비용이 설계 원칙보다 비쌀 때
패턴              예시                                  이유

조인용컬럼복제      tb_dash_station_rank.station_nm      매 조회마다 조인제거
사전집계컬럼       tb_order.total_amount                상세를 매번 SUM하지 않음
요약테이블         tb_mart_rental_daily                 100만행 → 365행
카운트캐시         tb_product.review_cnt                COUNT(*) 반복 회피
이력스냅샷         tb_order_item.unit_price             성능이 아니라 의미 (위 4개와 성격 다름)

* 성능을 위한 중복 : 원본과 같아야 함(정합성 검사 대상)
* 의미를 위한 중복 : 원본과 달라야 함(정합성 검사 미대상)

[[ 마지막 실습. 필요한 화면을 테이블로 역산 ]]